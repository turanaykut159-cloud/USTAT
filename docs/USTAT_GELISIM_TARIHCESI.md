# ÜSTAT — Gelişim Tarihçesi (Changelog)

<!--
╔══════════════════════════════════════════════════════════════════════╗
║                   GELİŞİM TARİHÇESİ NASIL TUTULUR?                ║
╠══════════════════════════════════════════════════════════════════════╣
║                                                                      ║
║  Bu dosya projenin TÜM değişikliklerinin kronolojik kaydıdır.       ║
║  "Keep a Changelog" (keepachangelog.com) standardını takip eder.    ║
║                                                                      ║
║  KURALLAR:                                                           ║
║                                                                      ║
║  1. YAPI: Her versiyon bir başlık, altında kategoriler.             ║
║     Kategoriler: Added, Changed, Fixed, Removed, Security           ║
║     - Added    → Yeni eklenen özellik                               ║
║     - Changed  → Mevcut davranışta değişiklik                       ║
║     - Fixed    → Hata düzeltmesi                                    ║
║     - Removed  → Kaldırılan özellik/kod                             ║
║     - Security → Güvenlik düzeltmesi                                ║
║                                                                      ║
║  2. SIRALAMA: En yeni versiyon en üstte (ters kronolojik).         ║
║     Aynı versiyon içinde: en önemli değişiklik ilk sırada.         ║
║                                                                      ║
║  3. UZUNLUK: Her madde 1-2 satır. Detay gerekiyorsa oturum         ║
║     raporuna (docs/YYYY-MM-DD_session_raporu_*.md) referans ver.    ║
║     Changelog = "NE yapıldı", Oturum Raporu = "NASIL yapıldı".     ║
║                                                                      ║
║  4. NUMARA: Her madde #N formatında benzersiz sıra numarası alır.  ║
║     Numara HİÇBİR ZAMAN tekrar kullanılmaz veya atlanmaz.          ║
║                                                                      ║
║  5. REFERANS: Commit hash'i parantez içinde verilir.                ║
║     Örnek: #83 — Vade geçişi düzeltmesi (1d93ffd)                  ║
║                                                                      ║
║  6. DİL: Türkçe. Teknik terimler İngilizce kalabilir.              ║
║                                                                      ║
║  7. VERSİYON: Semantic Versioning (semver.org).                     ║
║     MAJOR.MINOR.PATCH — minor 9'dan sonra major artar.              ║
║     v5.8 → v5.9 → v6.0 → v6.1 ...                                  ║
║                                                                      ║
║  ÖRNEK MADDE:                                                        ║
║  - #83 — Vade günü Top 5 engeli: `<=` → `<` operatörü (1d93ffd)   ║
║                                                                      ║
║  YAPMA:                                                              ║
║  ✗ 50 satırlık detaylı analiz yazma (oturum raporuna koy)          ║
║  ✗ Aynı numarayı iki kez kullanma                                   ║
║  ✗ Numara atlama (#57'den #69'a geçme)                              ║
║  ✗ Kronolojik sırayı bozma                                          ║
║  ✗ "Sorun Analizi / Çözüm / Etkilenen Dosyalar" bölümleri ekleme  ║
║                                                                      ║
║  ESKİ DETAYLI VERSİYON:                                              ║
║  docs/USTAT_GELISIM_TARIHCESI_YEDEK.md (arşiv, referans için)   ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝
-->

---

## [6.0.0] — 2026-04-11 "ÜSTAT Plus V6.0"

### Fixed
- #172 — Widget Denetimi Bulgu A17 (H6+H10) fix: BIST VİOP seans saatleri config/api/frontend zincirine taşındı (C3 — `config/default.json` Kırmızı Bölge sadece yeni `session` bloğu eklendi, risk/strateji/eşik sabiti DOKUNULMADI; backend `api/schemas.py` + `api/routes/settings.py` yeni endpoint; frontend `ErrorTracker.jsx` + `Performance.jsx` + `services/api.js`). **Kök neden:** `ErrorTracker.jsx` EOD geri sayım paneli ve `Performance.jsx` saat-bazlı heatmap'i hardcoded saat sabitleri kullanıyordu: ErrorTracker satır 61-67'de `EOD_CLOSE_HOUR = 17`, `EOD_CLOSE_MIN = 45`, `VIOP_OPEN_HOUR = 9`, `VIOP_OPEN_MIN = 30`, `VIOP_CLOSE_HOUR = 18`, `VIOP_CLOSE_MIN = 15` literal sabitleri; Performance satır 232'de `for (let h = 9; h <= 18; h++) { hours[h] = { hour: h, count: 0, pnl: 0, wins: 0 }; }` literal aralık. Backend `config/default.json::engine.trading_close = "17:45"` (EOD zorunlu kapanış saati, Anayasa Kural #5) ile frontend arasında **bütün senkronizasyon halkası yoktu** — engine.trading_close değişse bile ErrorTracker'daki EOD geri sayımı eski saatte tetiklenmeye devam eder, Performance heatmap'i BIST seans saatleri dışına taşınsa bile eski 9-18 aralığında çizmeye devam ederdi. Audit raporu 11.4 + 17 A17 (H6+H10): "ErrorTracker.jsx içindeki hardcoded saat sabitleri config/default.json.session altından okunsun; aynı şey Performans · Heatmap'teki 9-18 için". **Atomik değişiklikler (6 dosya + 1 test, 0 Siyah Kapı dokunusu):** (1) `config/default.json` — yeni üst seviye `"session": { "market_open": "09:30", "market_close": "18:15", "eod_close": "17:45" }` bloğu eklendi. Kırmızı Bölge #8 dokunusu ama risk/strateji/eşik/baseline/drawdown parametresi **DEĞİŞMEDİ**, yalnız UI'a session hours namespace'i açıldı; engine'in kendi `trading_open`/`trading_close` değerleri (09:45/17:45) dokunulmadı — engine buffered trading window'u kendi ayrı namespace'inde kalır, session namespace'i salt-UI referans. (2) `api/schemas.py` — yeni `SessionHoursResponse` Pydantic modeli (4 alan: `market_open`, `market_close`, `eod_close` str "HH:MM" + `source` config/default/error), default değerleri BIST VİOP sabitleri. (3) `api/routes/settings.py` — `DEFAULT_SESSION_HOURS` fallback dict + `_read_session_hours_from_config()` helper (engine.config'den oku, eksik key'leri default ile merge et, HH:MM regex doğrulaması `^\d{2}:\d{2}$` — geçersiz değer sessizce default'a düşer) + `GET /settings/session` endpoint'i helper'dan okur, `(merged, source)` tuple döner ve `SessionHoursResponse`'a çevirir. (4) `desktop/src/services/api.js` — yeni `getSession()` async fonksiyonu `client.get('/settings/session')` çağırır, hata durumunda BIST default'ları + `source: 'error'` fallback döner (UI asla kırılmaz). (5) `desktop/src/components/ErrorTracker.jsx` — eski 6 hardcoded `EOD_CLOSE_*`/`VIOP_*` sabit KALDIRILDI, yerine `DEFAULT_SESSION_HOURS = { market_open: '09:30', market_close: '18:15', eod_close: '17:45' }` ve `parseHHMM(str)` helper (geçerli HH:MM → `{hour, min}`, geçersiz → null); `getEodInfo(sessionCfg = DEFAULT_SESSION_HOURS)` parametre aldı (önceki sabitleri okumak yerine `sessionCfg.market_open/market_close/eod_close`'u parse eder, parse başarısızsa `DEFAULT_SESSION_HOURS`'a düşer); `useState(DEFAULT_SESSION_HOURS)` yeni `sessionHours` state'i + `useEffect` mount'ta `getSession()` çağırıp state'i güncellediğinde `setEod(getEodInfo(next))` ile ilk değeri de yeniler (cancelled flag ile cleanup); dakikalık `setInterval` artık `getEodInfo(sessionHours)` kullanır ve `sessionHours` dependency'si ile yeniden kurulur. (6) `desktop/src/components/Performance.jsx` — yeni üst seviye `DEFAULT_HEATMAP_HOURS = { market_open: '09:30', market_close: '18:15' }` sabiti ve `parseHour(str)` helper (HH:MM → integer saat kısmı, geçersiz null); `useState(DEFAULT_HEATMAP_HOURS)` yeni `heatmapHours` state'i + `useEffect` mount'ta `getSession()` çağırıp state'i günceller; `hourData` useMemo bloğu eski `for (let h = 9; h <= 18; h++)` hardcoded literal'i KALDIRDI, yerine `openHour = parseHour(heatmapHours.market_open) ?? parseHour(DEFAULT_HEATMAP_HOURS.market_open)` + `closeHour = parseHour(heatmapHours.market_close) ?? parseHour(DEFAULT_HEATMAP_HOURS.market_close)` + `for (let h = openHour; h <= closeHour; h++)`; useMemo dependency array'ine `heatmapHours` eklendi ki session fetch tamamlandığında heatmap otomatik yeniden hesaplanır. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_session_hours_from_config_to_frontend` (Flow 4i) — 6 aşamalı kontrol: (a) `config/default.json::session` bloğu var ve 3 anahtar (market_open, market_close, eod_close) string tipte; (b) `api/schemas.py::SessionHoursResponse` sınıfı var, `market_open`/`market_close`/`eod_close: str` alanları mevcut; (c) `api/routes/settings.py::get_session_hours` endpoint fonksiyonu var, `/settings/session` route kaydı var, `config.get("session"` çağırır (backend senkronu kaybı engelli); (d) `services/api.js::getSession` export var ve `'/settings/session'` endpoint'ine çağrı atar; (e) `ErrorTracker.jsx` `getSession` import eder + `DEFAULT_SESSION_HOURS` fallback var + eski 6 hardcoded sabit literal'i YASAK (`const EOD_CLOSE_HOUR = 17`, `const VIOP_CLOSE_MIN = 15`, vb.); (f) `Performance.jsx` `getSession` import eder + `heatmapHours` state'i var + eski hardcoded `for (let h = 9; h <= 18; h++)` literal'i YASAK. Gelecekte refactor sırasında hardcode geri eklenirse pre-commit hook commit'i bloklar. **Dokunulmayanlar (bilinçli):** `engine/baba.py::run_cycle` ve `engine/ogul.py::_check_end_of_day` EOD kontrolleri (Siyah Kapı #10/#13) dokunulmadı — backend zaten `self.config.get("engine.trading_close")` kullanıyor, frontend değişikliği backend davranışını etkilemez; `engine.trading_open`/`engine.trading_close` namespace'i dokunulmadı — engine buffered trading window (09:45 başlangıç, session.market_open 09:30'dan 30dk buffer) ayrı semantik; `api/routes/settings.py::_risk_baseline` ve `_notification_prefs` helper'ları dokunulmadı — session endpoint ayrı bir alan; `Dashboard.jsx` ve diğer bileşenlerde hardcoded saat referansı taraması yapıldı, başka kullanım bulunmadı. **Etki analizi:** 6 dosya (1 config + 2 backend + 3 frontend) + 1 test, ~140 satır net ekleme; Siyah Kapı/Kırmızı Bölge fonksiyonu dokunulmadı (yalnız config/default.json'a yeni namespace eklendi, mevcut değerler değişmedi); çağıran zinciri: `getSession()` → `ErrorTracker.sessionHours` state → `getEodInfo(sessionHours)` → EOD geri sayım label; `getSession()` → `Performance.heatmapHours` state → `hourData` useMemo → BarChart render; tüketici zinciri geriye dönük uyumlu (mevcut API konsümerleri yeni endpoint'i görmüyor, yeni state'ler yalnız ilgili bileşen içinde). **Doğrulama:** critical_flows **42/42 yeşil** (41 baseline + 1 yeni Flow 4i), 2.77s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.64s, 0 hata, dist/ üretildi: index.js 884.01 kB). İlk build `getEodInfo(hours = ...)` parametre adı ErrorTracker içindeki lokal `const hours = Math.floor(remaining / 60)` ile collision yaşadı → parametre `sessionCfg` olarak yeniden adlandırıldı, ikinci build temiz geçti. Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (hem backend route hem Electron bundle yenilenir). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 11.4 + Bölüm 17 A17 (H6+H10). (config/default.json, api/schemas.py, api/routes/settings.py, desktop/src/services/api.js, desktop/src/components/ErrorTracker.jsx, desktop/src/components/Performance.jsx, tests/critical_flows/test_static_contracts.py)
- #171 — Widget Denetimi Bulgu A9 (B10+H14) fix: Monitor Performans paneli eşikleri backend döngü bütçesine hizalandı (C1 — Yeşil Bölge `desktop/src/components/Monitor.jsx`, tek sabit + tek bileşen + 7 referans güncellemesi). **Kök neden:** Monitor.jsx'teki `ResponseBar` grid'i ve DÖNGÜ SÜRESİ StatCard eşikleri mikro-benchmark literal'leriydi: BABA/ÜSTAT/H-Engine `max=50`, OĞUL/VERİ GÜNCELLEME `max=100`, TOPLAM DÖNGÜ `max=300`, StatCard `cycleAvg > 50 ? red : teal`, DÖNGÜ İSTATİSTİK MAX `> 100ms ? warn`. Ancak backend `config/default.json::engine.cycle_interval = 10` saniye — döngü bütçesi **10000ms**. Canlı ortamda DataPipeline ~2596ms, toplam döngü ~2739ms ölçülüyor; bu değerler bütçenin %26-%27'sidir ama UI literal'leri karşısında %2596 / %913 oranları çıkıyor, `Math.min(pct, 100)` ile clamp'lendiği için **tüm çubuklar daima dolu** ve StatCard sürekli kırmızı gösteriyordu. Sonuç: performans paneli hiçbir zaman "normal" durumu yansıtmıyordu — kullanıcı "döngü sürekli aşıyor" algısı alıyor, gerçekte backend `overrun_count = 0` idi (çünkü backend doğru 10sn bütçesini kontrol ediyor). Frontend/backend arasında **bütçe senkronsuzluğu**. **Atomik değişiklik (1 dosya + 1 test):** (1) `Monitor.jsx` tepesine `CYCLE_INTERVAL_MS = 10000` sabiti eklendi (POLL_MS yanına, backend `engine.cycle_interval` değeri ile senkron; Flow 4h testi sabit değerini 10000'de kilitler). (2) `ResponseBar` bileşeni genişletildi: yeni `barColor = pct > 70 ? '#e74c3c' : pct > 30 ? '#f39c12' : color` mantığı — yük yüzdesi %70'i aştığında kırmızı, %30'u aştığında turuncu, altında temel renk. Etiket rengi de aynı mantığa bağlandı. (3) 6 `ResponseBar` max prop'u (BABA DÖNGÜ, OĞUL SİNYAL, ÜSTAT BEYİN, H-ENGINE, VERİ GÜNCELLEME, TOPLAM DÖNGÜ) hardcoded 50/100/300 yerine `CYCLE_INTERVAL_MS` ile değiştirildi — her bar artık "10sn bütçesinin payı" olarak okunur. (4) DÖNGÜ SÜRESİ StatCard color: `cycleAvg > CYCLE_INTERVAL_MS * 0.9 ? red : > * 0.5 ? orange : teal`; sub etiketi "ortalama döngü" → "bütçe 10000ms" olarak backend değerini gösterecek şekilde güncellendi. (5) DÖNGÜ İSTATİSTİK MAX renk eşiği: `(cycle?.max_ms ?? 0) > CYCLE_INTERVAL_MS * 0.9 ? red : > * 0.5 ? orange : '#8ab0d0'` — üç aşamalı. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_monitor_response_bars_use_cycle_interval_budget` — 6 aşamalı kontrol: (a) `CYCLE_INTERVAL_MS = 10000` sabit tanımı var; (b) eski literal max prop'lar (`max={50}`, `max={100}`, `max={300}`) YASAK — geri eklenirse commit bloklanır; (c) `max={CYCLE_INTERVAL_MS}` en az 6 kez kullanılır (6 ResponseBar); (d) `pct > 70` / `pct > 30` yük renk eşikleri mevcut; (e) eski `cycleAvg > 50 ?` StatCard hardcode YASAK, `cycleAvg > CYCLE_INTERVAL_MS` mantığı var; (f) eski `> 100 ? '#f39c12'` MAX hardcode YASAK, `cycle?.max_ms ?? 0) > CYCLE_INTERVAL_MS` mantığı var. **Dokunulmayanlar (bilinçli):** Backend `config/default.json::engine.cycle_interval` DEĞİŞMEDİ (Kırmızı Bölge #8 — risk/strateji parametresi değil ama yine de dokunulmadı, yalnız frontend senkronize edildi); `engine/main.py::_run_single_cycle` döngü zamanlaması dokunulmadı (Siyah Kapı #22); Dashboard'daki POLL_MS = 10000 ayrı bir sabittir (API poll interval, döngü bütçesi değil) — bilinçli ayrılık. **Etki analizi:** 1 dosya (desktop, Yeşil Bölge) + 1 test, ~20 satır net JSX (1 sabit + 3 satır renk mantığı + 6 max prop değişikliği + 2 eşik güncellemesi) + ~60 satır test; Siyah Kapı/Kırmızı Bölge/Sarı Bölge dokunusu YOK; çağıran zinciri yok (CYCLE_INTERVAL_MS yerel sabit), tüketici zinciri dahili (ResponseBar prop'u, StatCard color prop'u). **Doğrulama:** critical_flows **41/41 yeşil** (40 baseline + 1 yeni Flow 4h), 2.57s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.45s, 0 hata, dist/ üretildi: index.js 882.52 kB). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (Electron bundle yeniden yüklenir). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 11.4 + Bölüm 17 A9 (B10+H14). (desktop/src/components/Monitor.jsx, tests/critical_flows/test_static_contracts.py)
- #170 — Widget Denetimi Bulgu B25 fix: Monitor modül hata sayacı yapısal event.type classifier'ına dayandırıldı (C1 — Yeşil Bölge `desktop/src/components/Monitor.jsx`, tek helper + tek hesaplama bloğu). **Kök neden:** `Monitor.jsx::errorCounts` hesabı eski kodda `(ev.message || '').toLowerCase().includes('baba')` stilinde serbest metin substring kontrolü kullanıyordu. İki kritik kırılganlık: (1) Canlı üretimde DB'den gelen ERROR/CRITICAL event type'ları (`KILL_SWITCH` 25, `DRAWDOWN_LIMIT` 12, `TRADE_ERROR` 9, `SYSTEM_STOP`, `RISK_LIMIT`, `DAILY_LOSS_STOP`) mesaj metninde `baba`/`ogul`/`manuel`/`hengine`/`ustat` kelimelerini İÇERMİYOR. Örnek mesajlar: "Kill-switch L2 aktif: daily_loss", "Günlük kayıp limiti aşıldı", "Toplam drawdown: %30.43", "Emir başarısız: F_HALKB BUY 1.0 lot". Substring fallback **hiçbir üretim eventi ile eşleşmiyordu** — `errorCounts` daima {0,0,0,0,0} kalıyor, Monitor "HATA 0 bugün" gösterdiği halde DB'de 48 kritik event vardı. Tam ölü kod. (2) `String.prototype.toLowerCase()` Türkçe locale'e duyarsız; Unicode 'ğ'/'İ' karakterlerinde inconsistent eşleşir — 'Oğul'/'Üstat' içeren mesajlar yanlış modüle atanabilirdi. Ek olarak else-if zinciri ilk eşleşme kazandığı için baba en başta tanımlıydı → "baba hatası sırasında OĞUL sinyali reddedildi" tamamen baba'ya atanırdı. **Atomik değişiklik (1 dosya + 1 test):** (1) `Monitor.jsx` üstüne `classifyEventModule(ev)` helper fonksiyonu eklendi. İki aşamalı: (a) `MODULE_TYPE_PREFIX` tablosu — üretimde kullanılan event type prefix'leri yapısal olarak eşleşir (hengine: `HYBRID_`; manuel: `MANUAL_|MANUEL_`; baba: `KILL_SWITCH|DRAWDOWN_|RISK_|COOLDOWN_|REGIME_|BABA_|HARD_DRAWDOWN|DAILY_LOSS_STOP|MONTHLY_LOSS|WEEKLY_RESET|DAILY_RESET|LOT_HALVED|STOP_TRADING|SYSTEM_STOP|SYSTEM_HALT`; ogul: `TRADE_|ORDER_|SIGNAL_|OGUL_|VOLUME_SPIKE|PARTIAL_|FAKE_|SIGN_MISMATCH|EOD_|PAPER_TRADE`; ustat: `USTAT_|PATTERN_|STRATEGY_|TOP5_`). (b) Prefix eşleşmezse `MODULE_MSG_WORD` tablosu ile mesaj üzerinde `toLocaleLowerCase('tr-TR')` + word-boundary regex (`\b...\b` + `/u` flag) fallback çalışır: `\bh[-_ ]?engine\b|\bhibrit\b|\bhybrid\b`, `\bmanue?l\b`, `\bbaba\b`, `\bo[gğ]ul\b`, `\b[üu]stat\b`. (c) Sınıflandırılamayan event sayılmaz (false-positive yerine sessizlik; audit sonucunda yakalanamayan type'lar Kritik Akış testinde not edilir). (2) `errorCounts` hesap bloğu `(health?.recent_events || []).forEach(ev => { if (ev.severity === 'ERROR' || ev.severity === 'CRITICAL') { const mod = classifyEventModule(ev); if (mod) errorCounts[mod]++; } })` şeklinde sadeleştirildi — else-if zinciri kaldırıldı, öncelik sırası yapısal. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_monitor_error_counts_uses_structured_classifier` — Monitor.jsx'i text olarak okur ve 5 aşamalı kontrol yapar: (a) `function classifyEventModule` tanımı var mı; (b) eski `msg.includes('baba')` ve `toLowerCase().includes(` kalıpları YASAK (false positive, gelecek refactor'da geri eklenirse pre-commit hook'u bloklar); (c) `toLocaleLowerCase('tr-TR')` kullanımı var; (d) üretimde görülen 9 canonical type prefix (`KILL_SWITCH`, `DRAWDOWN_`, `TRADE_`, `SYSTEM_STOP`, `DAILY_LOSS_STOP`, `HYBRID_`, `MANUAL_`, `MANUEL_`, `USTAT_`) MODULE_TYPE_PREFIX haritasında mevcut; (e) word-boundary `\bbaba\b` regex fallback yerinde. İlk denemede test false-positive verdi (Monitor.jsx yorumu eski kalıbı literal olarak anmıştı), yorum yeniden yazıldı; ikinci denemede **40 passed**. **Dokunulmayanlar (bilinçli):** Backend `events` tablosuna `module` kolonu eklenmedi (`engine/database.py` Kırmızı Bölge #6, DB migration + tüm `insert_event` call-site'larının güncellenmesi savaş zamanı riski — frontend classifier yeterli koruma sağlıyor); `api/routes/health.py::_build_recent_events` şeması dokunulmadı (yeni alan opsiyonel değil); `error_tracker.py` ve `ErrorTracker.jsx` ayrı bir akış (gruplama algoritması farklı), audit kapsamı dışı; event type'larını üreten engine modülleri dokunulmadı — yalnız tüketici (Monitor.jsx) akıllandırıldı. **Etki analizi:** 1 dosya (desktop, Yeşil Bölge) + 1 test, ~55 satır JSX ekleme (helper + 2 tablo + fonksiyon) + 8 satır hesap bloğu refactor + ~70 satır test; Siyah Kapı / Kırmızı Bölge / Sarı Bölge dokunusu YOK; çağıran zinciri tek (`errorCounts` sözlüğünü modStatus, ModBox detail grid ve hata sayacı grid kullanır — hepsi aynı sözlüğü okur, yeni imza geriye dönük uyumlu); backend değişikliği GEREKSİZ. **Doğrulama:** critical_flows **40/40 yeşil** (39 baseline + 1 yeni Flow 4g), 2.94s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.60s, 0 hata, dist/ üretildi: index.js 882.41 kB). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (Electron bundle yeniden yüklenir). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 11.4.2-11.4.5 + Bölüm 16 B25. (desktop/src/components/Monitor.jsx, tests/critical_flows/test_static_contracts.py)
- #169 — Widget Denetimi B-finding fix: `engine/event_bus.py::emit` dup-key bug'i giderildi (C2 — Sarı Bölge değil, Yeşil Bölge `event_bus.py`, 1 fonksiyon, ~15 satır refactor). **Kök neden:** A3 fix sırasında tespit edilen bu bulgu event_bus'un `emit` fonksiyonunda dict literal + spread deseninden kaynaklanıyordu: outer `event` anahtarı önce set ediliyor, sonra iç `data` sözlüğü spread ile üzerine yazılıyordu. İç `data`'da `type` anahtarı varsa dict spread kuralları gereği inner değer outer'i eziyordu. Canlı etkisi: `engine/h_engine.py`'de 3 yerde (`_check_end_of_day` satır 701, `_check_direction_flip` satır 793, `_check_daily_reset` satır 2593) `_emit("notification", {"type": "hybrid_eod"|"hybrid_direction_flip"|"hybrid_daily_reset", ...})` çağrıları yapılıyordu; bu çağrılar sonrası `drain()` payload'i `{"type": "hybrid_eod", ...}` olarak çıkıyor, Dashboard'daki `if (msg.type === 'notification')` WS branch'i (satır 379) hiçbir zaman eşleşmiyor, bildirim drawer'a WS üzerinden hiç düşmüyordu — ÖLÜ KOD. Notifications sadece HTTP `getNotifications({limit:50})` polling path'inde geliyor, WS canlı gelmiyor; kullanıcı hibrit EOD uyarısını gecikmeli alıyor, direction flip ve daily reset ise drawer'a hiç yansımayabiliyordu (polling 20sn interval). **Neden A3'te çözülmedi?** A3 fix'i display-layer filter kararı aldı (`shouldShowNotification`) ve WS branch'i ölü kod olarak not etti; WS canlılığı için event_bus fix'i ayrı B-finding olarak işaretlendi. **Atomik değişiklik (1 dosya + 1 test):** `event_bus.py::emit` dict literal `{"type": event, **(data or {})}` yerine explicit merge ile değiştirildi: `payload: dict = dict(data or {})` → eğer `type` iç data'da varsa `payload.pop("type")` ile çıkarılıp `payload.setdefault("notif_type", inner_type)` ile `notif_type` altına taşındı → `payload["type"] = event` ile outer event type her zaman dış `type` olarak yazıldı. `setdefault` kullanımı: caller zaten `notif_type` set ettiyse override edilmez, explicit değer korunur. Dashboard'daki `shouldShowNotification` zaten `msg?.notif_type || msg?.type` okuyor — frontend değişikliği GEREKSİZ. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_event_bus_emit_preserves_outer_type` — 3 aşamalı: (a) **Runtime:** `emit("notification", {"type": "hybrid_eod", "title": ..., "message": ..., "severity": ...})` çağrısı sonrası `drain()` payload'inde `ev["type"] == "notification"` (outer korundu) + `ev["notif_type"] == "hybrid_eod"` (inner taşındı) + diğer alanlar (message) korundu; (b) **Override koruma:** caller zaten `notif_type` set ettiyse `setdefault` override etmez — `emit("notification", {"type": "X", "notif_type": "explicit_value"})` sonrası `notif_type == "explicit_value"` kalır, `X` sessizce düşer; (c) **Statik kaynak kontrolu:** `inspect.getsource(event_bus.emit)` içinde eski dup-key dict literal deseni YASAK + `notif_type` anahtarı mevcut olmalı. Test docstring'deki yanıltıcı pattern ilk denemede false positive verdi, `event_bus.py` docstring'inden dup-key pattern literal'i çıkarıldı, genel açıklamaya çevrildi. Gelecekte emit refactor'u dup-key'i geri eklerse pre-commit hook commit'i bloklar. **Dokunulmayanlar (bilinçli):** `engine/h_engine.py` emit call-site'ları (3 yer) dokunulmadı — fix event_bus merkezindedir, call-site'lar aynı `{"type": "hybrid_eod", ...}` formatını kullanmaya devam edebilir, event_bus artık bunu güvenle ele alıyor; Dashboard WS branch'i (satır 379-395) dokunulmadı — `msg.type === 'notification'` check'i artık gerçekten tetiklenecek, `shouldShowNotification` filtresi zaten yerinde (A3'ten); mevcut `getNotifications` HTTP path'i dokunulmadı — fallback olarak kalmaya devam ediyor. **Etki analizi:** 1 dosya (engine, Yeşil Bölge) + 1 test, ~15 satır net refactor, ~55 satır test; Siyah Kapı / Kırmızı Bölge / Sarı Bölge dokunusu YOK; tüketici zinciri — `api/routes/live.py::_event_drain_loop` → `broadcast(ev)` → WS → Dashboard (A3 filter) → notification drawer; geriye dönük uyumluluk — eski call-site'lar aynı davranır, yeni `notif_type` alanı Dashboard zaten okuyor, hiçbir tüketici kırılmadı. **Doğrulama:** critical_flows **39/39 yeşil** (38 baseline + 1 yeni Flow 4f), 2.56s; build gereksiz (backend-only Python değişikliği, React/JSX dokunulmadı). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (API modül reload). Kaynak: A3 session raporundaki "Dokunulmayanlar" bölümünde tespit edilen B-finding. (engine/event_bus.py, tests/critical_flows/test_static_contracts.py)
- #168 — Widget Denetimi Bulgu B9 fix: Monitor motor durum rozetleri gerçek sinyallere bağlandı (C1 — Yeşil Bölge `desktop/src/components/Monitor.jsx`, tek fonksiyon, ~35 satır refactor). **Kök neden:** `Monitor.jsx::modStatus` hesabında `manuel: 'ok'` **sabit hardcode** duruyordu — manuel motorda hata olsa bile rozet asla sarı/kırmızı olmuyordu. Ayrıca BABA/OĞUL/H-Engine/ÜSTAT rozetleri `engine_running=false` olduğunda bile "ok" görünebiliyordu (killLevel defaults 0, errorCounts kullanılmıyordu); `errorCounts` sözlüğü her cycle'da hesaplanıp modStatus tarafından referans edilmiyor, yalnız detail açılır paneline yazılıyordu (ölü değişken); OĞUL/H-Engine rozetleri `killLevel >= 2` (L2+ = motorlar durur, Anayasa 4.5 #10) kontrolünü yapmıyordu, yalnız `daily_loss_stop` bayrağına bakıyordu. Canlı L2 aktifken bile Monitor'da "OĞUL AKTİF" yeşil görünebiliyordu — kullanıcı "trading duruyor mu?" sorusunu Monitor'a güvenip yanıtladığında **yanıltıcı gösterim** alıyordu. Audit raporu 11.4.2-11.4.5: "🐛 `modStatus.manuel = 'ok'` SABİT HARDCODE — manuel motor hatası hiç warn/err göstermez. 🧱" + "BABA/ÜSTAT rozetleri sadece `engine_running`'e bağlı değil, ayrıca errorCounts ve killLevel ile zenginleştirilmeli". **Atomik değişiklik (1 dosya + 1 test):** `Monitor.jsx::modStatus` obje literal'i **conditional expression** ile değiştirildi. Yeni semantik: (a) `!engineRunning` → tüm modüller `'err'` (motor ölü, hiçbir rozet yeşil gösterilemez). (b) BABA: `killLevel >= 3 → err`, `killLevel > 0 → warn`, `errorCounts.baba > 0 → warn`, aksi `ok`. (c) OĞUL: `killLevel >= 2 → err` (YENİ — L2+ motorlar durdu), `daily_loss_stop → err`, `errorCounts.ogul > 0 || reject_count > 0 || timeout_count > 0 → warn`, aksi `ok`. (d) H-Engine: `killLevel >= 2 → err` (YENİ), `errorCounts.hengine > 0 → warn`, `active_hybrid_count > 0 → warn`, aksi `ok`. (e) ÜSTAT: `cache_stale → warn`, `errorCounts.ustat > 0 → warn`, aksi `ok` (kill-switch'te durmaz, risk motoru değil). (f) MANUEL: `errorCounts.manuel > 0 → warn`, aksi `ok` — **hardcode 'ok' KALDIRILDI**. `errorCounts` sözlüğü artık ölü değişken değil, modStatus tarafından ana referans. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_monitor_modstatus_not_hardcoded` — Monitor.jsx'i text olarak okur, modStatus bloğunu regex ile izole eder ve (1) `manuel: 'ok'` / `manuel: "ok"` literal'lerinin KALDIRILDIĞINI, (2) `engineRunning` referansının modStatus blogunda bulunduğunu, (3) `errorCounts` referansının blokta olduğunu, (4) `killLevel >= 2` L2+ kapısının OĞUL/H-Engine için yazıldığını assert eder. Gelecekte refactor sırasında hardcode geri eklenirse pre-commit hook commit'i bloklar. **Dokunulmayanlar (bilinçli):** `errorCounts` substring parse kırılganlığı (audit 11.4.2 B-finding — `msg.includes('baba')` yanlış pozitif riski) ayrı bir maddeye bırakıldı; backend `/api/manuel-motor/health` endpoint'i henüz yok — manuel rozet şu an yalnızca event log parse ile "en azından hata görünmüyor" garantisini veriyor (eski tam hardcode'tan net iyileşme, tam çözüm değil); BABA chip `killLevel >= 2` için `warn` kaldı (audit 11.4.2 "L2=warn Doğru" kararı korundu — BABA risk motoru, L2'de haberdar ve çalışıyor; OĞUL/H-Engine durduğu için onlar `err`). **Etki analizi:** 1 dosya (desktop, Yeşil Bölge), 1 test, ~35 satır JSX + ~45 satır test; Siyah Kapı/Kırmızı Bölge/Sarı Bölge dokunusu YOK; çağıran zinciri tek (`ModBox status` prop'una geçen değer), tüketici zinciri tek (Badge renk eşleme `ok/warn/err`); backend değişikliği GEREKSİZ (mevcut `/api/health.recent_events` + `/api/status.engine_running` + `/api/risk.kill_switch_level` alanları zaten mevcut). **Doğrulama:** critical_flows **38/38 yeşil** (37 baseline + 1 yeni Flow 4e), Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.63s, 0 hata, dist/ üretildi). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için build + Electron restart gerekli (frontend bundle yenilenir). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 11.4 + Bölüm 17 A4 (B9). (desktop/src/components/Monitor.jsx, tests/critical_flows/test_static_contracts.py)
- #167 — Widget Denetimi Bulgu S1 fix: Bildirim tercihleri gerçek davranışa bağlandı + backend kalıcılık (C3 — `config/default.json` Kırmızı Bölge sadece yeni `ui.notification_prefs` bölümü eklendi, risk/strateji/eşik sabiti DOKUNULMADI). **Kök neden:** Settings sayfasındaki 5 toggle (`soundEnabled`, `killSwitchAlert`, `tradeAlert`, `drawdownAlert`, `regimeAlert`) `localStorage['ustat_notification_prefs']` + `POST /api/settings/notification-prefs` yapıyordu, ancak backend `api/routes/settings.py::_notification_prefs` **process belleğinde dict** idi (restart → bellek sıfırlanır, yalnız `DEFAULT_PREFS` kalır); dahası projenin hiçbir yerinde bu dict'i okuyan kod yoktu (grep: 0 eşleşme). Frontend'de de hiçbir `new Audio`/`playSound`/`beep` altyapısı yoktu — audit "kullanıcı sessize aldım sanıyor ama sistem ses çıkarıyor" dedi, gerçek daha net: hiç ses zaten yok, 5 toggle tamamen UI tiyatrosu. **Atomik değişiklikler (4 dosya + 1 test, 0 Siyah Kapı dokunusu):** (1) `config/default.json` — yeni `"ui": { "notification_prefs": {...} }` bölümü eklendi (5 anahtar, default değerler). Kırmızı Bölge #8 dokunusu ama risk/strateji/eşik/baseline parametreleri **DEĞİŞMEDİ**, yalnız UI namespace altında yeni bir alan. (2) `api/routes/settings.py` — eski `_notification_prefs: dict` modül sabiti **KALDIRILDI**, yerine: `DEFAULT_NOTIFICATION_PREFS` fallback sabiti + `_read_notification_prefs_from_config()` helper (engine.config'den oku, eksik key'leri default ile merge et, tip kontrolü boolean) + `GET /settings/notification-prefs` helper'dan okur + `POST /settings/notification-prefs` `config.set("ui.notification_prefs", ...)` + `config.save()` zinciri ile disk'e yazar (`update_risk_baseline` ile aynı persistence deseni). Engine yoksa warning log + `success=False`. (3) `desktop/src/components/Dashboard.jsx` — `notifPrefsRef` (`useRef`) + `readNotifPrefs()` helper + `shouldShowNotification(msg)` filtresi + `storage` event listener (Settings'te toggle değişince otomatik güncellenir) + HTTP fetch path'inde (`getNotifications({limit:50}).then(...)`) ve WS `msg.type === 'notification'` branch'inde filtre uygulanır. Filtre şu an yalnız `hybrid_*` tiplerini `tradeAlert` bayrağına bağlıyor; diğer tipler her zaman gösterilir (backend'de emit edilmiyor zaten). (4) `desktop/src/components/Settings.jsx` — toggle bölümünün üstüne dürüst kapsam bilgi kutusu (`"Kapsam: Şu an yalnızca İşlem & Hibrit Uyarıları toggle'ı gerçek davranışa bağlıdır..."`) + `tradeAlert` label'ı "İşlem bildirimi" → "İşlem & Hibrit Uyarıları" olarak netleştirildi + diğer 4 toggle açıklamasına "(henüz aktif değil)" notu eklendi. Kullanıcı artık hangi toggle'ın gerçekten davranış ürettiğini biliyor. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_notification_prefs_persists_via_config` — (a) eski `_notification_prefs: dict` tanımının KALDIRILDIĞINI, (b) `DEFAULT_NOTIFICATION_PREFS` sabitinin 5 anahtarla var olduğunu, (c) `_read_notification_prefs_from_config` helper'ının `ui.notification_prefs` key'ini kullandığını, (d) `update_notification_prefs` POST'unun `config.set("ui.notification_prefs", ...)` + `config.save()` çağırdığını `inspect.getsource` ile statik olarak doğrular. Gelecekte bellek tabanlı dict geri eklenirse pre-commit hook commit'i bloklar. **Dokunulmayanlar (bilinçli):** `engine/h_engine.py` notification emit noktaları (Sarı Bölge ama gereksiz risk), `engine/event_bus.py` (ayrı bir bulgu: `emit("notification", {"type": "hybrid_eod", ...})` payload'ında dup-key nedeniyle dış `type` iç `type`'a ezilir → Dashboard'daki `msg.type === 'notification'` WS branch'i şu anda ölü kod — ancak HTTP fetch path canlı, filtre orada aktif), yeni notification türleri (kill_switch/drawdown/regime emit) — ayrı A-maddesi. **Etki analizi:** 4 dosya + 1 test, ~100 satır net ekleme; Siyah Kapı fonksiyonu dokunulmadı; çağıran zinciri: Settings → Dashboard (localStorage+storage event), Dashboard → notifications drawer (filtre sonrası); tüketici zinciri: yeni field/key'leri kimse eski yolla okumuyor, geriye dönük uyumluluk korundu. **Doğrulama:** critical_flows **37/37 yeşil** (36 baseline + 1 yeni), py_compile temiz (`api/routes/settings.py`, `api/schemas.py`, `api/routes/trades.py`), Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.67s, 0 hata, dist/ üretildi) — Vite JSX compile Dashboard.jsx + Settings.jsx syntax temiz. Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için API restart gerekli (backend modül reload). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 14.5 + Bölüm 17 A3 (S1). (config/default.json, api/routes/settings.py, desktop/src/components/Dashboard.jsx, desktop/src/components/Settings.jsx, tests/critical_flows/test_static_contracts.py)
- #166 — Widget Denetimi Bulgu B2 fix: Trade veri güvenilirliği — `best_trade`/`worst_trade` seçiminde MT5 netting anomalileri hariç tutuluyor (C1 Yeşil Bölge — `api/routes/trades.py`, `api/schemas.py`). **Kök neden:** Canlı `/api/trades/stats` çıktısında `best_trade = F_TOASO BUY 8 lot entry=259.87 exit=257.80 pnl=+2705` tespit edildi — BUY için exit<entry zarar olmalıyken pnl +2705. DB sorgusunda benzer örüntü: F_TKFEN SELL 104.26→104.40 pnl=+485, F_KONTR SELL 8.90→8.91 pnl=-7925 (magnitude 53x). Örüntü `engine/mt5_bridge.py::get_history_for_sync` (satır 2030-2135) kaynaklı: MT5 netting mod'unda aynı `position_id` altında scale-in/out veya ters dönüşler tek pozisyon olarak birleştirilir; fonksiyon `entry_price` ve `exit_price` için weighted average hesaplar, `pnl` için MT5 raw profit toplamını alır. Parçalı pozisyonlarda `(exit-entry)×lot×multiplier ≠ MT5 pnl` — matematik çelişir. `best_trade = max(items, key=lambda t: t.pnl)` ile seçildiği için anomali kayıt Performans sayfasında "En Kârlı İşlem" olarak kullanıcıya gösteriliyordu → veri güvenilirliği algısı bozuluyordu. **Atomik değişiklik (C1, 3 dosya, 0 Kırmızı Bölge dokunusu):** (1) `api/schemas.py::TradeItem` — `data_warning: str | None = None` alanı eklendi (`"SIGN_MISMATCH"` = parçalı/ters netting pozisyon şüphesi). (2) `api/schemas.py::TradeStatsResponse` — `anomaly_count: int = 0` alanı eklendi (UI "N işlem anomali nedeniyle hariç" banner'ı için). (3) `api/routes/trades.py` — yardımcı fonksiyon `_check_trade_consistency(direction, entry, exit, pnl)` eklendi: pnl ve `direction × (exit-entry)` işaretleri çelişirse `"SIGN_MISMATCH"`, aksi halde `None`. (4) `_to_trade_item` bu kontrolü her trade için çağırıp `data_warning` alanını dolduruyor. (5) `get_trade_stats` `clean_items = [t for t in items if t.data_warning is None]` filtresi ile `best_trade` ve `worst_trade`'i temiz kayıtlardan seçiyor; `anomaly_count` hariç tutulan trade sayısını UI'a iletiyor. **Kritik koruma:** `total_pnl`, `win_rate`, `avg_pnl`, `winning_trades`, `losing_trades` hesapları filtreden ETKİLENMEZ — pnl değerleri MT5 ground truth olarak kalır; yalnız best/worst seçimi temizlenir. **Mimari karar:** `engine/mt5_bridge.py::get_history_for_sync` DOKUNULMADI (Kırmızı Bölge #3, sync mantığını değiştirmek DB migration + re-sync + Savaş Zamanı riski demektir). Bunun yerine anomaliler tespit edilip filtreleniyor; ham veri DB'de korunuyor, UI'a doğru özet sunuluyor. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_trade_stats_excludes_sign_mismatch_from_best_worst` — helper fonksiyonun varlığını, `TradeItem.data_warning` + `TradeStatsResponse.anomaly_count` field'larını, `get_trade_stats` içinde `data_warning` filtresini ve `_check_trade_consistency`'nin 5 canonical durumda doğru sonuç ürettiğini (BUY/SELL × tutarlı/tutarsız × pnl=None) `inspect` + `Pydantic model_fields` ile doğrular. **Etki analizi:** 3 dosya, ~60 satır net ekleme; Siyah Kapı/Kırmızı Bölge dokunusu yok; frontend değişikliği GEREKSİZ (yeni field'lar opsiyonel, `best_trade` otomatik olarak temiz gelmeye başlar). **Doğrulama:** critical_flows **36/36 yeşil** (35 baseline + 1 yeni), py_compile temiz. Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 16.2 B2 + Bölüm 17 A2. (api/schemas.py, api/routes/trades.py, tests/critical_flows/test_static_contracts.py)

### Security
- #165 — Widget Denetimi Bulgu B1 fix: Anayasa Kural #6 L2→L3 eskalasyon (C4 Siyah Kapı #1 `check_risk_limits` — ADDITIVE bug fix, davranış genişlemesi): Widget denetimi sırasında canlı `/api/risk` çıktısında `total_drawdown_pct=0.304293` (%30.43) ama `kill_switch_level=2` tespit edildi; limit `hard_drawdown=0.15`. Anayasa Kural #6 "hard drawdown ≥%15 → L3 otomatik" ölü görünüyordu. **Kök neden:** `engine/baba.py::check_risk_limits` (Siyah Kapı #1) L2 aktifken 2. blokta koşulsuz `return verdict` yapıyordu (satır 1509-1525) → `_check_hard_drawdown` (Siyah Kapı #6) çağrısı yalnızca L0/L1 seviyesinde çalışıyordu → L2 önceden tetiklendikten sonra drawdown büyümeye devam etse bile otomatik L3 eskalasyonu yapılmıyordu. Canlı olay sırası: saat 16:10:49 `daily_loss` ile L2 tetiklendi; sonrasında floating zarar %15 eşiğini aşıp %30'a ulaştı; her 10 sn'de `check_risk_limits` çağrıldı ama `_check_hard_drawdown` tek seferlik de çalışmadı (grep: `_check_hard_drawdown` tüm kod tabanında 1 çağrı noktası). **Mantık çelişkisi:** Kural #3 monotonluk SADECE düşürmeyi yasaklar (L2→L1 YASAK); yukarı yönde eskalasyon (L2→L3) monotonluğa UYGUN ve zorunludur. **Atomik değişiklik:** `engine/baba.py` L2 return bloğunun başına hard drawdown kontrolü eklendi — L2 aktifken `_check_hard_drawdown(risk_params, snap=snap)` çağrılır, `"hard"` dönerse `_activate_kill_switch(L3, "hard_drawdown", ...)` ile L3'e yükseltilir ve verdict L3 bilgileriyle döner; `"hard"` değilse orijinal L2 davranışı (can_trade=False, risk_multiplier ks_reason'a göre) aynen çalışır. L0/L1 → L3 yolu (satır 1554) değişmedi, ikinci tetik noktası olarak korundu. **Etki analizi:** 1 dosya, 1 fonksiyon, ~20 satır net ekleme; `_check_hard_drawdown` imzası değişmedi; tüketici zinciri (`ogul.py::_execute_signal` BABA `can_trade` kapısı) değişmedi; `_activate_kill_switch` → `_close_all_positions` zinciri zaten L3'te çağrılıyordu — davranış değişimi SIFIR yeni state, mevcut state'e erişim yolu açıldı. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_check_risk_limits_escalates_l2_to_l3_on_hard_drawdown` — L2 blok içinde `_check_hard_drawdown` çağrısı, `KILL_SWITCH_L3` eskalasyon yolu ve `hard_drawdown` reason varlığını inspect ile doğrular. Gelecekte refactor sırasında eskalasyon yolu silinirse pre-commit hook commit'i bloklar. **Doğrulama:** critical_flows 35/35 yeşil (34 baseline + 1 yeni). Piyasa açık ama deploy kullanıcı kontrolünde — fix commit'lendi, `restart_app` YAPILMADI; kullanıcı deploy zamanlamasına karar verecek. Deploy sonrası sistem canlı L2'den L3'e yükselecek ve 1 açık pozisyonu kapatacak (amaç bu). Anayasa Kural #3 uyum: monotonluk yukarı yönde geçerli. Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 16.2 B1. (engine/baba.py, tests/critical_flows/test_static_contracts.py)

### Changed
- #164 — v6.0.0 versiyon bump + "ÜSTAT Plus V6.0" ürün adı yeniden markalama (build: semver+rebrand, davranış değişmez): v5.9.0 → v6.0.0 majör bump ve uygulama adının "ÜSTAT" → "ÜSTAT Plus V6.0" olarak yeniden markalanması. **Bump gerekçesi (CLAUDE.md Bölüm 7 Adım 3):** Son versiyon commit'i `e57b008` (v5.9 ilk, 2026-03-28) ile HEAD arası `git diff --shortstat`: 202 dosya, +89737 / -5096 satır = 94833 toplam. Toplam izlenen kod satırı 168064 → **ham oran %56.4**. Docs hariç üretken kod (engine+api+desktop+tests) oranı **%33.7** — her iki metrikte de %10 eşiğinin çok üzerinde, versiyon artışı zorunlu. Ek olarak `baba.py` modül docstring'lerinde 13 kez `"v5.9.3 — BULGU"` etiketi (informal internal convention) mevcut, versiyon dosyaları 5.9.0'da kalmış — hizalanma gerekliydi. CLAUDE.md kuralı "minor 9'dan sonra major artar" → 5.9 → **6.0**. **22 atomik patch (tek commit):** Fonksiyonel sabitler (9): `engine/__init__.py` VERSION, `config/default.json` version, `api/server.py` docstring+API_VERSION+title+root_name, `api/schemas.py` version field, `desktop/package.json` version+description; UI/Electron (13): `desktop/main.js` docstring+APP_TITLE+splash HTML, `desktop/preload.js` docstring, `desktop/mt5Manager.js` docstring (v5.7→V6.0 — geçmiş bump'ta atlanmıştı, fırsatla düzeltildi), `desktop/src/components/TopBar.jsx` docstring+layout comment+h1 logo, `desktop/src/components/LockScreen.jsx` docstring+h1 logo, `desktop/src/components/Settings.jsx` docstring+VERSION const+"Uygulama" FieldRow; Kısayol script'leri (5): `create_shortcut.ps1` header+filename+Description, `update_shortcut.ps1` $newName+Description. **Ürün adı yapısı:** "ÜSTAT" → "ÜSTAT Plus" (UI başlıkları), sürüm etiketi `v5.9` → `V6.0` (büyük V). Splash screen, TopBar logo, LockScreen logo, Electron APP_TITLE, Settings Uygulama satırı hepsi yeni isim. Kısayol dosya adı `USTAT v5.9.lnk` → `USTAT Plus V6.0.lnk` (kullanıcı `create_shortcut.ps1` script'ini çalıştırarak güncelleyecek). **Koruma kapsamı:** Tarihsel/arkeolojik yorumlardaki `v5.9.2`, `v5.9.3 — BULGU`, `// v5.9: ...` stili referanslara DOKUNULMADI — bunlar "bu değişiklik ne zaman yapıldı" etiketi, ürün adı değil. Test altyapısı (`tests/critical_flows/*`) değişmedi. Davranış değişimi sıfır (yalnız string/sabit değerleri). **Doğrulama:** py_compile `engine/__init__.py` + `api/server.py` + `api/schemas.py` temiz, critical_flows **34/34 yeşil**, production `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, dist/ üretildi: index.html 1.06 kB, index.css 90.52 kB, index.js 880.33 kB). Etki analizi: 13 dosya, 22 patch, 0 davranış değişimi, 0 çağıran zinciri kırığı. Piyasa kapalı doğrulandı (Cumartesi, Barış Zamanı) (engine/__init__.py, config/default.json, api/server.py, api/schemas.py, desktop/package.json, desktop/main.js, desktop/preload.js, desktop/mt5Manager.js, desktop/src/components/TopBar.jsx, desktop/src/components/LockScreen.jsx, desktop/src/components/Settings.jsx, create_shortcut.ps1, update_shortcut.ps1)

---

## [5.9.0] — 2026-03-28 / 2026-04-10

### Security
- #163 — BULGU #12 dead module constants removal (C3 Kırmızı Bölge, pure cleanup, davranış değişmez): `engine/baba.py` modül seviyesi 11 ölü sabit tespit edildi ve silindi. **Sabitler:** `MAX_WEEKLY_LOSS_PCT` (0.04), `MAX_MONTHLY_LOSS_PCT` (0.07), `HARD_DRAWDOWN_PCT` (0.15), `CONSECUTIVE_LOSS_LIMIT` (3), `COOLDOWN_HOURS` (4), `MAX_FLOATING_LOSS_PCT` (0.015), `MAX_DAILY_TRADES` (5), `MAX_RISK_PER_TRADE_HARD` (0.02), `MAX_SAME_DIRECTION` (3), `MAX_SAME_SECTOR_DIRECTION` (2), `MAX_INDEX_WEIGHT_SCORE` (0.25). **Kanıt (grep tarama):** her biri dosyada tam 1 kez (declaration satırı) referansa sahipti, kod mantığında hiçbir yerde kullanılmıyordu — gerçek değerler `config/default.json` → `engine/models/risk.py RiskParams` dataclass → `risk_params.xxx` yolundan geliyor: `risk_params.max_daily_trades` (`baba.py:1612, 1614`), `risk_params.consecutive_loss_limit` (1237/1240/1667/1671/1674), `risk_params.cooldown_hours` (1960/1963/1969), `risk_params.max_risk_per_trade_hard` (1212), `risk_params.max_same_direction` (2078/2081), `risk_params.max_same_sector_direction` (2093/2097), `risk_params.max_index_weight_score` (2105/2109), `self._config.get("risk.hard_drawdown_pct", 0.15)` (2818/3266). Dış import kontrolü: `grep -rn "from engine.baba import"` — bu 11 sabit hiçbir dosyada import edilmiyor (yalnız `Baba`, `RISK_BASELINE_DATE`, `VIOP_EXPIRY_DATES`, `_baseline_to_iso`, `validate_expiry_dates` import ediliyor). **Neden kaldırıldı:** (1) Dead code — declaration + 0 kullanım, 2018 öncesi refactor kalıntısı; (2) Config desync riski — örn. `HARD_DRAWDOWN_PCT = 0.15` modülde sabit ama kod `self._config.get("risk.hard_drawdown_pct")` kullanıyor → config değeri değişirse modül sabiti yalancı olur, yeni geliştirici yanlış değeri referans alabilir; (3) Yanıltıcı yorumlar (`# config: risk.xxx`) "sabit config'den gelir" izlenimi veriyor ama sabit zaten kullanılmıyor; (4) CLAUDE.md Bölüm 3.2 ruhuna aykırı — `HARD_DRAWDOWN_PCT = 0.15` teknik olarak sihirli sayı yasağı ihlali (config'de karşılığı var ama kod başka yerden okuyor, sabit ölü). **Tek atomik değişiklik:** (a) `baba.py:151-164` — iki sabit bloğu (`# Çok katmanlı kayıp limit sabitleri` + `# Korelasyon sabitleri`) tamamen kaldırıldı, yerine açıklayıcı bir "NOT (v5.9.3 — BULGU #12)" yorum bloğu eklendi (11 sabitin listesi, kaldırma gerekçesi, tek doğru kaynak referansı: config/default.json + engine/models/risk.py); (b) modül docstring `MAX_RISK_PER_TRADE_HARD` → `max_risk_per_trade_hard` olarak güncellendi; (c) modül docstring "Korelasyon (modül-seviye sabitler)" başlığı "Korelasyon (risk_params.* — config/default.json)" olarak güncellendi, her satıra gerçek `risk_params` field adı eklendi (`max_same_direction`, `max_same_sector_direction`, `max_index_weight_score`). **Anayasa C3 değerlendirmesi:** Kırmızı Bölge dosyası (`baba.py`), Siyah Kapı fonksiyonlarına DOKUNULMADI (silinen sabitler modül seviyesi, hiçbir fonksiyon gövdesine girmedi), davranış değişimi SIFIR (kod zaten bu sabitleri okumuyordu), çağıran zinciri SIFIR (dış import yok). Etki analizi: 1 dosya (yalnız `baba.py` üstü), -15 deletion / +11 insertion net. py_compile temiz, kritik akış testleri **34/34 yeşil**. Piyasa kapalı doğrulandı (Cumartesi gece, Barış Zamanı) (engine/baba.py)
- #162 — BULGU #11 regime hysteresis docstring + sihirli sayı config-fy (Karma C0 doc + C3 ADDITIVE config-fy, mantık değişmez): `engine/baba.py` `detect_regime()` (Siyah Kapı #8) içindeki regime hysteresis state machine'i için 4 sorun tespit edildi: (1) **Sihirli sayı (Anayasa ihlali):** `HYSTERESIS_CYCLES = 2` lokal sabit, config'de yok, CLAUDE.md Bölüm 3.2 "Sihirli sayılar YASAK" ihlali; (2) **Yanıltıcı yorum (`baba.py:714`):** `# OLAY hariç, rejim değişimi 2 ardışık cycle aynı sonucu verdiğinde onaylanır` — aslında lokal logic OLAY'ı dışarıda bırakmıyor; OLAY bypass'ı çok daha üstte (`:632-643`) early-return ile yapılıyor → yeni geliştiriciyi yanıltır; (3) **Docstring eksik:** `detect_regime` docstring'i sadece "Piyasa rejimini algıla" diyor, hysteresis state machine (3 state değişkeni, geçiş tablosu, ilk-cycle davranışı) tamamen dokümantasız; (4) **Undocumented invariant:** `_confirmed_regime` ASLA `RegimeType.OLAY` olmaz (OLAY tetiklenince fonksiyon `:643`'te early return yapar, state machine güncellenmez) — bu kasıtlı ama hiçbir yerde belirtilmemiş, bug gibi görünebilir. **Üç atomik değişiklik (tek commit):** (a) `config/default.json` → `engine.regime_hysteresis_cycles: 2` yeni anahtar (default 2 = mevcut davranış); (b) `engine/baba.py:405` `__init__` → `self._regime_hysteresis_cycles = int(self._config.get("engine.regime_hysteresis_cycles", 2))` (margin_reserve_pct pattern'i takip); (c) `engine/baba.py:626` `detect_regime` docstring tamamen yeniden yazıldı — Anayasa Siyah Kapı #8 referansı, 5 adımlı işlem zinciri, hysteresis state machine 3 değişken + geçiş tablosu, ilk-cycle davranışı, kasıtlı invariant açıkça belgelendi (`_confirmed_regime` OLAY olamaz nedeniyle), config kaynağı ve BULGU #11 referansı; (d) `baba.py:713-742` hysteresis bloğu — `HYSTERESIS_CYCLES = 2` local sabit kaldırıldı, `hysteresis_cycles = self._regime_hysteresis_cycles` ile değiştirildi; yanıltıcı yorum düzeltildi → "OLAY zaten yukarıda (~643) early return ile hysteresis'i atlar; bu blok yalnız teknik rejimler için". **Anayasa C4 değerlendirmesi:** `detect_regime()` Siyah Kapı #8 — izin verilen "kanıtlı bug fix" (sihirli sayı yasağı kanıtlı ihlal) + pure docstring/yorum + variable rename. Mantık ve çıktı default değer (2) ile birebir aynı, davranış değişimi sıfır. Hiçbir başka Siyah Kapı fonksiyonuna dokunulmadı. Etki analizi: 2 dosya, imza yok, tüketici yok, davranış değişimi sıfır. py_compile temiz, kritik akış testleri **34/34 yeşil**. Piyasa kapalı doğrulandı (Cumartesi gece, Barış Zamanı) (config/default.json, engine/baba.py)
- #161 — BULGU #10 datetime tz mini-audit + implicit local-time docstring (C0 dokümantasyon, ZERO logic): Audit BULGU #10 "datetime tz" iddiası için `engine/baba.py` (13 `datetime.now()` noktası), `engine/ogul.py`, `engine/mt5_bridge.py`, `engine/h_engine.py`, `engine/main.py`, `engine/ustat.py`, `engine/data_pipeline.py`, `engine/database.py` kapsamlı tarandı. **Bulgu:** Kod tabanı naive local time kullanımında **içsel olarak tutarlı**, kanıtlı bir naive/aware karşılaştırma bug'ı **YOK**. (a) `baba.py` içindeki tüm `datetime.now()` çağrıları naive (tz parametresi yok); (b) `database.py:478-481` `_now()` static method naive ISO string döner (`datetime.now().isoformat(timespec="seconds")`); (c) `_start_cooldown` (baba.py:1876-1893) naive datetime'ı `_risk_state["cooldown_until"]`'a yazar, `_is_in_cooldown` (1896-1913) naive `datetime.now()` ile karşılaştırır → tutarlı; (d) `_serialize_risk_state` (471-481) datetime serialization'ı düzgün; (e) cooldown read-back (baba.py:3184) naive DB string üzerinde `datetime.fromisoformat()` çalıştırır → tutarlı; (f) `mt5_bridge.py:1960` istisna olarak `tz=timezone.utc` kullanır ama yalnız MT5 epoch'u pozisyon `time` alanı için string gösterimine çevirmek üzere (karar mantığında kullanılmaz). **Aksiyon:** Anayasa "kanıt yoksa dokunma" gereği MANTIKLA dokunulmadı. Pure C0 dokümantasyon olarak `engine/baba.py` modül docstring'ine yeni "NOT (v5.9.3 — BULGU #10)" bloğu eklendi: implicit local-time varsayımı açıklandı (VİOP TR/Avrupa-İstanbul, sistem TZ ayarı işletim sorumluluğu), tutarlılık kanıtı listelendi (`database.py _now()`, `_start_cooldown`, `_is_in_cooldown`, baseline ve cooldown read-back hepsi naive), `mt5_bridge` istisna açıklandı, "Bilinen sınırlama" notu eklendi (Türkiye 2016'dan beri sabit UTC+3, DST yok → cooldown DST atlama senaryosu geçerli değil), mini-audit sonucu kayıt altına alındı. **Mantık değişmedi** — saf docstring, AST temiz, hiçbir runtime semantic etki yok, Siyah Kapı fonksiyonlarına dokunulmadı, imza/tüketici/davranış sıfır. Etki analizi: 1 dosya (yalnız modül docstring), 0 imza/davranış değişimi. Kritik akış testleri **34/34 yeşil**. Piyasa kapalı doğrulandı (Cumartesi gece, Barış Zamanı) (engine/baba.py)
- #160 — BULGU #9 `_calculate_index_weight_score` lot-aware fix (C3 Kırmızı Bölge, ADDITIVE bug fix): Endeks ağırlık skoru hesabında semantik asimetri tespit edildi — mevcut açık pozisyonlar `pos.get("volume")` (gerçek lot) ile çarpılırken, **yeni açılacak pozisyon her zaman `new_lot=1.0` (default)** ile değerlendiriliyordu. **Çağıran zincir kanıtı:** `engine/ogul.py:1686` `lot = self.baba.calculate_position_size(...)` ile gerçek lot hesaplanıyor (örn. risk-bazlı 0.05), ardından `engine/ogul.py:1719` `corr_verdict = self.baba.check_correlation_limits(symbol, direction, self.risk_params)` çağrısı **lot'u iletmiyor**, `engine/baba.py:2011` `_calculate_index_weight_score(positions, symbol, direction)` çağrısı 4. argümanı geçmiyor → `_calculate_index_weight_score` imzasındaki `new_lot: float = 1.0` default kullanılıyor → 20× over-estimation (0.05 lot sinyal 1.0 lot gibi değerlendirildi). **Net etki:** OĞUL küçük lot'lu sinyalleri yapay olarak şişirilmiş endeks ağırlık skoruyla değerlendirilip "Endeks ağırlık skoru aşıldı" sebebiyle iptal edilebiliyordu — meşru sinyal kaybı + risk yönetimi yanlış çalışıyordu. **Üç atomik değişiklik (tek commit):** (a) `engine/baba.py:1955` `check_correlation_limits` imzasına opsiyonel `new_lot: float = 1.0` parametresi eklendi (geriye uyumlu, default davranış değişmez); (b) `engine/baba.py:2011` çağrısı `_calculate_index_weight_score(positions, symbol, direction, new_lot=new_lot)` olarak güncellendi (parametre ileri iletim); (c) `engine/ogul.py:1719` çağrısı `check_correlation_limits(symbol, direction, self.risk_params, new_lot=lot)` olarak güncellendi — `lot` zaten `:1686`'da hesaplanmış. **Manuel motor (`engine/manuel_motor.py:207`) DEĞİŞMEDİ** — manuel motorda korelasyon kontrolü step 6, lot hesabı step 9'dadır → manuel motor lot'u henüz bilmiyor → default `new_lot=1.0` konservatif (en kötü senaryo) varsayımı kasıtlı olarak korundu. Docstring genişletildi: yeni parametre açıklaması, BULGU #9 history notu, manuel/OĞUL kullanım farkı not edildi. Imza geriye uyumlu (default), çağıran kontratları kırılmadı (`tests/test_ogul_200.py:149` `MagicMock` çağrıları imzadan bağımsız). Etki analizi: 2 üretim çağıran (ogul ✓ güncellendi, manuel_motor ✓ default'u koruyor), 0 imza kırığı, AST temiz. Kritik akış testleri **34/34 yeşil** (33 baseline + 1 post-fix doğrulama). Anayasa: `check_correlation_limits` Siyah Kapı 31 listesinde DEĞİL, yalnızca Kırmızı Bölge dosyasında. Piyasa kapalı doğrulandı (Cumartesi gece, Barış Zamanı) (engine/baba.py, engine/ogul.py)
- #159 — BULGU #8 `receive_feedback()` yanlış teşhis + statik koruma testi (Karma C3 doc + C2 test, ADDITIVE): Audit BULGU #8 fonksiyonu "defined but never called" olarak işaretlemişti, fakat derinlemesine inceleme bunun **yanlış teşhis** olduğunu kanıtladı. Fonksiyon CANLI bir ÜSTAT→BABA RISK_MISS feedback consumer'ıdır. **Gerçek zincir:** (1) Trigger — `Ustat.run_cycle` her cycle `_check_error_attribution(baba, ogul, now)` çağırır (`engine/ustat.py:193`); (2) Detection — son 24 saatteki zararlı trade'ler DB'den çekilir, her biri için `_determine_fault(trade, risk_events)` çalışır → trade süresince RISK_LIMIT/KILL_SWITCH/DRAWDOWN_WARNING vb. event tespit edilir VE exit_reason güvenli kapanış değilse `{"responsible": "BABA", "error_type": "RISK_MISS", ...}` döner (`engine/ustat.py:438-504`); (3) Producer — `engine/ustat.py:418-422` `if attribution.get("responsible") == "BABA": baba.receive_feedback(attribution)`; (4) Consumer — `engine/baba.py:2061` `receive_feedback()` çağrılır → `_risk_miss_log` ve `_risk_miss_count` güncellenir, DB'ye `BABA_FEEDBACK` event yazılır; (5) Aksiyonlar — 24h aynı sembolde 3+ miss → `_activate_kill_switch(level=1)` o sembol için (L1 sembol durdur), 24h toplam 5+ miss → `rp.max_floating_loss` %10 sıkılaştırılır (`_ustat_floating_tightened` bayrağı bir kez). Tetik koşullu ve nadirdir (zararlı işlem + risk event eşleşmesi gerekir) ama BABA'nın ÜSTAT analizine tepki verme TEK mekanizmasıdır — silinirse sembol bazlı L1 koruma + floating_loss tightening sessizce kaybolur. **İki doc bloğu güncellendi:** (a) `baba.py:2061-2098` `receive_feedback` docstring tamamen yeniden yazıldı — "NOT (v5.9.3 — BULGU #8): defined but never called GİBİ görünebilir ama DEĞİLDİR" uyarısı, 5 adımlı tetik zinciri, tetikletilen gerçek aksiyonlar, statik koruma testi referansı, "SİLMEYİN" notu; (b) `ustat.py:411-420` producer call-site yorumu eklendi — consumer adresi (`engine/baba.py:2061`), tetiklenen iki aksiyon ve audit "never called" sanıbilir uyarısı. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_ustat_to_baba_risk_miss_chain_intact` — producer satırı (`baba.receive_feedback(attribution)`), filter koşulu (`responsible == BABA`), `Ustat._determine_fault` BABA branch'i (inspect.getsource), consumer fonksiyon tanımı (`def receive_feedback`), iç aksiyon kalıpları (`_risk_miss_log` + kill_switch + max_floating_loss), ve `Ustat.run_cycle` içinden `_check_error_attribution` çağrısını assert eder. Gelecekte refactor sırasında bu zincirden biri kaybolursa pre-commit hook commit'i bloklar. Mantık değişmedi — saf docstring/yorum + additive statik test, AST temiz, hiçbir runtime semantic etki yok. Etki analizi: 3 dosya, sıfır tüketici/imza değişimi. Kritik akış testleri **34/34 yeşil** (33 baseline + 1 yeni). Piyasa kapalı doğrulandı (Cumartesi gece, Barış Zamanı) (engine/baba.py, engine/ustat.py, tests/critical_flows/test_static_contracts.py)
- #158 — BULGU #7 `_process_ustat_notifications()` yanlış teşhis + statik koruma testi (Karma C3 doc + C2 test, ADDITIVE): Audit BULGU #7 fonksiyonu "dead code" olarak işaretlemişti, fakat derinlemesine inceleme bunun **yanlış teşhis** olduğunu kanıtladı. Fonksiyon CANLI bir ÜSTAT→BABA feedback loop consumer'ıdır. **Gerçek zincir:** (1) Producer — `engine/ustat.py:1591` `rp.ustat_notifications.append(...)` `_apply_baba_feedback` içinde, BABA hatası ≥2 / floating loss / low scores koşulu sağlandığında ve win-rate ≤ %60 ise; (2) Wire — `engine/main.py:139,142` `self.ustat._risk_params = self.risk_params` + `self.baba._risk_params_ref = self.risk_params` (iki motor AYNI `RiskParams` instance'ını paylaşır, kuyruk ortaktır); (3) Consumer — `engine/baba.py:428` `self._process_ustat_notifications()` her cycle'da çağrılır, kuyruğu okur, `USTAT_NOTIFICATION` event'i olarak DB'ye persist edip temizler. Tetik: günde 1 kez ~18:00 daily report sonrası. Nadiren çalışır ama ÜSTAT'ın BABA risk parametrelerinde yaptığı mikro-ayarlamaların audit-trail kaynağıdır — silinirse parametre değişiklikleri sessizce kaybolur. **Üç doc bloğu güncellendi:** (a) `baba.py:425-433` call-site yorumu — producer/wire/consumer adresleriyle "SİLMEYİN" uyarısı; (b) `baba.py:531-571` `_process_ustat_notifications` docstring tamamen yeniden yazıldı — 5 adımlı tetik zinciri, wire açıklaması, statik koruma testi referansı; (c) `ustat.py:1589-1602` producer yorumu — consumer/wire açıklaması + statik koruma testi referansı. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_ustat_to_baba_notification_chain_intact` — producer satırı (`ustat_notifications.append`), iki wire satırı (`baba._risk_params_ref` + `ustat._risk_params`), consumer fonksiyon tanımı (`def _process_ustat_notifications`), `Baba.run_cycle` içinden çağrı, ve fonksiyon gövdesinin hala `ustat_notifications` okuduğunu assert eder. Gelecekte refactor sırasında bu zincirden biri kaybolursa pre-commit hook commit'i bloklar. Mantık değişmedi — saf docstring/yorum + additive statik test, AST temiz, hiçbir runtime semantic etki yok. Etki analizi: 3 dosya, sıfır tüketici/imza değişimi. Kritik akış testleri **33/33 yeşil** (32 baseline + 1 yeni). Piyasa kapalı doğrulandı (Cumartesi gece, Barış Zamanı) (engine/baba.py, engine/ustat.py, tests/critical_flows/test_static_contracts.py)
- #157 — BULGU #6 `check_drawdown_limits()` yanıltıcı isim doc-only fix (C3 Kırmızı Bölge, DOC-ONLY pure docstring): Fonksiyon adı yalnızca "drawdown" çağrıştırıyor ama iki farklı eşiği kontrol ediyor: (1) `risk_params.max_daily_loss` (varsayılan %1.8) — günlük kayıp eşiği, (2) `risk_params.max_total_drawdown` (varsayılan %10) — toplam drawdown eşiği. Hard drawdown (%15) AYRI bir fonksiyonda (`_check_hard_drawdown`, Siyah Kapı #6). Mevcut docstring sadece "Günlük ve toplam drawdown limitlerini kontrol et" diyordu — yeni geliştirici fonksiyonun gerçek kapsamını anlayamıyor, `check_risk_limits` step listesindeki yorum (`3. Günlük kayıp (mevcut check_drawdown_limits)`) sadece yarı doğru, inline call site yorumu (`4. Günlük kayıp (mevcut metod)`) eksikti. **Yeniden adlandırma neden yapılmadı:** fonksiyon `tools/impact_map.py:61` listesinde kayıtlı (kritik fonksiyon), Siyah Kapı stabilite gereği fonksiyon adı korunur — yeniden adlandırma `check_loss_and_drawdown` ya da `check_soft_loss_limits` daha doğru olurdu, doc-only yaklaşım bunu açıkça not eder. **Tek atomik değişiklik:** üç doc bloğu güncellendi: (a) fonksiyon docstring (lines 1165-1192) — iki katmanı açıkça listele, hard drawdown ayrımını kanıt referansıyla göster, yeniden adlandırma önerisi not edildi; (b) `check_risk_limits` step listesi 3. madde (line 1369): "Günlük kayıp" → "Günlük kayıp + toplam drawdown (check_drawdown_limits)"; (c) inline call yorumu line 1432: aynı düzeltme. **Mantık değişmedi** — saf docstring/yorum, AST temiz, hiçbir runtime semantic etki yok. Etki analizi: `grep -rn check_drawdown_limits --include="*.py"` aktif kodda yalnız 4 referans (definition + 2 baba.py iç + tools/impact_map.py listing); tests/ içinde sıfır referans → değişikliğin tüketici zinciri sıfır, regresyon yüzeyi sıfır. Diff: 18 insertion / 3 deletion (tek bloktaki docstring genişlemesi). Kritik akış testleri 32/32 yeşil (baseline + post-fix). Piyasa kapalı doğrulandı (Cumartesi gece, Barış Zamanı) (engine/baba.py)
- #156 — BULGU #5 baba.py modül başlığı stale dokümantasyon fix (C3 Kırmızı Bölge, DOC-ONLY pure docstring): `baba.py` modül başlık docstring'i (lines 1-35) HER şeyiyle gerçek koddan/configten ayrışmıştı. Kullanıcı ya da yeni geliştirici dosyayı açıp başlığı okuduğunda yanlış mental model oluşturuyordu — özellikle finansal risk parametrelerinde stale doc doğrudan operasyonel hata kaynağı. **Kanıt tablosu (9 stale satır):** (1) `(v13.0)` → gerçek `engine/__init__.py:3 VERSION='5.9.0'`; (2) `ATR>ort×2.0` → `ATR_VOLATILE_MULT=2.5` (v14 artışı); (3) `spread>normal×3` → `SPREAD_VOLATILE_MULT=4.0` (v14); (4) `%2+ hareket` → `PRICE_MOVE_PCT=2.5` (v14); (5) `vade son 2 gün` → `EXPIRY_DAYS=0` (v5.9'da kaldırıldı); (6) `Günlük zarar %2.5` → config `max_daily_loss_pct=0.018` = %1.8; (7) `2 saat cool-down` → config `cooldown_hours=4`; (8) `Floating loss %2.0` → config `max_floating_loss_pct=0.015` = %1.5; (9) `Günlük max işlem 8` → config `max_daily_trades=5`. **Tek atomik değişiklik:** modül docstring (lines 1-35) tamamen yeniden yazıldı. Her satıra gerçek kaynak referansı (config anahtarı ya da kod sabit adı) eklendi. Yeni bir "NOT (v5.9.3 — BULGU #5)" paragrafı eklendi: "Tek doğru kaynak `config/default.json` ve aşağıdaki modül-seviye sabitler. Kod-config tutarsızlığı görülürse config kazanır". BULGU #3'ün manuel limit'i de eklendi (`Günlük max manuel işlem 10`). Anayasa Kural 3 (kill-switch monotonluk) ve `_close_ogul_and_hybrid` / `_close_all_positions` L2/L3 fonksiyon adı referansları eklendi. **Mantık değişmedi** — saf docstring/yorum, AST dahil hiçbir runtime semantic etki yok. Siyah Kapu fonksiyonlarına dokunulmadı. Etki analizi: imza yok, ithal yok, tüketici yok — dosya başlığı. AST doğrulama temiz, kritik akış testleri 32/32 yeşil (baseline + post-fix). Diff: 30 insertion / 17 deletion (sadece docstring bloğu). Piyasa kapalı doğrulandı (Cumartesi gece, Barış Zamanı) (engine/baba.py)
- #155 — BULGU #3 manuel işlem ayrı günlük sayaç + 10 limit (Karma C2/C3, ADDITIVE): Manuel motor iki bug taşıyordu. (A) `manuel_motor.py:518` `self.baba.increment_daily_trade_count()` çağırısı `trade_type` parametresi vermiyordu → default `"auto"` kullanılıyordu → her manuel işlem `daily_auto_trade_count`'ı artırıp OTOMATİK bu̇tçeyi (`max_daily_trades=5`) eritiyordu, `daily_manual_trade_count` ise 0'da kalıyordu. (B) `baba.py:1469` sadece `daily_auto_trade_count >= max_daily_trades` kontrolü yapıyordu → manuel işlemler için günlük limit HİÇ yoktu → kullanıcı teorik olarak yetersiz teminat ya da `MAX_CONCURRENT_MANUAL=3` dolana kadar sınırsız manuel işlem açabilirdi. **Kullanıcı talimatı:** "manuel işlemler ayrı olsun 10 adet işlem limiti olsun." **Tek mantıksal değişiklik dört dosyada (atomik):** (1) `config/default.json`: yeni anahtar `max_daily_manual_trades: 10`; (2) `engine/models/risk.py`: `RiskParams` dataclass'ına default 10 ile yeni `max_daily_manual_trades` field'ı; (3) `engine/main.py`: `RiskParams(...)` ya da yapıcısına `max_daily_manual_trades=self.config.get(...)` kwarg'ı; (4) `engine/manuel_motor.py`: pre-trade adım 6.5 yeni manuel limit kontrolü eklendi (`MAX_CONCURRENT_MANUAL`'dan önce, `baba._risk_state.daily_manual_trade_count >= max_daily_manual_trades` ise `result.reason` "Günlük manuel işlem limiti doldu" + `return`); adım 9 increment çağırısı `trade_type="manual"` argument'ı ile düzeltildi; `risk_summary` payload’una `daily_manual_trade_count` ve `max_daily_manual_trades` alanları eklendi (UI gösterimi için). **BABA Anayasa Kural 10 (BABA tek merkez)** korundu — baba.py'ın mevcut otomatik kontrolü (`daily_auto_trade_count >= max_daily_trades`) değiştirilmedi, manuel kontrol manuel motor pre-trade gate'inde uygulandı (manuel motor zaten BABA `check_risk_limits` kapısının arkasında). `increment_daily_trade_count` zaten `trade_type` parametresini destekliyordu (sadece çağıran taraf eksikti). Etki analizi: `RiskParams` constructor 4 yerde çağırılıyor (main.py + 3 test) — yeni alan default'lu, geriye uyumlu; `increment_daily_trade_count` diğer çağıran (`ogul.py:1897`) parametre vermiyor — default `"auto"` OTOMATİK işlemler için doğru. Kritik akış testleri 32/32 yeşil (baseline + post-fix), AST doğrulama 4/4 dosya temiz. Piyasa kapalı doğrulandı (Cumartesi gece, Barış Zamanı) (config/default.json, engine/models/risk.py, engine/main.py, engine/manuel_motor.py)
- #154 — BULGU #4 `activate_kill_switch_l1` kilit eksikliği fix (C3 Kırmızı Bölge, ADDITIVE bug fix): `activate_kill_switch_l1` fonksiyonu (`baba.py:2315`) `_kill_switch_level`, `_kill_switch_details` ve `_killed_symbols` state'lerini `_ks_lock` olmadan mute ediyordu; buna karşılık kardeşi `_activate_kill_switch` ve `_clear_kill_switch` doğru pattern ile `with self._ks_lock:` kullanıyordu. Race senaryosu: Thread A `_activate_kill_switch(L2, ...)` çağırarak `_kill_switch_level = 2` atıyor; Thread B eski değeri okuyan `activate_kill_switch_l1` çağırısı `<= L1` kontrolünü geçip `_kill_switch_level = 1` overwrite edebiliyordu → **Anayasa Kural 3 monotonluk ihlali** (L2→L1 düşürme + L2 details kaybı, otomatik downgrade YASAK). Ayrıca `_killed_symbols.add()` set iterasyonu sırasında concurrent mutasyon riski taşıyordu. Tek atomik değişiklik: state mutasyon bloğu `with self._ks_lock:` ile sarıldı (`_activate_kill_switch` ile aynı pattern). `logger.warning` ve `_log_kill_event` çağırıları kilit dışında tutuldu (deadlock riski yok). Etki analizi: 1 çağıran (`baba.py:2636`, `_evaluate_kill_switch_triggers`), imza değişmiyor, Siyah Kapı listesinde yok → C3 klasifikasyonu. Kritik akış testleri 32/32 yeşil (baseline + post-fix), AST doğrulama temiz. Piyasa kapalı doğrulandı (Cuma gece, Barış Zamanı) (engine/baba.py)
- #153 — BULGU #2 `_still_above_hard_drawdown` sessiz ölü fonksiyon fix (C3 Kırmızı Bölge, ADDITIVE bug fix): `_close_all_positions` (Siyah Kapı #3, L3 akıllı kapanış) zarardaki pozisyonları kapattıktan sonra "hâlâ hard drawdown eşiği üstünde miyiz?" sorusunu `_still_above_hard_drawdown()` ile soruyordu. Ancak fonksiyon `self._risk_state.get("peak_equity", equity)` okuyordu ve `peak_equity` key'i `_risk_state` init dict'ine (satır 325-339) HİÇ EKLENMEMİŞTİ, projede başka hiçbir yerde de yazılmıyordu → `.get()` her zaman default (current equity) döndürüyordu → `dd = (peak-equity)/peak = 0` → fonksiyon her zaman `False` dönüyordu → felaket drawdown devam ediyor olsa bile kârdaki pozisyonlar sessizce korunuyordu (Anayasa Kural 6 "Hard drawdown ≥%15 → L3 → tüm pozisyonlar anında kapatılır" niyeti ölü). Tek atomik değişiklik: `_still_above_hard_drawdown` fonksiyonu yeniden yazıldı → `data_pipeline._calculate_drawdown` tarafından hesaplanıp `risk_snapshot`'a yazılan `drawdown` alanı (`_check_hard_drawdown`'ın zaten kullandığı doğrulanmış kaynak) + `self._config.get("risk.hard_drawdown_pct", 0.15)` eşiği kullanılıyor. Bozuk `peak_equity` / `hard_drawdown` key referansları tamamen kaldırıldı (başka tüketici yoktu). Siyah Kapı `_close_all_positions` DEĞİŞMEDİ — sadece çağırdığı yardımcının bozuk implementasyonu çalışır hâle getirildi. İmza değişmiyor, tek çağıran (baba.py:2416). Kritik akış testleri 32/32 yeşil, AST doğrulama temiz. Piyasa kapalı doğrulandı (Cuma gece, Barış Zamanı) (engine/baba.py)
- #152 — BULGU #1 OLAY rejimi Anayasa Kural 7 uyumu (C4 Siyah Kapı, ADDITIVE bug fix): OLAY rejimi (TCMB/FED günü, vade bitişi, USD/TRY şoku) algılandığında `_activate_kill_switch(L2, "olay_regime", ...)` çağrılıyordu ama L2 kapanış tetiği sadece `("daily_loss", "monthly_loss")` için çalışıyordu → OĞUL + Hybrid pozisyonları piyasa haberine karşı savunmasız açık kalıyordu (Anayasa Kural 7 "Yeni işlem açılmaz, mevcutlar kapatılır" ihlali). Ayrıca `check_risk_limits` OLAY L2 için `risk_multiplier = 0.15` veriyordu (Kural 7 "0.0" diyor). İki atomik değişiklik: (A) `_activate_kill_switch` satır 2170 tetik listesine `"olay_regime"` eklendi → `_close_ogul_and_hybrid` artık OLAY'da da çağrılıyor, manuel pozisyonlar dokunulmaz kalıyor; (B) `check_risk_limits` satır 1377 OLAY risk_multiplier 0.15→0.0. Siyah Kapı mantığı değişmedi — Anayasa'nın dikte ettiği davranış restore edildi (güvenlik katmanı ekleme). Etki analizi: `check_risk_limits` 9 çağıran (api/live, api/risk, main, manuel_motor, simulation+4 test), imza değişmiyor. Kritik akış testleri 32/32 yeşil. Piyasa kapalı doğrulandı (Cuma 21:42 yerel, Barış Zamanı) (engine/baba.py)

### Fixed
- #151 — Uygulama açılmıyor bug'ı 4 katmanlı kalıcı fix (TDD fail-first + 4 atomik commit): **Kök neden zinciri** — (A) `desktop/main.js` singleton çatışmasında `app.quit()` (asenkron) kullanıyordu → `whenReady()` promise yine fire ediyor → `createWindow()` hayalet pencere oluşturuyor → Chromium userData mutex sonsuza dek bloke oluyordu; (B) `ustat_agent.py handle_stop_app` PowerShell `Get-Process | Where CommandLine -match` kullanıyordu ama Windows `Get-Process.CommandLine` property'si boştur → regex hiçbir zaman eşleşmiyor → Electron asla öldürülmüyor → her `restart_app` zombi Electron biriktiriyordu; (C) iki bug birleşince 3-5 restart sonrasında uygulama sonsuza dek açılmaz hale geliyordu. **Çözüm:** (1) `main.js` singleton conflict handler → `app.exit(42)` ile SENKRON çıkış + exit code 42 parent Python'a sinyal (`8acecad`); (2) `ustat_agent.py` psutil tabanlı `_kill_ustat_electrons()` helper — `process_iter()` ile name="electron" + cmdline USTAT içerenleri filtrele, ajan PID'ini koruyarak tree-kill (`97b334b`); (3) `start_ustat.py run_webview_process` (C4 Siyah Kapı, ADDITIVE) — Electron exit 42 yakala → ProcessGuard `_find_orphan_electrons()` + `_tree_kill()` sweep → 1.5sn Chromium mutex release bekle → 1 kez retry, MAX_SINGLETON_RETRIES=1 sonsuz döngü koruması (`969ffd0`); (4) `tests/critical_flows/test_static_contracts.py` flow 13/14/15 fail-first testleri — Patch'lerden ÖNCE yazıldı ve fail olduğu kanıtlandı, sonra patch'lerle YEŞİLE döndü. **Live doğrulama:** 32/32 kritik akış testi geçti, restart sonrası API port 8000 0.6sn'de hazır (Anayasa Kural 14 ≤5sn ✓), Electron PID 50068 temiz başladı, hayalet process yok. Patch A Yellow Zone C2, Patch B Green Zone C1, Patch C Red Zone C4 additive — Anayasa Kural 11 (başlatma zinciri) ve Siyah Kapı #25 `run_webview_process` mantığı değişmedi (desktop/main.js, ustat_agent.py, start_ustat.py, tests/critical_flows/test_static_contracts.py)
- #150 — OĞUL kapsamlı 14 bulgu atomik fix seti (kanıta dayalı, 14 commit): **C-1** lot=1.0 hardcode → `BABA.calculate_position_size` (ATR + equity guard'lı, Anayasa Kural 4 uyumlu — `c74a2fe`); **C-2+O-6** yön konsensüsü 1→2 oy, H1 slope 0.001→0.005 (false pozitif azaltma — `268f8fc`); **C-4** SL ekleme başarısız branch'te `return` + `CANCELLED` + DB FILLED yazımı engellendi (`683af1c`); **C-6** commission/swap sadece `deal_summary` başarılıysa DB'ye yazılır (muhasebe sadakati — `20a631f`); **C-5** EOD verify retry 5×1s → 3×0.5s (main loop bloklaması azaltma — `a97f8f5`); **Y-1** `baba._kill_switch_level` → public `kill_switch_level` property (encapsulation — `dd7c708`); **Y-2** `_risk_multiplier` field sızıntısı temizliği — BABA zaten çarpanı uyguluyor (main.py + ogul.py — `0325865`); **O-4/O-5** ATR oylaması yön filtresi (son 3 mum konsensüsü, ATR tek başına yön vermez — `fcf8be9`); **Y-5** mean reversion TP = `bb_mid` (önceki `bb_mid ± 0.3*ATR` reversion tanımına aykırıydı — `aebb252`); **O-10** breakeven eşiği `BE_ATR_BY_CLASS` likidite sınıfına göre (A=1.0×, B=1.3×, C=1.5×ATR — `f665461`); **O-14** volume spike çıkış eşiği `-0.3×ATR` → `-0.8×ATR` (erken çıkış azaltma, trailing SL ile uyumlu — `875313c`); **O-9** breakout SL yapısal: `low_20/high_20 ∓ 0.2×ATR` buffer (false BO'da hızlı çıkış, R/R iyileşir — `9c9d610`); **O-1/D-7** ölü state temizliği — `_daily_loss_stop`, `_monthly_*` field'ları OĞUL'dan kaldırıldı (Anayasa Kural 10 gereği BABA tek merkez), `api/routes/health.py` `daily_loss_stop` artık `baba.kill_switch_level >= 2` turevinden okunuyor (API contract korundu — `5db89ac`); **Y-9** yetim pozisyon `warning` → `critical` log + `event_bus.emit("orphan_detected")` (frontend realtime uyarı — `f368118`). Toplam ~150 satır değişim (<%10 eşiği altında, patch bump opsiyonel). Tüm AST doğrulamaları + frontend build başarılı. Kaynak analiz: `docs/2026-04-10_ogul_kapsamli_analiz.md`, plan: `docs/2026-04-10_ogul_fixes_plan.md` (engine/ogul.py, engine/baba.py, engine/main.py, api/routes/health.py)

### Changed
- #149 — OĞUL/BABA sorumluluk ayrıştırması (mimari refactor): OĞUL'un BABA ile çakışan risk kontrol fonksiyonları kaldırıldı. Silinen OĞUL fonksiyonları: `_check_advanced_risk_rules()` (153 satır — günlük %3 kayıp, aylık DD, spread/iteration guard'ları), `_check_time_rules()` (86 satır — yatay piyasa + son 45dk kâr kapatma), `_check_cost_average_DISABLED()` (115 satır), `_calculate_lot()` + `_get_current_atr()` (145 satır dead code), `_confirm_h1()` (45 satır). Yerine: BABA'ya `_close_ogul_and_hybrid()` yardımcısı eklendi — L2 kill-switch (`daily_loss`/`monthly_loss`) tetiklendiğinde SADECE OĞUL aktif trade'ler + Hybrid pozisyonları kapatılır, manuel pozisyonlara dokunulmaz. `main.py` wire: `self.baba.ogul = self.ogul`. OĞUL'da 4 orphan guard + EOD exclusion eklendi (manuel + orphan set'leri `_verify_eod_closure`'dan hariç tutuldu). "LIMIT emir gönderildi" yanıltıcı log bloğu temizlendi. Net: OĞUL ~1016 satır azaldı, BABA ~89 satır arttı. Siyah Kapı listesi ve Kural 10 CLAUDE.md'de güncellendi. Live doğrulama: restart sonrası engine temiz başladı, BABA L2 tetiklendi ve `_close_ogul_and_hybrid()` çağrıldı (`engine.baba:2508/2551`) (engine/ogul.py, engine/baba.py, engine/main.py, CLAUDE.md)

### Fixed
- #148 — PRİMNET duplikat bekleyen emir + orphan temizliği: Restart sonrasında 4 orphan PRIMNET_TRAIL/TGT emri kalıyordu (F_AKBNK SELL ticket=8050545494), ters yönlü BUY_STOP_LIMIT emirler directional flip riski yaratıyordu. Kök neden: GCM broker MT5 comment alanını 16 karaktere kırpıyor (`PRIMNET_TRAIL_8050545494` → `PRIMNET_TRAIL_80`). Üç katmanlı savunma eklendi: (1) comment prefix `PRIMNET_TRAIL_`/`PRIMNET_TGT_` → `TRL_`/`TGT_` (14 kar — 16 limit altı), (2) yeni `_cancel_orphan_pendings_by_direction()` yardımcısı — izlenmeyen ters yön pending emirleri yön+tip bazlı iptal eder, (3) `_cleanup_primnet_orphans_on_restart` ve `_restore_stop_limit_tickets`'a fallback entegrasyonu + yeni emir göndermeden önce defansif orphan temizliği (`_trailing_via_stop_limit`). DEBUG logging `_find_orders_by_comment`'e eklendi. 27 comment prefix değiştirildi. Live doğrulama: 12:15:14 fallback 4 orphan temizledi, sistem tek pozisyon+2 bekleyen emirle stabilize (h_engine.py)
- #147 — PRİMNET netting SYNC atomik refactor: lot ekleme/çıkarma senkronizasyonunda state desync (retcode 10019 NO MONEY sonrası hp.volume eski pending emirlerle uyumsuz kalıyordu) giderildi. Margin ön kontrolü (free_margin ≥ 2000, margin_level ≥ 150%), atomik commit (hp.volume/entry_price SADECE emir başarısı sonrası güncellenir), trailing→hedef rollback mekanizması, lot çıkarmada entry_price/SL/TP koruması (netting mode MT5 davranışıyla birebir uyum). Config: `primnet.netting_sync_min_free_margin`, `primnet.netting_sync_min_margin_level` (h_engine.py, tests/test_hybrid_100.py)

### Added
- #139 — Kill-Switch bilgi modalı: Risk Yönetimi sayfasında Kill-Switch kartına tıklanınca seviye, neden, tetiklenme zamanı ve engelli kontratları gösteren modal pencere açılıyor (RiskManagement.jsx, api/routes/risk.py, api/schemas.py)
- #132 — Dashboard sürükle-bırak kart sıralaması: @dnd-kit ile tüm Dashboard bölümleri (8 kart) bağımsız olarak sürüklenip yeniden sıralanabiliyor. 4 stat kartı ayrı ayrı, büyük bölümler tam genişlik grid düzeninde. localStorage ile kalıcı sıralama
- #131 — SortableCard bileşenine className prop desteği eklendi — Dashboard grid düzeni için gerekli CSS class geçişi

### Fixed
- #146 — Kill-Switch L3 h_engine koordinasyonu: BABA L3 tetiklendiğinde `h_engine.force_close_all()` çağrılıyor — STOP LIMIT/LIMIT bekleyen emirler artık yetim kalmıyor (baba.py, main.py)
- #145 — Yön değişimi algılama: VİOP netting ile pozisyon yönü değişirse (ör. SELL→BUY) h_engine tüm PRIMNET emirlerini iptal edip hibrit takibi sonlandırıyor, UI'a critical bildirim gönderiyor (h_engine.py)
- #144 — Lot çıkarma sonrası bekleyen emir güncelleme: `_sync_netting_volume` lot azaltma branch'inde PRIMNET trailing+target emirleri yeni volume ile iptal+yeniden oluşturuluyor (h_engine.py)
- #143 — Trailing LOCK sonrası MT5-bellek desync tespiti: `modify_pending_order` başarısız olduğunda bellek/DB güncelleniyor ama MT5 emri eski fiyatta kalıyordu → LOCK bloğu desync'i gizliyordu. `_verify_trailing_sync()` eklendi: LOCK tetiklendiğinde MT5 bekleyen emri kontrol eder, fark varsa cancel+replace yapar. Restart sonrası yetim emir sahiplenmesi (comment-based) de destekleniyor (h_engine.py)
- #142 — Trailing Stop modify STOP LIMIT emirlerde "Invalid price" hatası: `modify_pending_order` sadece `price` gönderiyordu, `stoplimit` eksikti → SELL STOP LIMIT'te stoplimit < price kuralı ihlali (retcode 10015). Fonksiyona opsiyonel `new_stoplimit` parametresi eklendi, `_trailing_via_stop_limit` limit fiyatını da geçiyor (mt5_bridge.py, h_engine.py)
- #141 — Netting lot ekleme sonrası MT5 bekleyen emirler güncellenmiyor: `_sync_netting_volume` hp.current_sl'yi yeniden hesaplıyor ama MT5 stop limit emri eski fiyat+volume ile kalıyordu → iptal+yeni emir mekanizması eklendi (trailing + hedef, h_engine.py)
- #140 — Hibrit devir "Invalid order" hatası: GCM VİOP düz STOP emirleri desteklemiyor — `send_stop()` → `send_stop_limit()` dönüşümü yapıldı. 3 nokta düzeltildi: `transfer_to_hybrid()`, `_trailing_via_stop_limit()`, `_daily_primnet_refresh()` (h_engine.py)
- #134 — Veri yönetim sistemi çakışma düzeltmesi: `_run_daily_cleanup()` events ve risk_snapshots'ı aggregation yapmadan siliyordu → `daily_risk_summary` veri kaybına yol açıyordu. Cleanup artık sadece bars temizliyor, events/snapshots tamamen `run_retention()` tarafından yönetiliyor (main.py)
- #135 — Temizlik/retention/bakım tarihleri restart sonrası kayboluyordu (`None`'a dönüyordu → aynı gün tekrar çalışma riski). Tarihler artık `app_state` tablosuna persist ediliyor ve `_restore_state()` ile geri yükleniyor (main.py)
- #136 — Retention hiç çalışmıyordu (saat ≥18 koşulu + piyasa 18:15'te kapanma → pencere çok dar). Koşul 17:00'ye alındı (main.py)
- #137 — NABIZ çakışma algılama kodu (`nabiz.py`) yanlış sabit değerler kullanıyordu (events=60, snapshots=90) — gerçek cleanup config'den okuyor. Çakışma algılama mantığı güncellendi, retention kapalıysa uyarı veriyor
- #130 — OĞUL SE3 rejim-strateji filtresi: SE3 sinyal motoru regime.allowed_strategies kontrolünü atlatıyordu — RANGE rejiminde trend_follow sinyalleri üretilip zarar eden işlemler açılıyordu. Confluence döngüsüne rejim kapısı eklendi (ogul.py)

### Changed
- #138 — NABIZ retention tarihleri artık DB fallback ile okunuyor — engine bellekte yoksa `app_state` tablosundan okunuyor (nabiz.py)

### Fixed
- #139 — NABIZ üst kartlarında status ternary sırası hatalıydı: `>500 ? warn : >1000 ? err` → `err` koşuluna hiçbir zaman ulaşılamıyordu. Sıra düzeltildi: `>1000 ? err : >500 ? warn` (Nabiz.jsx, VERİTABANI + LOG DOSYALARI kartları)
- #133 — CLAUDE.md Bölüm 7 ADIM 1 güncellendi: Electron production build zorunluluğu detaylı açıklandı — kaynak dosya düzenlemek yetmez, `npm run build` + shortcut güncelleme + restart gerekir

### Security
- #129 — MT5 auto-launch koruma genişletme: `api/routes/mt5_verify.py _verify()` ve `health_check.py`'ye de terminal64.exe process kontrolü eklendi — mt5.initialize() çağrılmadan önce MT5 çalışıyor mu diye bakılıyor. `start_ustat.py` ölü pywebview koduna uyarı eklendi
- #128 — MT5 auto-launch koruma: Engine connect(launch=False) artık mt5.initialize() çağırmadan ÖNCE terminal64.exe process kontrolü yapıyor — process yoksa bağlantı atlanır, MT5 asla otomatik açılmaz. Siyah Kapı #31 + Anayasa Kural 4.15 eklendi. 10/10 açma-kapama testi geçti
- #127 — Electron single-instance kilidi HER ZAMAN aktif: `requestSingleInstanceLock()` artık API modda da çalışıyor — birden fazla pencere açılması engellendi (desktop/main.js)
- #126 — Ajan singleton koruması: PID dosyası + psutil kontrolü ile çoklu ajan instance engellendi. Atomik komut kilitleme (.processing rename) ile aynı komutun birden fazla kez işlenmesi önlendi (ustat_agent.py v3.2.0)
- #125 — handle_start_app Electron process kontrolü: Uygulama başlatılmadan önce hem API portu hem Electron process'i kontrol ediliyor — çift başlatma engellendi

### Fixed
- #124 — API başlatma hatası kök neden: pythonw.exe stdout/stderr=None yapıyor → uvicorn sessizce çöküyordu. python.exe + CREATE_NO_WINDOW ile değiştirildi, start_ustat.py'ya stdout/stderr fallback koruması eklendi
- #123 — Named Mutex namespace: `Local\` → `Global\` değiştirildi — admin/normal kullanıcı oturumları arası tek instance koruması sağlandı. TIME_WAIT socket süresi 120sn→30sn (TcpTimedWaitDelay registry)

### Fixed
- #122 — OĞUL anında kapanış sorunu: `_send_order_inner` TRADE_ACTION_SLTP başarısız olduğunda pozisyonu hemen kapatıyordu (GCM VİOP exchange modda SLTP desteklenmiyor). Artık OgulSLTP mekanizmasına (plain STOP pending emir) bırakıyor, pozisyon korumalı yaşıyor. OgulSLTP de başarısız olursa Anayasa 4.4 kuralı `_execute_signal`'da uygulanıyor

### Added
- #112 — ProcessGuard hayalet koruma modülü: PID registry, tree-kill, orphan Electron/MT5 tespiti, port/socket temizliği — kapanışta kalan zombie process/port/socket sorununu çözer
- #113 — api.pid dosya yazımı: start_ustat.py API başladığında PID yazar, Electron killApiProcess() artık API process'i bulabilir
- #114 — WebSocket graceful shutdown: shutdown_all_connections() fonksiyonu — kapanışta tüm WS bağlantıları kapatılır, TIME_WAIT socket birikimi önlenir
- #107 — pywebview migrasyonu: Electron+Vite → pywebview+pystray tek process mimarisi, SPA static serving FastAPI'den (65dc9ff)

### Fixed
- #115 — Shutdown zinciri düzeltmesi: _shutdown_api() timeout 20sn→45sn (engine.stop() 30sn sürebilir), çift çağrı koruması, Electron tree-kill subprocess durumundan bağımsız hale getirildi
- #116 — Kapanış sırası düzeltmesi: server.py lifespan shutdown — WebSocket kapat → Engine durdur → api.pid temizle (eskiden WebSocket cleanup yoktu)
- #108 — CORS/origin düzeltmesi: api.js absolute URL→relative (`/api`), server.py CORS listesine `127.0.0.1:8000` eklendi — pywebview origin uyumsuzluğu giderildi
- #109 — logger.py UTF-8 stderr sarmalayıcı: pythonw.exe charmap codec hatası düzeltildi — MT5 bağlantı başarısızlığının kök nedeni
- #110 — Tek instance kilidi: start_ustat.py port+PID kontrolü, stale lock temizleme, mevcut pencereyi öne getirme (EnumWindows)
- #111 — pywebview shim API referansı: `window.pywebviewApi`→`window.pywebview.api` — pencere kontrolleri, güvenli kapat, pin butonu çalışmıyordu (26b4945)
- #111 — Pencere maximized başlatma: `maximized=True` eklendi — uygulama tam ekran açılıyor (26b4945)
- #106 — 8 güvenlik açığı düzeltmesi: 1 Nisan olay raporu (230ee5f)

### Changed
- #105 — Otonom vade geçiş sistemi: 4 katmanlı koruma — trade_mode tabanlı kontrat seçimi, saatlik periyodik tarama, retcode 10044 reaktif handler, eski vade pozisyon tespiti + VADE_UYARI/STALE_POSITION event'leri (34ded93)
- #105 — Yarım gün (arefe) desteği: HALF_DAYS setleri (2025-2027), is_half_day(), get_close_time(), is_market_open() arefe günü 12:40 kapanış (34ded93)
- #104 — Veri Yönetim Sistemi iyileştirme: OHLCV validasyon katmanı (Katman 1), veri bayatlık tespiti (freshness monitor — FRESH/STALE/SOURCE_FAILURE ayrımı), SQLite PRAGMA optimizasyonu (synchronous=NORMAL, cache_size=64MB, mmap_size=256MB)
- #104 — Retention düzeltme: M1/M5 bar temizliği eklendi, hard-coded retention→config'den okuma, trade_archive_days config desteği
- #104 — 7 sessiz hata düzeltmesi: OĞUL SE3 `except: pass`→loglama, M5 yetersiz veri DEBUG→WARNING, H1/Voting atlama logları, insert_top5 boş liste koruması, insert_risk_snapshot güvenli .get() erişimi, insert_bars dönüş değeri kontrolü
- #90 — Log Yönetim Sistemi v3.0: FUSE cache bypass — 5 yeni ajan komutu (fresh_engine_log, search_all_logs, log_digest, log_stats, log_export), Claude Bridge v4.0, log_reader.py yardımcı scripti
- #55 — ÜSTAT Beyin Merkezi sayfası: trade kategorileri, kontrat profilleri, rejim analizi, hata atfetme (3d75fe3)
- #56 — Haber entegrasyonu: MT5 Calendar → NewsBridge → BABA/OĞUL erken uyarı sistemi (2026-03-23)
- #69 — Kademeli trailing stop + breakeven floor: ATR bazlı 3 fazlı sistem (2026-03-25)
- #70 — OĞUL manuel pozisyon koruma kalkanı: cross-motor netting koruması (2026-03-25)
- #73 — MT5-Direct pozisyon sahiplenme: ManuelMotor adopt mekanizması (2026-03-26)
- #79 — PRİMNET: Prim bazlı net emir takip sistemi — H-Engine yeni pozisyon yönetim modu (2026-03-27)
- #80 — PRİMNET v2: Bildirim sistemi, PrimnetDetail modalı, hibrit performans istatistikleri, notifications API (6a38f64)
- #82 — Frameless titlebar: SVG pencere kontrolleri, IPC bridge (da730b1)
- #82 — Ajan Windows Service desteği: pywin32, crash restart, auto-install, shutdown.signal (b56ef71)
- #82 — Watchdog OTP bekleme: WATCHDOG_INITIAL_DELAY=30sn, eski heartbeat temizleme (da730b1)
- #83 — 10.003 kombinasyonlu stres testi: BABA/OĞUL/H-Engine/MT5/Manuel/Top5/DB/Entegrasyon (a36f3d0)

### Added
- #120 — mt5_bridge.py'ye 4 yeni fonksiyon: send_stop (BUY/SELL STOP), send_limit (BUY/SELL LIMIT), modify_pending_order, cancel_pending_order — plain STOP/LIMIT emir desteği
- #119 — OĞUL Stop Limit SL/TP sistemi: ogul_sltp.py modülü — GCM VİOP netting modunda TRADE_ACTION_SLTP yerine bekleyen emirlerle SL koruması
- #119 — Trade modeline sl_order_ticket alanı eklendi (bekleyen emir takibi)

### Fixed
- #121 — PRİMNET trailing stop KİLİT mekanizması: send_stop() başarısız olduğunda hp.current_sl güncellenmiyordu → trailing geri gidiyordu. Artık her durumda bellek+DB güncelleniyor, software SL güvenlik ağı olarak çalışıyor (734999e)
- #121 — PRİMNET çift emir temizliği: _cancel_stop_limit_orders artık ticket + comment deseni ile arama yapıyor → restart sonrası kalan orphan emirler de temizleniyor (734999e)
- #121 — PRİMNET 0.50 prim adım filtresi: trailing stop artık giriş priminden başlayan 0.50'lik ızgaraya yuvarlanıyor (config step_prim=0.5). Floating point hatası round(,10) ile giderildi
- #120 — Stop Limit → plain STOP/LIMIT migrasyonu: Stop Limit emirleri tetiklendiğinde limit fiyatı yüzünden dolmuyordu (SL) veya [Invalid price] hatası veriyordu (TP). Plain STOP (SL için — market dolum garantili) ve plain LIMIT (TP için — doğru fiyat yönü) ile değiştirildi

### Changed
- #120 — H-Engine trailing: _trailing_via_stop_limit() artık send_stop + modify_pending_order kullanıyor (Stop Limit + gap yerine tek fiyatlı plain STOP)
- #120 — H-Engine hedef: _place_target_stop_limit() artık send_limit kullanıyor (TP için plain LIMIT — [Invalid price] hatası düzeldi)
- #120 — H-Engine devir + yenileme: send_stop_limit çağrıları send_stop/send_limit ile değiştirildi, _cancel_stop_limit_orders cancel_pending_order kullanıyor
- #120 — ogul_sltp.py: Stop Limit → plain STOP migrasyonu, gap/limit_price hesabı kaldırıldı, modify_stop_limit → modify_pending_order
- #119 — OĞUL'da tüm modify_position çağrıları OgulSLTP.update_trailing_sl() ile değiştirildi — breakeven, KORUMA, TREND, SAVUNMA, fallback trailing, MR breakeven, breakout trailing
- #119 — Lifecycle entegrasyonu: _handle_closed_trade → SL iptal, EOD → SL iptal, _sync_positions → SL tetiklenme kontrolü, restore_active_trades → SL kurtarma
- #119 — config/default.json'a ogul.stop_limit_gap_prim=0.3 eklendi
- #118 — Motor izolasyonu: OĞUL ardışık kayıp sayacı motor bazlı ayrıştırma — H-Engine/ManuelMotor kayıpları OĞUL cooldown'ını tetiklemez, OĞUL trade kaydına `source: "auto"` eklendi (baba.py, ogul.py)
- #117 — PRİMNET Stop Limit emir sistemi: TRADE_ACTION_SLTP (modify_position) yerine Buy/Sell Stop Limit bekleyen emirler ile trailing stop ve hedef yönetimi — GCM VİOP netting modunda "Invalid order" (retcode 10035) sorununu çözer
- #117 — mt5_bridge.py'ye 3 yeni fonksiyon: send_stop_limit, modify_stop_limit, cancel_stop_limit + get_pending_orders STOP_LIMIT tip tanıma
- #117 — H-Engine tam yaşam döngüsü: devir→trailing→hedef→kapanış→gece geçişi→restart kurtarma Stop Limit emirlerle
- #97 — PRİMNET sabit trailing: Faz 1/Faz 2 ayrımı kaldırıldı, her zaman 1.5 prim sabit trailing, stop > giriş olduğunda kilitli kâr otomatik (9020e43)

### Fixed
- #105 — VIOP_EXPIRY_DATES Mayıs 2026 düzeltmesi: date(2026,5,29)→date(2026,5,25) — Kurban Bayramı arefesi yarım gün kuralı nedeniyle vade 25 Mayıs Pazartesi (34ded93)
- #103 — OĞUL 3 katmanlı pipeline: SE3 binary→sürekli strength, confluence ≥50 kapı→çarpan (min 20), yön 2/3→1/3, R:R sert blok→penalty, risk_multiplier lot çarpanı (116cc5e, c755cf2, 04dc627, b0baa87)
- #99 — 16 kritik sessiz mayın temizliği: haber filtresi VİOP ilgililik kontrolü, H1 iloc/label index, vade CRITICAL log, risk fail-safe, api.js client fix, 5 silent pass→logger (536ccb7, 57989f2)
- #100 — Engine başlatma resilience: MT5 retry 15sn×20 döngü, smoke test toleransı (kısıtlı mod), API os._exit kaldırıldı→yeniden başlatma (59d81cd)
- #101 — Açılış hızı optimizasyonu: wait_for_port 1s→0.2s polling, cleanup sleep'ler kısaltıldı, watchdog 30→10sn, splash 10→4sn, Vite poll 500→150ms (6f62507)
- #102 — Nabız hardcoded kartlar dinamik kontrole çevrildi: çakışma uyarısı config karşılaştırması, missing_retention DB'den hesaplama (4c6364e)
- #98 — PRİMNET devir hatası: h_engine.py'de 6 yerde eski _primnet_faz1_stop/_primnet_faz2_trailing referansları _primnet_trailing olarak düzeltildi — transfer_to_hybrid, netting sync, referans fiyat güncelleme fonksiyonları
- #75 — Vade geçişi GCM paraleli: EXPIRY_NO_NEW_TRADE_DAYS/EXPIRY_CLOSE_DAYS=1→0, gözlem süresi kaldırıldı (2026-03-26)
- #80 — H-Engine: ATR/profit trailing kaldırıldı, PRİMNET tek pozisyon yönetim modu (6a38f64)
- #80 — OĞUL EOD hibrit kapatma kaldırıldı — her motor kendi işini yapacak (6a38f64)
- #80 — Eski trailing mantığı arşive taşındı: engine/archive/h_engine_trailing_legacy/ (6a38f64)
- #81 — OĞUL v5.9 yeniden yapılandırma: 44 bulgu, 5 aşamalı revizyon (e57b008)
- #81 — Sinyal mimarisi: yön konsensüsü (3 kaynak, 2/3 çoğunluk) + tek confluence gate (e57b008)
- #81 — Pozisyon yönetimi: 4 modlu adaptif sistem (KORUMA→TREND→SAVUNMA→ÇIKIŞ) (e57b008)
- #81 — Emir mekanizması: limit→market, lot=1 sabit (test süreci) (e57b008)
- #81 — TRADING_CLOSE, MAX_LOT, MAX_CONCURRENT config'den okunuyor (e57b008)
- #81 — 700+ satır ölü kod kaldırıldı, SE2→SE3 isimlendirme (e57b008)
- #82 — BABA EXPIRY_DAYS=2→0: vade kısıtlaması kaldırıldı (da730b1)

### Changed
- #85 — Hibrit İşlem Paneli sürükle-bırak: react-grid-layout → @dnd-kit — 5 kart sürüklenebilir sıralama, localStorage kalıcılık, sıfırla butonu

### Fixed
- #91 — Vade geçişi otomasyonu: `_resolve_symbols()` en yeni vadeden activate eder — vade son günü otomatik yeni kontrata geçiş (mt5_bridge.py)
- #92 — Haber kaynaklı L1 kontrat engeli günlük sıfırlama: `_reset_daily()` önceki günün L1'ini kaldırır (baba.py)
- #93 — VİOP ilgisizlik filtresi: `get_news_warnings()` currency kontrolü + SYMBOL_KEYWORDS genel sektör kelimeleri temizlendi (news_bridge.py)
- #94 — OLAY rejimi vade kontrolü: `_check_olay()` EXPIRY_DAYS=0 ile `<=` → `<` operatörü (baba.py)
- #95 — Pozisyon görünürlük: `_to_base()` prefix fallback + `_resolve_symbols()` çoklu visible'da max seçimi (mt5_bridge.py)
- #96 — Başlatıcı: shutdown.signal koşulsuz temizleme + API timeout 30→15sn (start_ustat.py)
- #88 — Confluence strateji-bazlı skorlama: trend_follow/breakout sinyallerinde RSI>70 ve direnç yakınlığı cezaları kaldırıldı — 184 sinyal/0 işlem sorununu çözer
- #89 — PrimnetDetail.jsx Faz 2 açıklama satırları düzeltmesi + theme.css PRİMNET modal stilleri iyileştirmesi
- #76 — VİOP ilgisizlik filtresi: USD haberlerinin Türk hisselerinde yanlış OLAY rejimi tetiklemesi (2026-03-27)
- #77 — Exchange netting position ticket: VİOP netting modunda ticket çözümleme hatası (2026-03-27)
- #78 — Native SL/TP config geri alma: yanlış config override düzeltmesi (2026-03-27)
- #81 — _check_advanced_risk_rules() + _check_time_rules() çağrılmıyordu — 5 risk kontrolü devre dışıydı (e57b008)
- #81 — SE3'e symbol gönderilmiyordu — haber kaynağı sessizce devre dışıydı (e57b008)
- #83 — Vade günü Top 5 engeli: `<=` → `<` operatörü — 31 Mart tüm semboller engelleniyordu (1d93ffd)
- #83 — Ajan ChangeServiceConfig parametre çakışması + main() eksik (ebab753)
- #83 — ManualTrade MT5 pozisyon filtresi: `'Manuel'` → `'Manuel' || 'MT5'` (ebab753)
- #83 — Settings.jsx VERSION=5.8→5.9, BUILD_DATE=03-26→03-29 (7bba799)
- #83 — Python 3.10 __pycache__ kalıntıları temizlendi, build yenilendi
- #84 — Event deduplication: aynı uyarı 5dk içinde tekrar DB'ye yazılmaz — EARLY_WARNING 3600+ spam düzeltildi (ddcb053)
- #84 — Gelişim tarihçesi Keep a Changelog formatına dönüştürüldü (0927520)
- #84 — Dosya organizasyonu: raporlar RAPORLAR/, oturum raporları docs/ (2b64dc7)
- #86 — Ajan start_app Session 0 sorunu: subprocess.Popen → schtasks /IT ile kullanıcı oturumunda başlatma
- #87 — Pozisyon tür ayrımı: _source_for_position() fallback düzeltmesi — DB'de source boş olan pozisyonlar MT5 olarak etiketleniyor

---

## [5.8.0] — 2026-03-20 / 2026-03-26

### Added
- #46 — Otomatik İşlem Paneli WebSocket entegrasyonu: canlı pozisyon + durum güncellemesi (2026-03-20)
- #48 — OĞUL v2 Revizyon: 6 aşamalı pipeline yenileme, aktif işlem kapasitesi (2026-03-21)
- #49 — M5 sinyal tetikleme: M15→M5 timeframe geçişi (2026-03-21)
- #50 — OĞUL tam mimari dokümantasyonu (2026-03-21)
- #51 — ANAYASA v2.0: CEO genişletmesi, Siyah Kapı tanımları (2026-03-21)
- #52 — CEO FAZ-1: Kritik güvenlik yamaları (2026-03-21)
- #53 — CEO FAZ-2: Altyapı güçlendirme (2026-03-21)
- #54 — CEO FAZ-3: Governance — modüler refactoring (2026-03-21)
- #57 — Gelişim tarihçesi audit: yapılmadı/atlandı/yarım kaldı analizi (2026-03-23)
- #58 — OĞUL v13.0 işlem yönetimi iyileştirmeleri (2026-03-22)
- #59 — Stres test altyapısı: FAZ-1/2/3 (2026-03-22)
- #60 — Hibrit Motor breakeven/trailing parametre optimizasyonu (2026-03-24)
- #63 — Kâr bazlı trailing stop (Profit Trailing) (2026-03-24)
- #65 — Zombie process koruması: CIM fallback (2026-03-25)
- #67 — Trailing mesafe sınırları: VİOP rapor uyumu (2026-03-25)
- #71 — Güvenli çıkış: shutdown.signal + watchdog singleton düzeltmesi (2026-03-26)
- #72 — Tam sistem audit: 25 dosya, 8 kategori düzeltme + ÜSTAT beyin geliştirme (2026-03-26)

### Changed
- #62 — Motor bazlı floating ayrıştırma: CEO Option C (2026-03-24)
- #74 — WebSocket TÜR sütunu tutarsızlığı düzeltmesi (2026-03-26)

### Fixed
- #45 — Kırmızı Bölge kapsamlı bug tarama: 29 sorun tespiti ve düzeltmesi (2026-03-20)
- #47 — v5.6 Kırmızı Bölge: 9 bug düzeltmesi (3 kritik, 3 yüksek, 3 orta) (2026-03-20)
- #61 — VİOP limit emir invalid expiration düzeltmesi (2026-03-24)
- #66 — SSE tür tutarsızlığı: Manuel/Hibrit flipping sorunu (2026-03-25)

### Security
- #52 — SL/TP zorunluluk kontrolü güçlendirildi (2026-03-21)
- #54 — Circuit breaker cooldown doğrulaması eklendi (2026-03-21)

---

## [5.7.0] — 2026-03-13 / 2026-03-17

### Added
- #40 — v14 Production-Ready: Risk & sinyal motoru tam revizyon (2026-03-13)
- #41 — Tam sistem audit: WebSocket güvenliği, süre etiketi, PnL düzeltmesi (2026-03-16)

### Fixed
- #44 — Risk hesaplama: Günlük/haftalık/aylık PnL doğru sıralama (2026-03-17)

---

## [5.6.0] — 2026-03-10

### Added
- #38 — Kurumsal değerlendirme eleştiri düzeltmeleri (2026-03-10)
- #39 — Sürdürülebilirlik ve çökme direnci iyileştirmeleri (2026-03-10)

### Fixed
- #41 — Kill-Switch L2 döngü bugu düzeltmesi (2026-03-10)
- #42 — Manuel işlem force-close bugu + aktif pozisyon kartları (2026-03-10)
- #43 — OĞUL manuel/hibrit pozisyon sahiplenme bugu (2026-03-10)

---

## [5.4.0] — 2026-03-09 → [5.5.0]

### Added
- #35 — System Monitor: Sistem sağlığı + günlük birleştirme (2026-03-09)
- #36 — Monitor: Çift yönlü oklar + metin okunabilirliği (2026-03-09)

### Changed
- Versiyon geçişi: v5.3 → v5.4 (2026-03-09)

### Fixed
- #37 — Manuel/Hibrit bug fix + risk baseline ayarı (2026-03-09)

### Removed
- #14 — Ölü kod temizliği + performans optimizasyonu (2026-03-09)

---

## [5.3.0] — 2026-03-08

### Added
- #29 — Kod merkezileştirme + ekran inceleme düzeltmeleri (2026-03-08)
- #31 — Bağımsız Manuel İşlem Motoru (ManuelMotor) (2026-03-07)
- #32 — 50 kişilik uzman ekip raporu uygulaması (2026-03-08)
- #33 — İşlem geçmişi: filtre ve sıralama iyileştirmesi (2026-03-08)

### Changed
- #30 — Versiyon yükseltme: v5.1 → v5.2 (2026-03-08)

### Fixed
- #34 — Engine başlatma hatası: RISK_BASELINE_DATE (2026-03-08)

---

## [5.2.0] — 2026-03-07

### Added
- #13 — OĞUL Evrensel Pozisyon Yönetim Sistemi (2026-03-06)
- #14 — Sistem Sağlığı Paneli (2026-03-06)
- #16 — Dashboard düzenleme + Otomatik İşlem Paneli (2026-03-07)
- #18 — Açık Pozisyonlar Dashboard'a birleştirilmesi (2026-03-07)
- #22 — Event-Driven işlem geçmişi + tarih filtresi (2026-03-07)
- #28 — OĞUL Aktivite Kartı + USDTRY sembol düzeltmesi (2026-03-07)

### Changed
- #15 — REGIME_STRATEGIES risk otoritesi taşınması (2026-03-07)
- #21 — Hibrit işlem türü sınıflandırma (2026-03-07)
- #23 — Dashboard istatistikleri 01.02.2026 tarih filtresi (2026-03-07)

### Fixed
- #17 — Risk baseline tarih güncelleme (2026-03-07)
- #19 — Peak equity doğrulama eşiği düzeltmesi (2026-03-07)
- #20 — İşlem geçmişi baseline filtresini kaldır (2026-03-07)
- #24 — Profit Factor baseline tutarsızlığı düzeltmesi (2026-03-07)
- #25 — Stres testi bulguları: 6 düzeltme (2026-03-07)
- #26 — Sistem sağlığı: 8 sorun düzeltme (2026-03-07)
- #27 — OĞUL işlem yapmama: 7 engel düzeltmesi (2026-03-07)

---

## [5.1.0] — 2026-03-05 / 2026-03-06

### Added
- #1 — Yazılımsal SL/TP modu: software bazlı stop-loss/take-profit (2026-03-05)
- #4 — Açık Pozisyonlar "Tür" sütunu: backend tek kaynak (2026-03-05)
- #5 — İşlem geçmişi MT5 anlık sync (2026-03-05)
- #7 — Kapanma-tetiklemeli MT5 sync: event-driven mimari (2026-03-05)
- #8 — VİOP uzlaşma gecikmesi retry mekanizması (2026-03-05)
- #9 — v13.0: Top 5 kontrat seçimi ÜSTAT → OĞUL taşınması (2026-03-05)
- #10 — v13.0: ÜSTAT Brain tam implementasyon (2026-03-05)
- #11 — v13.0: BABA risk aksiyonu olay kaydı (2026-03-05)
- #12 — Sistem sağlık iyileştirmeleri (2026-03-05)

### Fixed
- #2 — Açık Pozisyonlar "İşlemi Kapat" butonu düzeltmesi (2026-03-05)
- #3 — mt5_bridge.py önceki düzeltmeler (2026-03-05)
- #6 — İşlem geçmişi yüklenme ve hata mesajları (2026-03-05)

---

> **Not:** Detaylı sorun analizleri, çözüm açıklamaları ve etkilenen dosya listeleri
> oturum raporlarında bulunur: `docs/YYYY-MM-DD_session_raporu_*.md`
>
> Eski detaylı tarihçe arşivi: `docs/USTAT_GELISIM_TARIHCESI_YEDEK.md`
