# ÜSTAT — Gelişim Tarihçesi (Changelog)

<!--
╔══════════════════════════════════════════════════════════════════════╗
║                   GELİŞİM TARİHÇESİ NASIL TUTULUR?                ║
╠══════════════════════════════════════════════════════════════════════╣
║                                                                      ║
║  Bu dosya projenin TÜM değişikliklerinin kronolojik kaydıdır.       ║
║  "Keep a Changelog" (keepachangelog.com) standardını takip eder.    ║
║                                                                      ║
║  KURALLAR:                                                           ║
║                                                                      ║
║  1. YAPI: Her versiyon bir başlık, altında kategoriler.             ║
║     Kategoriler: Added, Changed, Fixed, Removed, Security           ║
║     - Added    → Yeni eklenen özellik                               ║
║     - Changed  → Mevcut davranışta değişiklik                       ║
║     - Fixed    → Hata düzeltmesi                                    ║
║     - Removed  → Kaldırılan özellik/kod                             ║
║     - Security → Güvenlik düzeltmesi                                ║
║                                                                      ║
║  2. SIRALAMA: En yeni versiyon en üstte (ters kronolojik).         ║
║     Aynı versiyon içinde: en önemli değişiklik ilk sırada.         ║
║                                                                      ║
║  3. UZUNLUK: Her madde 1-2 satır. Detay gerekiyorsa oturum         ║
║     raporuna (docs/YYYY-MM-DD_session_raporu_*.md) referans ver.    ║
║     Changelog = "NE yapıldı", Oturum Raporu = "NASIL yapıldı".     ║
║                                                                      ║
║  4. NUMARA: Her madde #N formatında benzersiz sıra numarası alır.  ║
║     Numara HİÇBİR ZAMAN tekrar kullanılmaz veya atlanmaz.          ║
║                                                                      ║
║  5. REFERANS: Commit hash'i parantez içinde verilir.                ║
║     Örnek: #83 — Vade geçişi düzeltmesi (1d93ffd)                  ║
║                                                                      ║
║  6. DİL: Türkçe. Teknik terimler İngilizce kalabilir.              ║
║                                                                      ║
║  7. VERSİYON: Semantic Versioning (semver.org).                     ║
║     MAJOR.MINOR.PATCH — minor 9'dan sonra major artar.              ║
║     v5.8 → v5.9 → v6.0 → v6.1 ...                                  ║
║                                                                      ║
║  ÖRNEK MADDE:                                                        ║
║  - #83 — Vade günü Top 5 engeli: `<=` → `<` operatörü (1d93ffd)   ║
║                                                                      ║
║  YAPMA:                                                              ║
║  ✗ 50 satırlık detaylı analiz yazma (oturum raporuna koy)          ║
║  ✗ Aynı numarayı iki kez kullanma                                   ║
║  ✗ Numara atlama (#57'den #69'a geçme)                              ║
║  ✗ Kronolojik sırayı bozma                                          ║
║  ✗ "Sorun Analizi / Çözüm / Etkilenen Dosyalar" bölümleri ekleme  ║
║                                                                      ║
║  ESKİ DETAYLI VERSİYON:                                              ║
║  docs/USTAT_GELISIM_TARIHCESI_YEDEK.md (arşiv, referans için)   ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝
-->

---

## [6.0.0] — 2026-04-11 "ÜSTAT Plus V6.0"

### Fixed
- #185 — Widget Denetimi Bulgu H12 fix: Settings sayfası "Tema Ayarı" → Açık Tema kartı `disabled` state'ine alındı ve `applyTheme` fonksiyonu H12 guard ile koyu tema dışındaki tüm çağrıları sessizce reddediyor. **Kök neden:** `desktop/src/components/Settings.jsx` `applyTheme(newTheme)` fonksiyonu hiçbir whitelist kontrolü yapmadan `setTheme + localStorage.setItem('ustat_theme', newTheme) + documentElement.setAttribute/removeAttribute` zincirini çalıştırıyordu; `useState` initializer legacy `'light'` değerini olduğu gibi kabul ediyor, Açık Tema kartı `active` class'ı alıyor ve bir sonraki mount'ta `App.jsx` effect'i `data-theme="light"` attribute'u set ederek `theme.css :root[data-theme="light"]` değişkenlerini aktifleştiriyordu — fakat Dashboard, Monitor, Risk Yönetimi, Nabiz, TradeHistory gibi tüm bileşenler koyu tema için tasarlandığından açık modda kontrast kırılıyor, risk renk kodları (`--color-danger`, `--color-warning`) karmaşıklaşıyor ve kullanıcı gerçek para ile işlem yaparken yanıltıcı bir görsel deneyim oluşuyordu. Dosya başlığındaki `3. Tema Ayarı (şimdilik sadece koyu tema)` yorumu kodun gerçek durumunu yansıtmıyordu (açık tema aslında çağrılıyor ve persist ediliyordu). Audit Bölüm 12.3 + 16.3 H12 (Düşük kritiklik): "Settings → Tema: Açık tema CSS'te hazır ama tüm bileşenlerde doğrulanmamış — kullanıcı seçerse UI karmaşıklaşır, 'Koyu Tema aktif' yazısı yanıltıcı". **Çözüm (1 kod dosyası + 1 test, 0 Siyah Kapı dokunusu, 0 Kırmızı/Sarı Bölge dokunusu, 0 config değişikliği, 0 CSS değişikliği):** (1) `Settings.jsx` dosya başlığındaki yanıltıcı yorum dürüst bir açıklamayla değiştirildi: `3. Tema Ayarı (koyu tema aktif; açık tema CSS'te hazır ama tüm bileşenlerde doğrulanmadığı için disabled — Widget Denetimi H12)`. (2) `useState(theme)` initializer'ı legacy `'light'` kayıtlı kullanıcıyı sessizce `'dark'` ile override eder: `saved === 'light' ? 'dark' : (saved || 'dark')` — böylece eski kullanıcıların UI state'i hemen koyu temaya döner. (3) `applyTheme` fonksiyonu H12 guard eklendi: `if (newTheme !== 'dark') { return; }` — `'light'` veya başka bir değer çağrıldığında sessizce hiçbir şey yapmaz, ardından `setTheme('dark') + localStorage.setItem('ustat_theme', 'dark') + documentElement.removeAttribute('data-theme')` koyu temayı garanti altına alır. (4) JSX'te Açık Tema kartının `className`'i dinamik `${theme === 'light' ? 'active' : ''}` ifadesinden sabit `"st-theme-card disabled"` string'ine çevrildi (tema.css `:3751` civarında `.st-theme-card.disabled { opacity: 0.4; cursor: not-allowed; }` zaten hazırdı — CSS değişikliği gerekmedi); `onClick={() => applyTheme('light')}` tamamen kaldırıldı (karta tıklamak artık no-op); `title="Açık tema henüz tüm bileşenlerde doğrulanmadı — yalnız koyu tema aktif (Widget Denetimi H12)"` tooltip eklendi; `aria-disabled="true"` erişilebilirlik için eklendi; etiket `Açık Tema` → `Açık Tema (yakında)` olarak güncellendi (gelecekte tam doğrulandığında kartın yeniden etkinleştirileceği sinyali). (5) Kullanıcı deneyimi: Koyu Tema kartı eskiden olduğu gibi aktif ve tıklanabilir; Açık Tema kartı görünür (CSS groundwork hâlâ yerinde), soluk (opacity 0.4), imleç üstüne geldiğinde tooltip neden devre dışı olduğunu açıklıyor, tıklama no-op, legacy `'light'` kullanıcıları bir sonraki mount'ta sessizce koyu temaya alınıyor. **Neden kaldırma değil disable seçildi:** (a) Açık tema CSS değişkenleri (`:root[data-theme="light"]`) ve `.st-theme-card.disabled` class'ı `theme.css` içinde zaten mevcut — bunları silmek gelecekte re-enablement için ek iş demektir; (b) Kartı tamamen kaldırmak "bu özellik hiç yoktu" sinyali verir; disable + "(yakında)" etiketi "özellik geliyor, hazır değil" mesajı verir — daha dürüst bir UX; (c) Gelecekte tüm bileşenler doğrulandığında yalnız `disabled` class'ını + `applyTheme` guard'ını kaldırmak yeterli, JSX yapısı değişmez. **Neden App.jsx mount effect dokunulmadı:** `App.jsx` satır 41-46 `localStorage.getItem('ustat_theme') === 'light'` olursa `data-theme="light"` set ediyor; teorik olarak kaldırılabilirdi ama legacy `'light'` kayıtlı tarayıcılar artık `Settings.jsx::useState` initializer ile UI'da `'dark'` gösterdiği için bir sonraki Koyu Tema tıklamasında `applyTheme('dark')` zaten `removeAttribute('data-theme')` çağırıyor — scope'u tek dosyada tutmak geri alma zamanını kısaltır. **Neden config'e taşınmadı:** Açık tema "hazır değil" durumu bir kullanıcı ayarı değil, geliştirme süreci state'i — config'e koymak mantıksız olurdu. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_settings_light_theme_disabled` (Flow 4v) — 5 aşamalı kontrol: (a) `Settings.jsx` içinde `şimdilik sadece koyu tema` yanıltıcı yorumu YASAK (eski yorum geri gelirse test kırılır); (b) `Widget Denetimi H12` marker mevcut; (c) `applyTheme` guard `if (newTheme !== 'dark')` mevcut (silinirse açık tema tekrar persist olur); (d) Açık Tema kartı `className="st-theme-card disabled"` + `aria-disabled="true"` + `title="..."` + `Widget Denetimi H12` atfı mevcut; (e) `applyTheme('light')` çağrısı YASAK (kart tekrar tıklanabilir hale gelmesin). Pre-commit hook bu regression'ları bloklar. **Anayasa:** Siyah Kapı yok, Kırmızı/Sarı Bölge yok — `Settings.jsx` Yeşil Bölge, frontend-only davranış temizliği. Risk mantığı, kill-switch, trading akışları, BABA/OĞUL/H-Engine dokunulmadı. Config dokunulmadı. CSS dokunulmadı (`.st-theme-card.disabled` zaten vardı). Motor davranışı değişmedi — yalnızca UI tema seçimi gerçek bir seçim olmayacak şekilde netleştirildi. **Etki analizi:** 1 JSX dosyası + 1 test dosyası, ~25 satır net ekleme; çağıran zinciri: `Settings render → theme state (localStorage 'light' → 'dark' zorlaması) → JSX Koyu Tema active / Açık Tema disabled`. Tüketici zinciri: yalnız `Settings.jsx` etkilenir. **Dokunulmayanlar (bilinçli):** `desktop/src/styles/theme.css` (re-enablement için korundu); `App.jsx` satır 41-46 `data-theme` attribute yönetimi (scope dar tutuldu); `config/default.json` (Kırmızı Bölge + tema geliştirme state'i config konusu değil); BABA/OĞUL/risk modülleri (scope dışı). **Doğrulama:** critical_flows **55/55 yeşil** (54 baseline + yeni Flow 4v), 3.35s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.64s, 0 hata, dist/ üretildi: index.js 889.93 kB, index.css 90.52 kB). Bundle boyutu +0.09 kB. Piyasa kapalı (Pazar, Barış Zamanı). Deploy için `restart_app` yeterli. Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 12.3 + 16.3 H12. Oturum raporu: `docs/2026-04-11_session_raporu_H12_light_theme_disabled.md`. (desktop/src/components/Settings.jsx, tests/critical_flows/test_static_contracts.py)
- #184 — Widget Denetimi Bulgu H8 + H9 fix: NABIZ Sistem Monitörü eşik değerleri tek kaynağa (canonical) bağlandı — `desktop/src/components/Nabiz.jsx` içindeki `TABLE_THRESHOLDS` (15 tablo warn/danger seviyeleri), SummaryCard'daki hardcode karşılaştırmalar (`file_size_mb > 1000`, `total_size_mb > 2000`, `usage_pct > 90` vb.) ve `files.slice(0, 15)` log listesi kırpma sabiti tamamen kaldırıldı; canonical kaynak `api/routes/nabiz.py` modül seviyesinde üç yeni sabit (`NABIZ_TABLE_ROW_THRESHOLDS`, `NABIZ_SUMMARY_THRESHOLDS`, `NABIZ_LOG_FILES_DISPLAY_LIMIT`) olarak tanımlandı ve `/api/nabiz` response'una yeni `thresholds` alanı eklendi (`_build_thresholds_info()` helper üzerinden). **Kök neden:** `Nabiz.jsx` dosyası üç ayrı noktada hardcode eşiklerle doluydu — (1) satır 18-34 civarında `const TABLE_THRESHOLDS = { bars: {warn: 50000, danger: 150000}, trades: {...}, ... }` 15 tablonun warn/danger sınırlarını içeren dict; `getRowColor(name, count)` fonksiyonu bu dict'i okuyup tablo satırı için sarı/kırmızı renk seçiyordu. (2) SummaryCard render'ında inline magic pattern'ler: "Veritabanı" kartı `db.file_size_mb > 1000 ? COLORS.red : db.file_size_mb > 500 ? COLORS.yellow : COLORS.cyan`, "Loglar" kartı `logs.total_size_mb > 2000 ? ... > 500 ? ...`, "Disk" kartı `disk.usage_pct > 90 ? ... > 80 ? ...` — üç kart için 6 ayrı sihirli sayı (500, 1000, 500, 2000, 80, 90) kod içinde saçılmıştı. (3) `LogFilesPanel` render'ında `files.slice(0, 15)` hardcode kırpması — log listesi UI'de maksimum 15 dosyaya düşüyordu ama bu sayı sabit, config'den değil. Audit Bölüm 13.1 + 16.3 H8 (Orta kritiklik): "🧱 TABLE_THRESHOLDS (15 tablo) frontend hardcode — backend canonical kaynak olmalı"; Bölüm 13.2 + H9 (Düşük kritiklik): "🧱 `files.slice(0,15)` log listesi 15'e kırpılıyor — config'den okunmalı". **Çözüm (3 kod dosyası + 1 test, 0 Siyah Kapı dokunusu, 0 Kırmızı Bölge dokunusu — `config/default.json` DOKUNULMADI, 0 Sarı Bölge dokunusu):** (1) `api/routes/nabiz.py` — modül seviyesinde üç canonical sabit eklendi: `NABIZ_TABLE_ROW_THRESHOLDS: dict[str, dict[str, int]]` (15 VİOP tablosu için `{"warn": N, "danger": M}` mapping — bars, trades, risk_snapshots, events, top5_history, notifications, daily_risk_summary, weekly_top5_summary, config_history, manual_interventions, hybrid_positions, hybrid_events, strategies, liquidity_classes, app_state); `NABIZ_SUMMARY_THRESHOLDS: dict[str, float]` (6 eşik: `database_mb_warn/err` 500/1000, `log_mb_warn/err` 500/2000, `disk_pct_warn/err` 80/90); `NABIZ_LOG_FILES_DISPLAY_LIMIT: int = 15`. Üç sabitin üstünde 20+ satırlık detaylı yorum bloğu H8/H9 audit arka planını, canonical kaynak kararının nedenini (config'e taşımak yerine Yeşil Bölge backend modülü seçmek — çünkü bu eşikler "UI gösterim politikası / soft threshold", motor davranışını etkilemez; config Kırmızı Bölge olduğu için her değişiklik çift doğrulama gerektirir, bu eşikler UI ekibinin kontrolünde olmalı; pattern A-H3 `WATCHED_SYMBOLS` ile aynı), gelecek kullanıcı özelleştirme yolu (`config.nabiz.*` anahtarları merge ile) ve regresyon koruma test referansını (Flow 4u) açıklar. Yeni `_build_thresholds_info()` helper fonksiyonu üç sabiti tek bir dict'e paketler: `{"table_row_thresholds": {...}, "summary": {...}, "log_files_display_limit": 15, "source": "api.routes.nabiz"}`. `get_nabiz()` response'undaki dict'e yeni `"thresholds": _build_thresholds_info()` alanı eklendi (timestamp, database, logs, disk, retention, cleanup_conflict sonrası). (2) `desktop/src/components/Nabiz.jsx` — eski `const TABLE_THRESHOLDS = {...}` tamamen kaldırıldı, yerine `DEFAULT_TABLE_THRESHOLDS` (backend erişilemezse kullanılacak fallback, aynı 15 tablonun kopyası), `DEFAULT_SUMMARY_THRESHOLDS` ve `DEFAULT_LOG_FILES_DISPLAY_LIMIT = 15` fallback sabitleri eklendi; `DEFAULT_*` prefix'i kod okurken "bu hardcode değil, backend çalışmazsa devreye girecek güvenli default" kastını net ediyor. `getRowColor(table, count, tableThresholds)` imzası eklendi — üçüncü parametre opsiyonel, default `DEFAULT_TABLE_THRESHOLDS`. Yeni `pickSummaryStatus(value, warn, err)` saf fonksiyonu — değeri warn/err eşikleriyle karşılaştırıp `"ok" | "warn" | "err"` döner, SummaryCard render'ını inline magic pattern'den kurtarır. Ana bileşende threshold ekstraksiyonu: `const thresholds = data?.thresholds || {}`, `tableRowThresholds = thresholds.table_row_thresholds || DEFAULT_TABLE_THRESHOLDS`, `summaryThresholds = thresholds.summary || DEFAULT_SUMMARY_THRESHOLDS`, `logFilesDisplayLimit = Number.isFinite(thresholds.log_files_display_limit) ? thresholds.log_files_display_limit : DEFAULT_LOG_FILES_DISPLAY_LIMIT` (sayı tipinde değilse fallback'e düş). SummaryCard render öncesi `dbStatus = pickSummaryStatus(dbSizeMb, summaryThresholds.database_mb_warn, summaryThresholds.database_mb_err)`, `logStatus`, `diskStatus` hesaplanır; `diskColor = diskStatus === 'err' ? COLORS.red : diskStatus === 'warn' ? COLORS.yellow : COLORS.cyan`; üç SummaryCard bu durum değişkenlerini kullanır, inline `> 1000` / `> 500` / `> 2000` / `> 80` / `> 90` karşılaştırmaları tamamen kaldırıldı. `TableSizesPanel({ tables, tableRowThresholds })` imzası genişletildi ve `getRowColor(name, count, tableRowThresholds)` çağrısı threshold'u geçirir; `LogFilesPanel({ logs, displayLimit = DEFAULT_LOG_FILES_DISPLAY_LIMIT })` imzası genişletildi ve eski `files.slice(0, 15)` → `files.slice(0, displayLimit)` olarak güncellendi. Kullanıcı deneyimi: aynı 15 VİOP kontratı satırı, aynı renk kodları, aynı 15 log dosyası listesi — sadece eşikler artık backend'den dinamik akıyor, gelecekte (v6.1) yeni bir tablo veya yeni bir eşik seviyesi eklenmek istenirse sadece `api/routes/nabiz.py` modül sabitini değiştirmek yeterli, frontend rebuild gerekmez (restart_app ile endpoint yenilenir). **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_nabiz_thresholds_are_backend_driven` (Flow 4u) — 6 aşamalı zincir kontrolü: (a) `api/routes/nabiz.py` içinde üç canonical sabit (`NABIZ_TABLE_ROW_THRESHOLDS`, `NABIZ_SUMMARY_THRESHOLDS`, `NABIZ_LOG_FILES_DISPLAY_LIMIT`) + `def _build_thresholds_info` helper + response dict'te `"thresholds": _build_thresholds_info()` eşleştirmesi mevcut; (b) `NABIZ_TABLE_ROW_THRESHOLDS` dict gövdesi regex ile parse edilir, anahtar sayısı ≥10 (mevcut 15 tabloyu korur — gelecekte tabloları azaltmaya karşı regression); (c) `Nabiz.jsx` eski `const TABLE_THRESHOLDS = {` hardcode YASAK (regression koruma) + `DEFAULT_TABLE_THRESHOLDS` fallback mevcut; (d) `Nabiz.jsx` `data?.thresholds` consumption + `tableRowThresholds` state + `getRowColor(name, count, tableRowThresholds)` çağrı pattern'i mevcut; (e) `Nabiz.jsx` eski inline sihirli pattern'ler (`file_size_mb\s*>\s*1000`, `total_size_mb\s*>\s*2000`, `usage_pct\s*>\s*90`) YASAK + `pickSummaryStatus` helper + `summaryThresholds` ifadesi mevcut; (f) `Nabiz.jsx` eski `files.slice(0, 15)` hardcode YASAK + `files.slice(0, displayLimit)` dinamik kullanımı mevcut. Gelecekte refactor sırasında hardcode eşikler/limitler geri eklenirse pre-commit hook commit'i bloklar. **Neden config/default.json dokunulmadı:** Config Kırmızı Bölge (Anayasa Bölüm 4.1 #8) — her değişiklik çift doğrulama + kullanıcı açık onayı gerektirir. Bu eşikler UI-only "soft threshold" — BABA/OĞUL risk kararlarına etkisi yok, sadece NABIZ sayfasındaki renk/alarm görsel politikası. A-H3 (`WATCHED_SYMBOLS`) pattern'iyle aynı mantıkla canonical kaynak olarak Yeşil Bölge backend modülü (`api/routes/nabiz.py`) seçildi. Config'e taşımak "hangi kaynak doğru?" sorusunu doğurur (config vs kod); tek kaynak olarak kod + frontend fallback daha net. Gelecekte kullanıcı özelleştirmesi istenirse `config.nabiz.*` anahtarları eklenip helper'da merge edilebilir. **Anayasa:** Siyah Kapı yok — `get_nabiz` sadece SELECT/stat yapar, motor davranışını değiştirmez; Kırmızı Bölge yok — `config/default.json` DOKUNULMADI; Sarı Bölge yok. `api/routes/nabiz.py` + `desktop/src/components/Nabiz.jsx` + `tests/critical_flows/test_static_contracts.py` — üçü de Yeşil Bölge. Motor davranışı, kill-switch mantığı, risk hesaplamaları, retention — hiçbiri etkilenmedi. **Etki analizi:** 3 kod dosyası + 1 test dosyası, ~180 satır net ekleme; çağıran zinciri: `/api/nabiz → get_nabiz() → _build_thresholds_info() → NABIZ_TABLE_ROW_THRESHOLDS/SUMMARY/LOG_LIMIT → response.thresholds → Nabiz.jsx fetch → data.thresholds → {tableRowThresholds, summaryThresholds, logFilesDisplayLimit} → {TableSizesPanel, SummaryCard x3, LogFilesPanel}`. Tüketici zinciri: yalnız `Nabiz.jsx` etkilenir; Dashboard, Monitor, Settings, Risk Yönetimi — dokunulmadı. **Dokunulmayanlar (bilinçli):** `config/default.json` (Kırmızı Bölge — canonical yeri değil); `engine/database.py::get_table_sizes` (Kırmızı Bölge, zaten kaynak veriyi sağlıyor); `api/routes/nabiz.py::_build_database_info`, `_build_log_info`, `_build_disk_info`, `_build_retention_info`, `_build_cleanup_conflict_info` — hiçbiri değiştirilmedi, sadece `thresholds` alanı response'a eklendi; Nabiz.jsx'in diğer render bileşenleri (RetentionCard, CleanupConflictPanel) dokunulmadı. **Doğrulama:** critical_flows **54/54 yeşil** (53 baseline + yeni Flow 4u), 3.30s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.67s, 0 hata, dist/ üretildi: index.js 889.84 kB, index.css 90.52 kB). Bundle boyutu +0.38 kB (DEFAULT_* fallback sabitler + pickSummaryStatus helper + threshold ekstraksiyon mantığı). Piyasa kapalı (Pazar, Barış Zamanı). Deploy için `restart_app` yeterli. Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 13.1 + 13.2 + 16.3 H8 + H9. Oturum raporu: `docs/2026-04-11_session_raporu_H8_nabiz_thresholds_backend_canonical.md`. (api/routes/nabiz.py, desktop/src/components/Nabiz.jsx, tests/critical_flows/test_static_contracts.py)
- #183 — Widget Denetimi Bulgu H17 fix: `api/schemas.py::RiskResponse.graduated_lot_mult` ölü placeholder alanı kaldırıldı — BABA'nın graduated lot mantığı (3 ardışık kayıp → 0.75, 4 ardışık kayıp → 0.50, vb.) zaten `verdict.lot_multiplier` üzerinden `resp.lot_multiplier` alanına akıp `RiskManagement.jsx` "Lot Çarpanı" kartında görünüyor; `graduated_lot_mult` v5.1'de eklenmiş ama hiçbir üretici (`api/routes/risk.py` bu alana asla atama yapmıyordu) ve hiçbir tüketici (`RiskManagement.jsx` sadece `risk.lot_multiplier` okuyor) olmayan çift kaynak riski taşıyan ölü bir alandı. **Kök neden:** `api/schemas.py` satır 251'de `graduated_lot_mult: float = 1.0` Pydantic alanı vardı. Kaynak kodu araması (`grep -r graduated_lot_mult`) tüm projede yalnızca 3 referans buldu: (a) `api/schemas.py` alan tanımı, (b) `docs/2026-04-11_widget_denetimi.md` audit bulgu, (c) bu tarihçe girişi — yani üretici ve tüketici SIFIR. `engine/baba.py` graduated lot mantığı satır 1608'de `verdict.lot_multiplier = 0.50` (4 ardışık kayıp) ve satır 1698-1699'da `graduated = {1: 0.75, 2: 0.50}` dict'i ile çalışıyor; sonuç `verdict` objesine yazılır, `api/routes/risk.py` satır 141'de `resp.lot_multiplier = verdict.lot_multiplier` ile `RiskResponse`'a akar. `RiskManagement.jsx` satır 275-280 bu değeri "Lot Çarpanı" kartında `risk.lot_multiplier?.toFixed(2) ?? '—'` olarak görüntüler. Yani graduated lot bilgisi tamamen işliyordu, ama `graduated_lot_mult` placeholder alanı yanlış isim (verdict'te böyle bir alan yok) + yanlış değer (hep default `1.0`) ile "dead weight" olarak taşınıyordu. Audit Bölüm 16.3 H17 (Orta kritiklik): "H17 | Risk Yönetimi (11.5) | `graduated_lot_mult` RiskResponse'ta var ama frontend'de gösterilmiyor — config'den gelen graduated lot limiti UI'de eksik". Audit ipucu "frontend'de göster" önerisi yanıltıcıydı çünkü graduated lot zaten `lot_multiplier` alanı üzerinden gösteriliyor — iki ayrı alan tutmak çift kaynak riski yaratırdı. **Çözüm (1 kod dosyası + 1 test, 0 Siyah Kapı dokunusu, 0 Kırmızı/Sarı Bölge dokunusu, 0 config değişikliği, 0 backend route değişikliği, 0 frontend bileşen değişikliği):** `api/schemas.py::RiskResponse` içinden `graduated_lot_mult: float = 1.0` satırı kaldırıldı ve yerine 6 satırlık yorum bloğu geldi: "Eski `graduated_lot_mult: float = 1.0` v5.1'de placeholder olarak eklenmişti, hiçbir üretici tarafından populate edilmiyor, hiçbir frontend tüketicisi yok. BABA'nın graduated lot mantığı (0.75, 0.50, 0.25, vb.) zaten `lot_multiplier` alanına `verdict.lot_multiplier` üzerinden aktarılır ve RiskManagement.jsx'te 'Lot Çarpanı' kartında görünür. Dead field regression koruması: Flow 4t statik sözleşme testi". `lot_multiplier: float = 1.0` (satır 229) canonical alanı korundu — asıl graduated lot aktarım kanalı. `api/routes/risk.py::get_risk()` fonksiyonu dokunulmadı — zaten `resp.lot_multiplier = verdict.lot_multiplier` atamasını yapıyordu; bu değişiklikten sonra hiçbir regresyon mümkün değil. **Neden populate değil removal seçildi:** (1) `verdict.lot_multiplier` zaten graduated lot bilgisini taşıyor — paralel ikinci bir kanal eklemek "hangi alan doğru?" belirsizliği yaratır. (2) Audit ipucu "frontend'de göster" diyordu ama `RiskManagement.jsx` `risk.lot_multiplier`'ı zaten "Lot Çarpanı" kartında gösteriyordu — yani işlev zaten UI'da vardı, sadece schema'daki placeholder ölüydü. (3) Dead field bırakmak daha fazla bakım yükü (gelecekte "bu alan ne iş yapıyor?" sorusu tekrar doğar); temizlik daha dürüst çözüm. (4) CLAUDE.md Bölüm 3.2 "sihirli sayılar kullanmak YASAK" kuralı tam olarak bu tür "hep 1.0 döndüren placeholder" durumları için var. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_risk_response_has_no_dead_graduated_lot_mult` (Flow 4t) — 5 aşamalı kontrol: (a) `api/schemas.py` içinde `graduated_lot_mult:` alan tanımı YASAK (regex ile `^\s*graduated_lot_mult\s*:\s*` pattern regression koruma — gelecekte yeniden eklenirse test kırılır); (b) `lot_multiplier: float` canonical alanı mevcut (silinirse graduated lot aktarımı kırılır); (c) `api/routes/risk.py` içinde `resp.lot_multiplier = verdict.lot_multiplier` atama zinciri mevcut (kaldırılırsa graduated lot verdict'ten endpoint'e akmaz); (d) `RiskManagement.jsx` içinde `risk.lot_multiplier` consumption + "Lot Çarpanı" label mevcut (UI kartı silinirse kullanıcı graduated lot değerini göremez); (e) `RiskManagement.jsx` içinde `graduated_lot_mult` referansı YASAK (ölü alana UI bağımlılığı regresyon koruması). Gelecek refactor sırasında ölü alanın geri eklenmesi veya gerçek alanın silinmesi durumunda pre-commit hook commit'i bloklar. **Anayasa:** Siyah Kapı yok, Kırmızı/Sarı Bölge yok — `api/schemas.py` Yeşil Bölge; `api/routes/risk.py`, `desktop/src/components/RiskManagement.jsx`, `engine/baba.py` DOKUNULMADI. Config değişikliği yok. Motor davranışı değişmedi — yalnızca ölü şema alanı temizlendi. **Etki analizi:** 1 schema dosyası (dead field removal, +9 yorum / -1 alan) + 1 test dosyası (+50 satır Flow 4t); çağıran zinciri: `engine/baba.py::verdict.lot_multiplier → api/routes/risk.py::resp.lot_multiplier = verdict.lot_multiplier → RiskResponse → /api/risk → RiskManagement.jsx risk.lot_multiplier → "Lot Çarpanı" kartı`. Tüketici zinciri: Hiçbir tüketici etkilenmedi — dead field idi, kimse okumuyordu. **Dokunulmayanlar (bilinçli):** `engine/baba.py` graduated lot mantığı (Kırmızı Bölge + Siyah Kapı, scope dışı — zaten doğru çalışıyor); `api/routes/risk.py::get_risk` (Yeşil ama mevcut `resp.lot_multiplier` zinciri değiştirilmedi — regresyon riski sıfır); `RiskManagement.jsx` "Lot Çarpanı" kartı (zaten `risk.lot_multiplier` okuyor, çalışır durumda); config `graduated_loss_thresholds` veya benzeri anahtar EKLENMEDİ — BABA graduated lot dict'i (`{1: 0.75, 2: 0.50}`) kod içinde çünkü ardışık kayıp sayısı → çarpan mapping'i bir "policy" değil "safeguard", config'e çıkarmak saçma bir esneklik seviyesi olurdu (kullanıcı 3 ardışık kayıp sonrası lot'u 10'a çekebilir mi? → hayır, güvenlik nedenli). **Doğrulama:** critical_flows **53/53 yeşil** (52 baseline + yeni Flow 4t), 3.70s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.74s, 0 hata, dist/ üretildi: index.js 889.46 kB, index.css 90.52 kB). Şema değişikliği frontend bundle boyutunu etkilemedi — beklenen çünkü removal frontend'de hiç kullanılmıyordu. Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli. Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 11.5 + 16.3 H17. Oturum raporu: `docs/2026-04-11_session_raporu_H17_graduated_lot_mult_dead_field.md`. (api/schemas.py, tests/critical_flows/test_static_contracts.py)
- #182 — Widget Denetimi Bulgu H15 fix: Monitor sayfası BABA L1/L2/L3 kill-switch seviyelerinin yanıltıcı "kesin eşik" görünümü kaldırıldı — UI'de artık `~YAKLAŞIK` disclosure badge + her L1/L2/L3 kartında `~%{pct}` prefix + tooltip (gerçek eşik olmadığı açıkça belirtilmiş) görünür. **Kök neden:** `desktop/src/components/Monitor.jsx` satır 396-401'de `ksLevels` dizisi `limDaily * 0.5`, `limDaily * 0.75`, `limDaily * 1.0` illüstratif çarpanlarıyla hesaplanıyor ve kullanıcıya "L1 UYARI %0.9", "L2 DURDUR %1.35", "L3 KRİTİK %1.8" şeklinde gösteriliyordu. BABA'nın gerçek L1/L2/L3 kill-switch tetikleyicileri ise scalar eşik DEĞİL, event-driven: L1 — anomali/haber/kontrat engeli, L2 — günlük-aylık kayıp limiti + 3 ardışık kayıp + OLAY rejimi, L3 — hard drawdown ≥%15 + flash crash. Kod yorumu satır 396'da "yaklaşık hesaplama" diyordu ama UI'de hiçbir disclosure yoktu — kullanıcı bu yüzdeleri gerçek eşik sanıp "günlük kayıp %0.9'a ulaşırsa L1 UYARI tetiklenir" gibi yanlış mental model kurabiliyordu. Audit Bölüm 11.8 + 16.3 H15 (Orta kritiklik): "BABA L1/L2/L3 eşikleri UI'de 'yaklaşık' olarak hardcode ama 'yaklaşık' notu yok — kullanıcı gerçek eşik sanar". **Çözüm (1 kod dosyası + 1 test, 0 Siyah Kapı dokunusu, 0 Kırmızı/Sarı Bölge dokunusu, 0 config değişikliği, 0 backend dokunusu):** (1) `ksLevels` hesabı dokümante edilmiş yeni local sabit `KS_LEVEL_PCT_FRACTIONS = { L1: 0.5, L2: 0.75, L3: 1.0 }` üzerinden yapılır — inline 0.5/0.75 sihirli sayıları kaldırıldı; sabitin üzerinde 8 satırlık yorum bloğu BABA'nın gerçek L1/L2/L3 tetikleyicilerini açıklar ve çarpanların "illüstratif payda" olduğunu not eder. (2) Render header'ında `RİSK & KİLL-SWİTCH` başlığının yanına yeni `~YAKLAŞIK` badge eklendi — çok satırlı tooltip (title attribute) BABA'nın gerçek event-driven L1/L2/L3 tetikleyicilerini listeler ve gerçek değerler için Risk Yönetimi sayfasına yönlendirir. (3) Her ksLevel kartında yüzde değeri artık `~%{pct}` prefix'i ile render edilir (eskiden `%{pct}`) ve her kart `title="Yaklaşık görselleştirme — BABA'nın gerçek eşiği değil (H15)"` tooltip'i ile bezenmiştir. (4) Kullanıcı deneyimi: aynı görsel sütun layout'u (L1 UYARI, L2 DURDUR, L3 KRİTİK), aynı aktif/pasif renk kodlaması, aynı yüzde değerleri — sadece `~` prefix + YAKLAŞIK badge + tooltip eklendi, çıktı hiçbir gerçek koruma mantığını değiştirmez. **Neden config'e taşınmadı:** Çarpanlar BABA'daki gerçek eşikler DEĞİL, frontend-only görsel aidi — config'e koymak ikinci bir "yalan eşik" kaynağı yaratır. Dürüst çözüm: UI'de illüstratif olduğunu açıkça belirtmek + local sabite taşıyarak okunabilirliği artırmak. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_monitor_kill_switch_levels_are_disclosed_as_approximate` (Flow 4s) — 4 aşamalı kontrol: (a) `KS_LEVEL_PCT_FRACTIONS` sabiti + `.L1`, `.L2`, `.L3` üzerinden okuma mevcut (inline 0.5/0.75 sihirli sayıların geri gelmesini engeller); (b) Render header'ında `~YAKLAŞIK` disclosure badge string'i mevcut (silinirse test kırılır); (c) ksLevel kartında `~%{pct}` prefix mevcut (eski `%{pct}` render pattern'inin geri gelmesini engeller); (d) Tooltip içinde `gerçek eşik` / `gerçek eşiği` açıklaması mevcut. Gelecekte refactor sırasında bu disclosure'lardan biri silinirse pre-commit hook commit'i bloklar. **Anayasa:** Siyah Kapı yok, Kırmızı/Sarı Bölge yok — `Monitor.jsx` Yeşil Bölge, frontend-only değişiklik. Backend route'lar, BABA kill-switch mantığı, `_activate_kill_switch`, risk state'i — hiçbiri dokunulmadı. Motor davranışı korundu, yalnızca UI dürüstlük katmanı eklendi. **Etki analizi:** 1 JSX dosyası + 1 test dosyası, ~25 satır net ekleme; çağıran zinciri: `Monitor render → ksLevels (KS_LEVEL_PCT_FRACTIONS üzerinden) → JSX kartlar (~%{pct} + tooltip) + header (~YAKLAŞIK badge + tooltip)`. Tüketici zinciri: yalnız `Monitor.jsx` etkilenir; Dashboard, Settings, Risk Yönetimi, TopBar — dokunulmadı. **Dokunulmayanlar (bilinçli):** BABA `_activate_kill_switch`, `_check_hard_drawdown`, `_check_monthly_loss` — Kırmızı Bölge + Siyah Kapı, scope dışı. `config.engine.kill_switch_multipliers` gibi yeni config anahtarı eklenmedi (yukarıda gerekçelendirildi). Drawdown bar render'ı (sonraki blok) dokunulmadı — o değerler `risk?.max_daily_loss` / `risk?.max_weekly_loss` üzerinden gerçek backend kaynağından zaten geliyor. **Doğrulama:** critical_flows **52/52 yeşil** (51 baseline + yeni Flow 4s), 3.18s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.68s, 0 hata, dist/ üretildi: index.js 889.46 kB, index.css 90.52 kB). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli. Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 11.8 + 16.3 H15. Oturum raporu: `docs/2026-04-11_session_raporu_H15_monitor_kill_switch_disclosure.md`. (desktop/src/components/Monitor.jsx, tests/critical_flows/test_static_contracts.py)
- #181 — Widget Denetimi Bulgu H4 fix: ManualTrade lot input hardcoded `min=1 max=10 step=1` kaldırıldı — canonical kaynak `config/default.json.engine.max_lot_per_contract` (1.0) artık `/api/settings/trading-limits` endpoint'i üzerinden UI lot input'una akıyor; kullanıcı eskiden 10 lot girebiliyor ama `engine/manuel_motor.py::MAX_LOT_PER_CONTRACT` silent truncation ile 1.0'a kırpıyordu (yanıltıcı UX). Yeni `TradingLimitsResponse` (`lot_min`, `lot_max`, `lot_step`, `source`) + `_read_trading_limits` helper (runtime `Config()` import, fallback default 1.0/1.0/1.0) + `getTradingLimits` services/api.js fonksiyonu + ManualTrade `lotLimits` state'i (`useEffect` mount fetch, cancelled flag cleanup, mevcut `lot` sınırlar dışındaysa içine çekme) + Flow 4r statik sözleşme testi (6 aşamalı zincir: config → schema → route → service → component → JSX literal regex koruması). Test: critical_flows **51/51 yeşil**, 3.25s. Build: `ustat-desktop@6.0.0`, 728 modül, 2.65s, index.js 888.72 kB. Siyah Kapı yok, Kırmızı/Sarı Bölge yok — 4 Yeşil Bölge dosyası + 1 test. Config dokunulmadı (canonical zaten oradaydı). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 4.1 + 16.3 H4 (Yüksek kritiklik). Oturum raporu: `docs/2026-04-11_session_raporu_H4_manual_trade_lot_limits.md`. (api/schemas.py, api/routes/settings.py, desktop/src/services/api.js, desktop/src/components/ManualTrade.jsx, tests/critical_flows/test_static_contracts.py)
- #180 — Widget Denetimi Bulgu A-H3 fix: ManualTrade `SYMBOLS` hardcode drift riski ortadan kaldırıldı — izlenen 15 VİOP kontratı artık `engine/mt5_bridge.py::WATCHED_SYMBOLS` canonical kaynaktan `/api/settings/watchlist` endpoint'i üzerinden frontend'e akıyor (C1 — Yeşil Bölge `api/schemas.py` + `api/routes/settings.py` + `desktop/src/services/api.js` + `desktop/src/components/ManualTrade.jsx` + 1 test, Siyah Kapı yok, Kırmızı Bölge yok). **Kök neden:** `ManualTrade.jsx` satır 21'de `const SYMBOLS = [...]` hardcode 15 element'li dizi vardı ve satır 177 `{SYMBOLS.map((s) => <option>...)}` ile dropdown'a bağlanıyordu. `engine/mt5_bridge.py::WATCHED_SYMBOLS` listesi backend canonical kaynak — `data_pipeline`, `baba.py`, `ogul.py`, `backtest.py` tümü oradan okur. İki liste elle senkronlanması gerekiyordu: yeni kontrat eklendiğinde (örn. 16. kontrat F_YENI) geliştirici hem `WATCHED_SYMBOLS` hem `ManualTrade.jsx::SYMBOLS` hem `services/api.js::getRisk` fallback hem `services/api.js::getWatchlistSymbols` fallback güncellemeliydi — unutulursa UI ile backend'in izlediği semboller arasında drift oluşurdu, kullanıcı dropdown'dan desteklenmeyen sembol seçemezdi. Audit Bölüm 4.1 + 16.3 H3: "H3 | Manuel İşlem (4.1) | `SYMBOLS` sabit dizisi (15 kontrat) `config/default.json.watchlist.symbols` ile senkron değil" (Yüksek kritiklik). **Çözüm (5 dosya + 1 test, 0 Siyah Kapı dokunusu, 0 Kırmızı Bölge dokunusu, 0 config değişikliği):** (1) `api/schemas.py` — yeni `WatchlistResponse` Pydantic modeli eklendi (`symbols: list[str] = []`, `source: str = "bridge"`), gelecekte config tabanlı watchlist'lere (örn. `config.watchlist.symbols` kullanıcı override) genişletilebilir şekilde tasarlandı. (2) `api/routes/settings.py` — `DEFAULT_WATCHLIST_SYMBOLS` fallback sabiti (15 VİOP kontratı — import başarısız olursa UI kırılmasın) + `_read_watchlist_symbols()` helper (runtime'da `from engine.mt5_bridge import WATCHED_SYMBOLS as BRIDGE_SYMBOLS` import eder, try/except ile hata yakalar, tip+boş kontrol + string temizlik + sessizce default'a düşme) + `GET /settings/watchlist` endpoint'i helper'dan `(symbols, source)` alır ve `WatchlistResponse` döndürür. Runtime import kullanıldı çünkü global import (top-level) circular import riski + bridge modülün test ortamında MetaTrader5 bağımlılığıyla yüklenmemesi durumunda endpoint'in de çalışmaması sorununa yol açardı. (3) `desktop/src/services/api.js::getWatchlistSymbols` async export fonksiyonu `client.get('/settings/watchlist')` çağırır, hata durumunda 15 sembolluk fallback objesi + `source: 'error'` döner — dropdown asla boş gösterilmez. (4) `desktop/src/components/ManualTrade.jsx` — eski `const SYMBOLS = [...]` KALDIRILDI, yerine `DEFAULT_SYMBOLS` (fallback yorum eklendi); `getWatchlistSymbols` import eklendi; yeni `watchlist` state'i (`useState(DEFAULT_SYMBOLS)` — ilk render'da hardcode default ile); yeni mount `useEffect` `getWatchlistSymbols()` çağırır, başarılı olursa `setWatchlist(resp.symbols)` + mevcut `symbol` state'i yeni listede yoksa ilk sembole çeker (dropdown'un seçili değeri geçerli kalır); `SYMBOLS.map` → `watchlist.map` olarak değiştirildi. `cancelled` flag ile cleanup — unmount sonrası state update warning'i engelleyici. (5) Kullanıcı ekranında davranış aynı — dropdown hala 15 sembollu gösterir, BUY/SELL form çalışır, risk kontrolü değişmez. Yeni kontrat eklendiğinde sadece `engine/mt5_bridge.py::WATCHED_SYMBOLS` güncellenir; UI otomatik senkronize olur (restart sonrası). **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_manual_trade_watchlist_single_source` (Flow 4q) — 6 aşamalı zincir kontrolü: (a) `engine/mt5_bridge.py::WATCHED_SYMBOLS: list[str]` tanımı var + ≥10 VİOP kontratı; (b) `api/schemas.py::WatchlistResponse` Pydantic sınıfı + `symbols: list[str]` alanı; (c) `api/routes/settings.py::_read_watchlist_symbols` helper + `from engine.mt5_bridge import WATCHED_SYMBOLS` runtime import + `@router.get("/settings/watchlist"` route + `WatchlistResponse` kullanımı; (d) `services/api.js::getWatchlistSymbols` export + `/settings/watchlist` endpoint çağrısı + fallback objesinde `symbols:` alanı; (e) `ManualTrade.jsx` eski `const SYMBOLS = [` hardcode YASAK (regression koruma) + `getWatchlistSymbols` import + `const [watchlist, setWatchlist]` state; (f) `ManualTrade.jsx` JSX'te `watchlist.map` kullanımı + `SYMBOLS.map` YASAK. Gelecekte refactor sırasında hardcode SYMBOLS dizisi geri eklenirse pre-commit hook commit'i bloklar. **Anayasa:** Siyah Kapı yok, Kırmızı/Sarı Bölge yok — `api/schemas.py` + `api/routes/settings.py` + 1 frontend + `services/api.js` + 1 test Yeşil Bölge. Config dokunulmadı (engine/mt5_bridge canonical). Startup performansı etkilenmedi — endpoint runtime import kullanır, lifespan sırası değişmez. **Etki analizi:** 5 kod dosyası + 1 test dosyası, ~100 satır net ekleme; Siyah Kapı fonksiyonu dokunulmadı; çağıran zinciri: `ManualTrade mount → getWatchlistSymbols() → /settings/watchlist → _read_watchlist_symbols() → WATCHED_SYMBOLS (bridge import) → WatchlistResponse → state → JSX`. Tüketici zinciri: yalnız ManualTrade.jsx etkilenir; `api/routes/manual_trade.py`, `api/routes/hybrid_trade.py`, diğer trading route'lar dokunulmadı. **Dokunulmayanlar (bilinçli):** `config/default.json::watchlist.symbols` bloğu EKLENMEDİ — audit ipucu "config'den okunmalı" diyordu ama canonical kaynak zaten backend kodundaki `WATCHED_SYMBOLS` listesi. Config'e ikinci bir kaynak eklemek daha fazla drift riski yaratır (hangisi "doğru"?). Canonical olarak bridge'i seçmek tek source of truth principle'a daha uygun. Gelecekte kullanıcı özelleştirmesi istenirse `config.watchlist.user_overrides` olarak eklenip `_read_watchlist_symbols` helper'da merge edilebilir. `ManualTrade.jsx::DEFAULT_SYMBOLS` fallback bırakıldı — backend import başarısız olursa UI kırılmaz. `manual_trade.py` route'u dokunulmadı — emir kontrolü ve yürütme mantığı aynı. **Doğrulama:** critical_flows **50/50 yeşil** (49 baseline + yeni Flow 4q), 3.20s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.65s, 0 hata, dist/ üretildi: index.js 888.05 kB, index.css 90.52 kB). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (hem backend route hem Electron bundle yenilenir). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 4.1 + 16.3 H3. (api/schemas.py, api/routes/settings.py, desktop/src/services/api.js, desktop/src/components/ManualTrade.jsx, tests/critical_flows/test_static_contracts.py)
- #179 — Widget Denetimi Bulgu A5 (H1) fix: Versiyon sabiti tekilleştirildi — `TopBar.jsx`, `LockScreen.jsx`, `Settings.jsx` eskiden kendi hardcode `V6.0` / `const VERSION = '6.0.0'` stringlerine sahipti; artık tek kaynak `engine/__init__.py::VERSION` ve `/api/status.version` üzerinden frontend'e akıyor (C1 — Yeşil Bölge `api/routes/status.py` + 3 frontend bileşen + `services/api.js` fallback + 1 test). **Kök neden:** Audit Bölüm 2.1 + 16.3 H1: "🧱 Versiyon 'V6.0' hardcoded — `engine/__init__.py::VERSION` tek kaynaktan okunmalı". Üç ayrı UI bileşeninde üç ayrı hardcode vardı: `TopBar.jsx` satır `<h1>ÜSTAT Plus <span className="version">V6.0</span></h1>`, `Settings.jsx` satır 11 `const VERSION = '6.0.0'`, `LockScreen.jsx` satır `<h1>ÜSTAT Plus <span className="version">V6.0</span></h1>`. Yeni major/minor release (v6.1, v6.2, v7.0) yapıldığında geliştiricinin bu üç dosyayı da manuel güncellemesi gerekiyordu — unutulursa UI tutarsızlığı oluşurdu (TopBar V6.0 gösterir, Settings v6.1.0 gösterir). CLAUDE.md Bölüm 7 ADIM 4 "Versiyon Güncelleme Noktaları" listesinde bu üç dosya zaten vardı, ama DRY ihlali olduğu için audit çıktı. **Çözüm (5 kod dosyası + 1 test, 0 Siyah Kapı dokunusu, 0 backend schema değişikliği):** (1) `api/routes/status.py` — `from engine import VERSION as ENGINE_VERSION` import eklendi; `get_status()` return'deki `StatusResponse(version=ENGINE_VERSION, ...)` atama eklendi (daha önce schema default `"6.0.0"` string'ine düşüyordu, artık runtime'da `engine/__init__.py::VERSION`'ı okur). `StatusResponse.version` alanı zaten `schemas.py` satır 19'da mevcuttu, yeni schema yok. (2) `desktop/src/services/api.js::getStatus` fallback objesine `version: '6.0.0'` eklendi — backend erişilemezse dahi TopBar/Settings/LockScreen display kırılmaz. (3) `desktop/src/components/TopBar.jsx` — hardcode `V6.0` KALDIRILDI; yeni `const versionLabel = (status.version || '6.0.0').split('.').slice(0, 2).join('.')` hesaplaması eklendi ve `<h1>ÜSTAT Plus <span className="version">V{versionLabel}</span></h1>` kullanımı; `/api/status` polling zaten 2sn'de bir çalıştığı için yeni fetch eklenmedi, mevcut `status` state'inden okur. (4) `desktop/src/components/Settings.jsx` — `const VERSION = '6.0.0'` KALDIRILDI, yerine `const VERSION_FALLBACK = '6.0.0'` fallback sabiti geldi; iki ayrı FieldRow `ÜSTAT Plus V{(status?.version || VERSION_FALLBACK).split('.').slice(0, 2).join('.')} Desktop` + `v{status?.version || VERSION_FALLBACK}` formatında güncellendi; Settings.jsx zaten `status` state'i tutuyordu (satır 73), yeni fetch gerekmedi. (5) `desktop/src/components/LockScreen.jsx` — hardcode `V6.0` KALDIRILDI; `getStatus` import eklendi; `const VERSION_FALLBACK = '6.0.0'` + yeni `appVersion` state'i (`useState(VERSION_FALLBACK)`); yeni `useEffect` mount'ta `getStatus()` çağırıp `s.version` varsa `setAppVersion(s.version)` çağırır (LockScreen pre-auth olduğu için kendi fetch'i gerekiyor — TopBar'ın status polling'ine ulaşmaz); hata durumunda `VERSION_FALLBACK` state'te zaten mevcut, try/catch sessizce düşer; `<h1>ÜSTAT Plus <span className="version">V{appVersion.split('.').slice(0, 2).join('.')}</span></h1>` kullanımı. **Display format korundu:** Full semver (`6.0.0`) yerine `V6.0` formatı için `.split('.').slice(0, 2).join('.')` pattern'i kullanıldı — minimum görsel değişiklik, tek kaynak principle sağlandı. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_version_single_source_of_truth` (Flow 4p) — 6 aşamalı zincir kontrolü: (a) `engine/__init__.py::VERSION` semver (`X.Y.Z`) formatında; (b) `api/routes/status.py` `from engine import VERSION` ve `version=ENGINE_VERSION` atama mevcut; (c) `services/api.js::getStatus` fallback objesinde `version:` alanı mevcut; (d) `TopBar.jsx` eski `<span className="version">V6.0</span>` hardcode YASAK + `status.version` ifadesi mevcut (regression koruma); (e) `Settings.jsx` eski `const VERSION = '6.0.0'` hardcode YASAK + `status?.version` ifadesi mevcut; (f) `LockScreen.jsx` eski `<span className="version">V6.0</span>` hardcode YASAK + `appVersion` state + `getStatus` import mevcut. **Anayasa:** Siyah Kapı yok, Kırmızı/Sarı Bölge yok (`status.py` route Yeşil, Pydantic schema alanı zaten vardı); backend + 3 frontend + 1 test Yeşil Bölge. Tek atomik değişiklik — versiyon yayılım zinciri kuruldu. **Etki analizi:** 6 dosya, ~50 satır net değişiklik; Siyah Kapı fonksiyonu dokunulmadı; çağıran zinciri: `engine/__init__.py::VERSION → api/routes/status.py → StatusResponse → /api/status → getStatus() → {TopBar polling | Settings mount | LockScreen mount} → JSX`. Kullanıcı deneyimi: davranış aynı — `V6.0` görünüyor; v6.1 release'inde sadece `engine/__init__.py::VERSION = "6.1.0"` değiştirilmesi yeterli, TopBar/Settings/LockScreen otomatik güncellenir. CLAUDE.md Bölüm 7 ADIM 4 listesinde `TopBar.jsx` / `LockScreen.jsx` / `Settings.jsx` artık manuel değil; `engine/__init__.py` + `config/default.json` + `api/server.py` + `api/schemas.py` + `desktop/package.json` + `desktop/main.js` (splash) hâlâ manuel güncelleme gerektirir. **Doğrulama:** critical_flows **49/49 yeşil** (48 baseline + yeni Flow 4p), 3.66s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.70s, 0 hata, dist/ üretildi: index.js 887.41 kB, index.css 90.52 kB). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (hem backend route hem Electron bundle yenilenir — hotpath polling frekansı değişmedi). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 2.1 + 16.3 H1 + 17 A5. (api/routes/status.py, desktop/src/services/api.js, desktop/src/components/TopBar.jsx, desktop/src/components/Settings.jsx, desktop/src/components/LockScreen.jsx, tests/critical_flows/test_static_contracts.py)
- #178 — Widget Denetimi Bulgu A18 (H2) fix: Dashboard "Açık Pozisyonlar" rozetindeki `/5` hardcode'u `config/default.json::risk.max_open_positions` zincirine bağlandı (C1 — Yeşil Bölge `desktop/src/components/Dashboard.jsx` + `desktop/src/services/api.js`, tek rozet satırı + state + fetch). **Kök neden:** `Dashboard.jsx` satır 671'de `{(livePositions || []).length} / 5` hardcode pattern'i vardı. `max_open_positions` (eşzamanlı maks açık pozisyon sayısı) zaten `config/default.json::risk.max_open_positions = 5` üzerinden backend'e biliniyordu ve `api/routes/risk.py::get_risk()` RiskResponse'a atama yapıyordu (satır 119 `resp.max_open_positions = rp.max_open_positions` / satır 132 config fallback); ancak Dashboard bu endpoint'i hiç çağırmıyordu. Kullanıcı config'den `max_open_positions`'ı 3'e veya 7'ye çekse bile UI rozetinde hâlâ `n / 5` görünüyor, BABA'nın gerçekten kullandığı limit ile UI arasında kavramsal kopukluk oluşuyordu. Audit B16 + H2: "🧱 Hardcode — config'den `max_open_positions` okunmalı" + "H2 | Dashboard (3.3) | `1 / 5` pozisyon sayacı — `max_open_positions` config'den değil, sabit ‘/5’" (Orta kritiklik). **Çözüm (2 dosya + 1 test):** (1) `desktop/src/services/api.js::getRisk` fallback objesine `max_open_positions: 5` eklendi — hata durumunda bile Dashboard rozetinin `n / X` formatı kırılmaz, koruma değeri garantili. (2) `desktop/src/components/Dashboard.jsx` — `getRisk` import'a eklendi; yeni `riskState` state'i (`_dashCache?.riskState ?? null` cache'li); `fetchAll` Promise.all tuple'ına `getRisk()` çağrısı eklendi (`rk` değişkeni) ve `setRiskState(rk || null)` state'e yazıldı; `_dashCache` objesi `riskState` alanını içerecek şekilde genişletildi; rozet satırı `{(livePositions || []).length} / 5` → `{(livePositions || []).length} / {riskState?.max_open_positions ?? 5}` olarak güncellendi (geriye dönük fallback 5 mevcut — ilk render'da veya REST hata durumunda). **Backend dokunulmadı:** `/api/risk` endpoint'i zaten `max_open_positions` döndürüyordu (`api/routes/risk.py` satır 119/132) — sadece frontend tüketicisi eklendi. Yeni API, yeni schema, yeni route YOK. **Anayasa:** Siyah Kapı yok, Kırmızı/Sarı Bölge yok, backend dokunulmadı. `Dashboard.jsx` + `services/api.js` Yeşil Bölge. Tek atomik değişiklik — iki dosyada state + rozet senkronu. **Dokunulmayanlar:** `ManualTrade.jsx` lot min/max hardcode (A16 ayrı bulgu), `NewsPanel` SYMBOLS listesi (B8 ayrı bulgu), Dashboard diğer "/5" referansları yok (`daily_trade_count` için ayrı bir bulgu, Performance/Monitor max_daily_trades ayrı). **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_dashboard_max_open_positions_from_config` (Flow 4o) — 5 aşamalı zincir kontrolü: (a) `config/default.json::risk.max_open_positions` var + pozitif int; (b) `api/schemas.py::RiskResponse.max_open_positions` alanı mevcut; (c) `api/routes/risk.py` `resp.max_open_positions` atama yapıyor; (d) `services/api.js::getRisk` export var + fallback objesinde `max_open_positions` mevcut (regex ile fonksiyon gövdesi kontrolü); (e) `Dashboard.jsx` eski `|| []).length} / 5\n` hardcode pattern'i YOK (regression koruma) + `getRisk` import + `riskState` state + `setRiskState` setter + `riskState?.max_open_positions` ifadesi mevcut. Gelecek refactor sırasında hardcode geri eklenirse pre-commit hook commit'i bloklar. **Etki analizi:** 2 kod dosyası + 1 test dosyası, ~20 satır net ekleme; Siyah Kapı fonksiyonu dokunulmadı; çağıran zinciri: `Dashboard mount → fetchAll → getRisk() → /api/risk → _read (risk.py) → config.get("risk.max_open_positions") → RiskResponse → state → JSX`. Kullanıcı deneyimi: rozet artık canlı config değerini yansıtıyor — admin `max_open_positions`'ı 3'e çekse restart sonrası Dashboard `n / 3` gösterir. Hata durumunda `?? 5` fallback devrede, rozet asla `n / undefined` göstermez. **Doğrulama:** critical_flows **48/48 yeşil** (47 baseline + yeni Flow 4o), 3.08s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.59s, 0 hata, dist/ üretildi: index.js 886.99 kB, index.css 90.52 kB). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (sadece Electron bundle yenilenir, backend etkilenmez). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 3.3 (B16), Bölüm 16.3 H2, Bölüm 17 A18. (desktop/src/components/Dashboard.jsx, desktop/src/services/api.js, tests/critical_flows/test_static_contracts.py)
- #177 — Widget Denetimi Bulgu A19 (H5) fix: `KILL_HOLD_DURATION` hardcode'u config/default.json zincirine taşındı (C3 — `config/default.json` Kırmızı Bölge sadece yeni `ui.kill_hold_ms` anahtarı eklendi, mevcut risk/strateji/eşik sabiti DOKUNULMADI; `api/schemas.py::UiPrefsResponse`; `api/routes/settings.py::_read_ui_prefs_from_config` helper + `/settings/ui-prefs` endpoint; `desktop/src/services/api.js::getUiPrefs` + fallback; `desktop/src/components/SideNav.jsx` eski `const KILL_HOLD_DURATION = 2000` KALDIRILDI, yerine `DEFAULT_KILL_HOLD_MS` fallback + `killHoldMs` state + `useEffect` mount fetch). **Kök neden:** `SideNav.jsx` satır 29'da `const KILL_HOLD_DURATION = 2000` hardcode idi ve iki yerde kullanılıyordu: (1) satır 55 `setInterval` progress animasyonu `elapsed / KILL_HOLD_DURATION`, (2) satır 70 `setTimeout` tetikleme süresi. Kritik koruma parametresi — kullanıcının yanlışlıkla kill-switch tetiklemesini engelleyen çift aşamalı koruma (basılı tutma + progress animasyonu) için minimum süre. Config'den okunmadığı için admin bu değeri değiştirmek istese (örn. çok hızlı tetiklenme şüphesi → 3000 ms'e çıkar, veya stres testi → 500 ms'e düşür) kod düzenleyip rebuild etmesi gerekiyordu. Audit Bölüm 15.1 + 15.4: "🧱 KILL_HOLD_DURATION hardcode: Kritik bir koruma parametresi config'de olmalı" + Bölüm 16.3 H5: "SideNav (15.2) `KILL_HOLD_DURATION=2000ms` sabit — kritik koruma parametresi config'de olmalı" (Orta kritiklik). **Atomik değişiklikler (5 dosya + 1 test, 0 Siyah Kapı dokunusu):** (1) `config/default.json` — mevcut `ui` bloğuna `"kill_hold_ms": 2000` alanı eklendi (`notification_prefs` yanında). Kırmızı Bölge #8 dokunusu ama yalnız UI namespace'inde yeni alan; risk/strateji/eşik/baseline/drawdown parametresi **değişmedi**. (2) `api/schemas.py::UiPrefsResponse` Pydantic modeli eklendi (`kill_hold_ms: int = 2000`, `source: str = "config"`), gelecek UI-layer sabitlerini (animation speed, auto-refresh interval) de aynı endpoint altında toplamak için tasarlandı. (3) `api/routes/settings.py` — `DEFAULT_UI_PREFS` fallback sabiti + `_read_ui_prefs_from_config()` helper (engine.config'den oku, `isinstance(raw, int) and not bool and 500 <= raw <= 10000` tip+aralık doğrulaması, geçersiz değer sessizce fallback'e düşer — UI kırılmaz; try/except ile exception yakalama) + `GET /settings/ui-prefs` endpoint'i helper'dan okur, `(merged, source)` tuple'ını `UiPrefsResponse`'a çevirir. (4) `desktop/src/services/api.js::getUiPrefs` async export fonksiyonu `client.get('/settings/ui-prefs')` çağırır, hata durumunda `{kill_hold_ms: 2000, source: "error"}` döner (kill-switch koruması asla kırılmaz). (5) `desktop/src/components/SideNav.jsx` — eski `const KILL_HOLD_DURATION = 2000` KALDIRILDI, yerine: `DEFAULT_KILL_HOLD_MS = 2000` fallback sabiti (config okunamazsa kullanılır, Anayasa kritik koruma yorumu eklendi); `useState(DEFAULT_KILL_HOLD_MS)` yeni `killHoldMs` state'i; `useEffect` mount'ta `getUiPrefs()` çağırıp `val = Number(prefs?.kill_hold_ms)` + `Number.isFinite(val) && 500 <= val <= 10000` güvenlik kontrolü ile state'i günceller (cancelled flag ile cleanup); `setInterval` progress animasyonu `elapsed / killHoldMs` kullanır, `setTimeout` tetikleme süresi `killHoldMs` değişkeni alır; `handleKillDown` useCallback dependency'sine `killHoldMs` eklendi ki değer yenilendiğinde callback yeniden oluşturulur. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_sidenav_kill_hold_ms_from_config` (Flow 4n) — 6 aşamalı zincir kontrolü: (a) `config/default.json::ui.kill_hold_ms` var + `isinstance(int) and 500 <= val <= 10000`; (b) `api/schemas.py::UiPrefsResponse` Pydantic sınıfı var + `kill_hold_ms` + `source` alanları mevcut; (c) `api/routes/settings.py::_read_ui_prefs_from_config` helper fonksiyonu var + `/settings/ui-prefs` route kaydı var + `config.get("ui.kill_hold_ms"` çağırır (backend senkronu kopmasın); (d) `services/api.js::getUiPrefs` export var + `/settings/ui-prefs` endpoint çağrısı atar; (e) `SideNav.jsx` eski `const KILL_HOLD_DURATION = 2000` hardcode YASAK (regression koruma) + `getUiPrefs` import/kullanımı var; (f) `killHoldMs` state + `setKillHoldMs` setter + `elapsed / killHoldMs` ifadesi mevcut (setInterval hala dinamik değere bağlı). Gelecekte refactor sırasında hardcode geri eklenirse pre-commit hook commit'i bloklar. **Dokunulmayanlar (bilinçli):** Mevcut `ui.notification_prefs` endpoint'i (`/settings/notification-prefs`) dokunulmadı — ayrı bir yazma API'si var ve S1 fix'inde tamamlandı; `setInterval(..., 16)` 60fps tick dokunulmadı (audit "Kritik değil, sabit kalabilir" kararı); `activateKillSwitch('operator')` operatör kimliği sabit string (audit H16 ayrı bulgu, A19 kapsamı dışı); backend `api/routes/killswitch.py` dokunulmadı — fiziksel kill-switch aktivasyonu aynı, yalnızca frontend tetikleme gecikmesi config'e bağlandı. **Etki analizi:** 5 dosya + 1 test, ~80 satır net ekleme; Siyah Kapı fonksiyonu dokunulmadı; çağıran zinciri: `SideNav mount` → `getUiPrefs()` → `/settings/ui-prefs` → `_read_ui_prefs_from_config()` → `config.get("ui.kill_hold_ms")` → state → `setInterval`/`setTimeout`; tüketici zinciri geriye dönük uyumlu (yeni endpoint, mevcut konsümerler etkilenmez). Kullanıcı deneyimi: davranış aynı (2 saniye basılı tutma), yalnız admin config değiştirip restart yaparak süreyi ayarlayabiliyor. **Doğrulama:** critical_flows **47/47 yeşil** (46 baseline + 1 yeni Flow 4n), 3.16s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.74s, 0 hata, dist/ üretildi: index.js 886.82 kB, index.css 90.52 kB). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (hem backend route hem Electron bundle yenilenir). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 15.1 + 15.4 + 16.3 H5 + 17 A19. (config/default.json, api/schemas.py, api/routes/settings.py, desktop/src/services/api.js, desktop/src/components/SideNav.jsx, tests/critical_flows/test_static_contracts.py)
- #176 — Widget Denetimi Bulgu A10 (B16) fix: TopBar "Günlük K/Z (MT5)" etiketi "Günlük K/Z (Snapshot)" olarak düzeltildi (C1 — Yeşil Bölge `desktop/src/components/TopBar.jsx`, 3 satır etiket + 3 satır yorum). **Kök neden:** TopBar metrik alanı `account.daily_pnl` değerini `/api/account` endpoint'inden alıyor; ancak backend `api/routes/account.py::get_account` (satır 32-37) bu değeri `db.get_latest_risk_snapshot()["daily_pnl"]` üzerinden risk_snapshots DB tablosundan okuyor, doğrudan MT5 hesabından DEĞİL. TopBar.jsx satır 5 dosya başı yorumunda "Günlük K/Z (MT5, 2sn)", satır 245 title tooltip'inde "MT5 günlük P/L (gerçek zamanlı)", satır 246 metric label'inde "Günlük K/Z (MT5)" — üç noktada da "MT5" etiketi kullanıcıya veri kaynağı olarak MT5 terminalini işaret ediyordu, oysa gerçek kaynak backend'in DB tablosundaki snapshot'ı. Dashboard'daki aynı değer WebSocket `liveEquity.daily_pnl` üzerinden daha hızlı geliyor; TopBar REST 2 sn polling. İki akış da aynı risk_snapshots kaynağına bağlı ama etiket farklı kaynak ima ediyordu. Audit B16: "🐛 Yanlış etiket." **Çözüm:** TopBar.jsx'teki üç nokta güncellendi — (1) Satır 5 header yorumu: "Günlük K/Z (MT5, 2sn)" → "Günlük K/Z (Snapshot, 2sn)". (2) Satır 245 div title tooltip'i: "MT5 günlük P/L (gerçek zamanlı)" → "Günlük P/L — risk_snapshots tablosundan (2 sn polling)". (3) Satır 246 metric label: "Günlük K/Z (MT5)" → "Günlük K/Z (Snapshot)". Ek olarak 3 satır yorum eklendi ki gelecek refactor hangi dosyadan okuduğunu anında görsün: `api/routes/account.py::get_account → db.get_latest_risk_snapshot()`. Mantık/çağrı/veri akışı aynı, yalnız etiket doğru kaynağı söylüyor. **Anayasa:** Siyah Kapı yok, Kırmızı/Sarı Bölge yok, backend dokunulmadı. `TopBar.jsx` Yeşil Bölge, tek bileşen, üç string + yorum değişikliği. **Test:** `tests/critical_flows/test_static_contracts.py::test_topbar_daily_pnl_label_not_mt5` (Flow 4m) eklendi — 3 aşama: (1) `"Günlük K/Z (MT5)"` label hardcode dosyada YOK (regression koruma), (2) `"Günlük K/Z (Snapshot)"` yeni label dosyada mevcut, (3) header yorumu `"Günlük K/Z (MT5, 2sn)"` kaldırılmış. Birisi refactor sırasında eski etiketi geri ekleyemez. 46/46 kritik akış testi yeşil (3.08s), Windows `npm run build` 728 modül 2.59s 0 hata, index.js 886.43 kB. (desktop/src/components/TopBar.jsx, tests/critical_flows/test_static_contracts.py)
- #175 — Widget Denetimi Bulgu B11 fix: Monitor Flow Diagram ÜSTAT ModBox `MT5` detail satırı gerçek `mt5Connected` state'ine bağlandı (C1 — Yeşil Bölge `desktop/src/components/Monitor.jsx`, tek satır + yorum). **Kök neden:** Monitor bileşeninde `mt5Connected = status?.mt5_connected ?? false` zaten satır 265'te hesaplanıyor ve [B] bölümünde GATEWAY panelinde doğru kullanılıyordu (`'BAĞLANTI', mt5Connected ? '✓ CANLI' : '✗ KOPUK'`). Ancak aynı dosyanın [C] Flow Diagram ÜSTAT geniş ModBox'ında satır 766'daki detail satırı sabit şekilde `['MT5', 'BAĞLANTI YOK', '#556680']` olarak hardcode'lanmıştı — MT5 bağlı olsa bile kullanıcı Monitor ekranında her zaman gri 'BAĞLANTI YOK' görüyordu. Audit B11'in tam olarak işaret ettiği durum. **Çözüm:** Aynı dosyadaki [B] GATEWAY paneli için zaten geçerli olan pattern uygulandı: `['MT5', mt5Connected ? '✓ BAĞLI' : '✗ KOPUK', mt5Connected ? '#2ecc71' : '#e74c3c']`. Böylece etiket, değer ve renk MT5 heartbeat durumuna göre canlı olarak güncelleniyor. **Anayasa:** Tek dosya, tek satır içerik değişikliği + 3 satır yorum. Siyah Kapı yok, Kırmızı/Sarı Bölge yok, backend dokunulmadı. `Monitor.jsx` Yeşil Bölge. **Test:** `tests/critical_flows/test_static_contracts.py::test_monitor_mt5_panel_uses_dynamic_state` (Flow 4l) eklendi — 3 aşama: (1) `const mt5Connected` state'i hâlâ hesaplanıyor, (2) `['MT5', 'BAĞLANTI YOK'` hardcode pattern'i dosyada YOK (regression koruma), (3) `['MT5', mt5Connected` dinamik pattern'i dosyada mevcut. 45/45 kritik akış testi yeşil (3.15s), Windows `npm run build` 728 modül 2.70s 0 hata.
- #174 — Widget Denetimi Bulgu A14 (B17) fix: `TRADE_ERROR` ve `MANUAL_TRADE_ERROR` event tipleri Hata Takip panelinde doğru kategoriye (`emir`) eşlendi (C1 — Yeşil Bölge `engine/error_tracker.py::ERROR_CATEGORIES` ve `api/routes/error_dashboard.py::EVENT_TYPE_CATEGORY` — iki dict'e toplam 4 anahtar eklendi, mevcut anahtarlar dokunulmadı). **Kök neden:** `ogul.py::_execute_signal` send_order başarısızlığında ve SL yerleştirilemeyip kapatma da başarısız olduğunda (korumasız pozisyon orphan) `TRADE_ERROR` event'i emit ediyor; `manuel_motor.py::execute_manual_trade` MT5 reject durumunda `MANUAL_TRADE_ERROR` emit ediyor. İki event tipi de engine/error_tracker.py::ERROR_CATEGORIES ve api/routes/error_dashboard.py::EVENT_TYPE_CATEGORY sözlüklerinde yoktu; `_categorize()` default'u iki tarafta sırasıyla 'diğer' ve 'sistem' döndürüyordu. Sonuç: Hata Takip panelinde kullanıcı 'emir' kategori filtresine bastığında canlı TRADE_ERROR kayıtları görünmüyor, 'sistem' altında karışık şekilde duruyordu. Audit 'canlı 9x miskategorize' olarak kayıt almış. **Çözüm:** Her iki dict'e `"TRADE_ERROR": "emir"` ve `"MANUAL_TRADE_ERROR": "emir"` eklendi; yorum satırıyla hangi dosyanın bu tipleri emit ettiği belgelendi. **Anayasa:** Siyah Kapı fonksiyonlarına dokunulmadı — değişiklik sadece iki dict içerik genişletmesi. ogul.py ve manuel_motor.py emit noktaları aynen korundu. error_tracker.py kategori yardımcı dosyası Kırmızı/Sarı Bölge dışı Yeşil Bölge. **Test:** `tests/critical_flows/test_static_contracts.py::test_trade_error_category_mapping_consistent` (Flow 4k) eklendi — 4 aşama: (1) ERROR_CATEGORIES 'TRADE_ERROR'+'MANUAL_TRADE_ERROR'→'emir', (2) EVENT_TYPE_CATEGORY aynı anahtarlar→'emir', (3) iki sözlük arası parite kontrolü, (4) ogul.py ve manuel_motor.py'nin gerçekten bu tipleri emit ettiği static source check. 44/44 kritik akış testi yeşil (3.13s). Frontend dokunulmadığı için build atlandı.
- #173 — Widget Denetimi Bulgu A7 (B21) fix: `STATS_BASELINE` ile `risk.baseline_date` aynı kaynaktan okunur hale getirildi (C3 — `config/default.json` Kırmızı Bölge sadece yeni `risk.stats_baseline_date` eklenerek, mevcut risk/strateji sabitleri dokunulmadan; `api/constants.py` Yeşil Bölge `get_stats_baseline()` helper + `STATS_BASELINE` fallback; `api/schemas.py` `StatsBaselineResponse`; `api/routes/settings.py` yeni `/settings/stats-baseline` endpoint — iki baseline + source meta; `api/routes/performance.py` ve `api/routes/trades.py` helper üzerinden okur; `desktop/src/services/api.js` `getStatsBaseline()` + eski sabit fallback; `desktop/src/components/Performance.jsx` ve `TradeHistory.jsx` mount'ta baseline çeker ve `İstatistik tabanı / Risk tabanı` etiketi gösterir). **Kök neden:** audit B21 — Dashboard/Performance/TradeHistory istatistikleri `api/constants.py::STATS_BASELINE` (hardcoded `2026-02-01`) üzerinden çalışıyordu; BABA peak_equity/drawdown ise `config.risk.baseline_date` (`2026-04-01 00:01`) üzerinden ayrı bir kaynaktan okuyordu. İki kavram UI'de ayrıştırılmadığı ve Settings'ten değiştirilemediği için kullanıcı hangi baseline aktif olduğunu göremiyordu. **Çözüm:** istatistik baseline'ı config'e `risk.stats_baseline_date` olarak taşındı, geri uyum için eski sabit fallback olarak korundu; yeni `/settings/stats-baseline` endpoint her iki baseline'ı tek payload'da source bilgisiyle döndürüyor; frontend bileşenleri bu endpoint'ten çekiyor ve küçük bir etikette iki tarihi ayrı ayrı gösteriyor. **Anayasa:** Siyah Kapı fonksiyonlarına (BABA peak_equity/drawdown mantığı, `update_risk_baseline` POST) dokunulmadı — sadece okuma zinciri birleştirildi. **Test:** `tests/critical_flows/test_static_contracts.py::test_stats_baseline_single_source_chain` (Flow 4j, 9 aşama) eklendi, 43/43 kritik akış testi yeşil; Windows `npm run build` 728 modül 2.65s 0 hata.
- #172 — Widget Denetimi Bulgu A17 (H6+H10) fix: BIST VİOP seans saatleri config/api/frontend zincirine taşındı (C3 — `config/default.json` Kırmızı Bölge sadece yeni `session` bloğu eklendi, risk/strateji/eşik sabiti DOKUNULMADI; backend `api/schemas.py` + `api/routes/settings.py` yeni endpoint; frontend `ErrorTracker.jsx` + `Performance.jsx` + `services/api.js`). **Kök neden:** `ErrorTracker.jsx` EOD geri sayım paneli ve `Performance.jsx` saat-bazlı heatmap'i hardcoded saat sabitleri kullanıyordu: ErrorTracker satır 61-67'de `EOD_CLOSE_HOUR = 17`, `EOD_CLOSE_MIN = 45`, `VIOP_OPEN_HOUR = 9`, `VIOP_OPEN_MIN = 30`, `VIOP_CLOSE_HOUR = 18`, `VIOP_CLOSE_MIN = 15` literal sabitleri; Performance satır 232'de `for (let h = 9; h <= 18; h++) { hours[h] = { hour: h, count: 0, pnl: 0, wins: 0 }; }` literal aralık. Backend `config/default.json::engine.trading_close = "17:45"` (EOD zorunlu kapanış saati, Anayasa Kural #5) ile frontend arasında **bütün senkronizasyon halkası yoktu** — engine.trading_close değişse bile ErrorTracker'daki EOD geri sayımı eski saatte tetiklenmeye devam eder, Performance heatmap'i BIST seans saatleri dışına taşınsa bile eski 9-18 aralığında çizmeye devam ederdi. Audit raporu 11.4 + 17 A17 (H6+H10): "ErrorTracker.jsx içindeki hardcoded saat sabitleri config/default.json.session altından okunsun; aynı şey Performans · Heatmap'teki 9-18 için". **Atomik değişiklikler (6 dosya + 1 test, 0 Siyah Kapı dokunusu):** (1) `config/default.json` — yeni üst seviye `"session": { "market_open": "09:30", "market_close": "18:15", "eod_close": "17:45" }` bloğu eklendi. Kırmızı Bölge #8 dokunusu ama risk/strateji/eşik/baseline/drawdown parametresi **DEĞİŞMEDİ**, yalnız UI'a session hours namespace'i açıldı; engine'in kendi `trading_open`/`trading_close` değerleri (09:45/17:45) dokunulmadı — engine buffered trading window'u kendi ayrı namespace'inde kalır, session namespace'i salt-UI referans. (2) `api/schemas.py` — yeni `SessionHoursResponse` Pydantic modeli (4 alan: `market_open`, `market_close`, `eod_close` str "HH:MM" + `source` config/default/error), default değerleri BIST VİOP sabitleri. (3) `api/routes/settings.py` — `DEFAULT_SESSION_HOURS` fallback dict + `_read_session_hours_from_config()` helper (engine.config'den oku, eksik key'leri default ile merge et, HH:MM regex doğrulaması `^\d{2}:\d{2}$` — geçersiz değer sessizce default'a düşer) + `GET /settings/session` endpoint'i helper'dan okur, `(merged, source)` tuple döner ve `SessionHoursResponse`'a çevirir. (4) `desktop/src/services/api.js` — yeni `getSession()` async fonksiyonu `client.get('/settings/session')` çağırır, hata durumunda BIST default'ları + `source: 'error'` fallback döner (UI asla kırılmaz). (5) `desktop/src/components/ErrorTracker.jsx` — eski 6 hardcoded `EOD_CLOSE_*`/`VIOP_*` sabit KALDIRILDI, yerine `DEFAULT_SESSION_HOURS = { market_open: '09:30', market_close: '18:15', eod_close: '17:45' }` ve `parseHHMM(str)` helper (geçerli HH:MM → `{hour, min}`, geçersiz → null); `getEodInfo(sessionCfg = DEFAULT_SESSION_HOURS)` parametre aldı (önceki sabitleri okumak yerine `sessionCfg.market_open/market_close/eod_close`'u parse eder, parse başarısızsa `DEFAULT_SESSION_HOURS`'a düşer); `useState(DEFAULT_SESSION_HOURS)` yeni `sessionHours` state'i + `useEffect` mount'ta `getSession()` çağırıp state'i güncellediğinde `setEod(getEodInfo(next))` ile ilk değeri de yeniler (cancelled flag ile cleanup); dakikalık `setInterval` artık `getEodInfo(sessionHours)` kullanır ve `sessionHours` dependency'si ile yeniden kurulur. (6) `desktop/src/components/Performance.jsx` — yeni üst seviye `DEFAULT_HEATMAP_HOURS = { market_open: '09:30', market_close: '18:15' }` sabiti ve `parseHour(str)` helper (HH:MM → integer saat kısmı, geçersiz null); `useState(DEFAULT_HEATMAP_HOURS)` yeni `heatmapHours` state'i + `useEffect` mount'ta `getSession()` çağırıp state'i günceller; `hourData` useMemo bloğu eski `for (let h = 9; h <= 18; h++)` hardcoded literal'i KALDIRDI, yerine `openHour = parseHour(heatmapHours.market_open) ?? parseHour(DEFAULT_HEATMAP_HOURS.market_open)` + `closeHour = parseHour(heatmapHours.market_close) ?? parseHour(DEFAULT_HEATMAP_HOURS.market_close)` + `for (let h = openHour; h <= closeHour; h++)`; useMemo dependency array'ine `heatmapHours` eklendi ki session fetch tamamlandığında heatmap otomatik yeniden hesaplanır. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_session_hours_from_config_to_frontend` (Flow 4i) — 6 aşamalı kontrol: (a) `config/default.json::session` bloğu var ve 3 anahtar (market_open, market_close, eod_close) string tipte; (b) `api/schemas.py::SessionHoursResponse` sınıfı var, `market_open`/`market_close`/`eod_close: str` alanları mevcut; (c) `api/routes/settings.py::get_session_hours` endpoint fonksiyonu var, `/settings/session` route kaydı var, `config.get("session"` çağırır (backend senkronu kaybı engelli); (d) `services/api.js::getSession` export var ve `'/settings/session'` endpoint'ine çağrı atar; (e) `ErrorTracker.jsx` `getSession` import eder + `DEFAULT_SESSION_HOURS` fallback var + eski 6 hardcoded sabit literal'i YASAK (`const EOD_CLOSE_HOUR = 17`, `const VIOP_CLOSE_MIN = 15`, vb.); (f) `Performance.jsx` `getSession` import eder + `heatmapHours` state'i var + eski hardcoded `for (let h = 9; h <= 18; h++)` literal'i YASAK. Gelecekte refactor sırasında hardcode geri eklenirse pre-commit hook commit'i bloklar. **Dokunulmayanlar (bilinçli):** `engine/baba.py::run_cycle` ve `engine/ogul.py::_check_end_of_day` EOD kontrolleri (Siyah Kapı #10/#13) dokunulmadı — backend zaten `self.config.get("engine.trading_close")` kullanıyor, frontend değişikliği backend davranışını etkilemez; `engine.trading_open`/`engine.trading_close` namespace'i dokunulmadı — engine buffered trading window (09:45 başlangıç, session.market_open 09:30'dan 30dk buffer) ayrı semantik; `api/routes/settings.py::_risk_baseline` ve `_notification_prefs` helper'ları dokunulmadı — session endpoint ayrı bir alan; `Dashboard.jsx` ve diğer bileşenlerde hardcoded saat referansı taraması yapıldı, başka kullanım bulunmadı. **Etki analizi:** 6 dosya (1 config + 2 backend + 3 frontend) + 1 test, ~140 satır net ekleme; Siyah Kapı/Kırmızı Bölge fonksiyonu dokunulmadı (yalnız config/default.json'a yeni namespace eklendi, mevcut değerler değişmedi); çağıran zinciri: `getSession()` → `ErrorTracker.sessionHours` state → `getEodInfo(sessionHours)` → EOD geri sayım label; `getSession()` → `Performance.heatmapHours` state → `hourData` useMemo → BarChart render; tüketici zinciri geriye dönük uyumlu (mevcut API konsümerleri yeni endpoint'i görmüyor, yeni state'ler yalnız ilgili bileşen içinde). **Doğrulama:** critical_flows **42/42 yeşil** (41 baseline + 1 yeni Flow 4i), 2.77s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.64s, 0 hata, dist/ üretildi: index.js 884.01 kB). İlk build `getEodInfo(hours = ...)` parametre adı ErrorTracker içindeki lokal `const hours = Math.floor(remaining / 60)` ile collision yaşadı → parametre `sessionCfg` olarak yeniden adlandırıldı, ikinci build temiz geçti. Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (hem backend route hem Electron bundle yenilenir). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 11.4 + Bölüm 17 A17 (H6+H10). (config/default.json, api/schemas.py, api/routes/settings.py, desktop/src/services/api.js, desktop/src/components/ErrorTracker.jsx, desktop/src/components/Performance.jsx, tests/critical_flows/test_static_contracts.py)
- #171 — Widget Denetimi Bulgu A9 (B10+H14) fix: Monitor Performans paneli eşikleri backend döngü bütçesine hizalandı (C1 — Yeşil Bölge `desktop/src/components/Monitor.jsx`, tek sabit + tek bileşen + 7 referans güncellemesi). **Kök neden:** Monitor.jsx'teki `ResponseBar` grid'i ve DÖNGÜ SÜRESİ StatCard eşikleri mikro-benchmark literal'leriydi: BABA/ÜSTAT/H-Engine `max=50`, OĞUL/VERİ GÜNCELLEME `max=100`, TOPLAM DÖNGÜ `max=300`, StatCard `cycleAvg > 50 ? red : teal`, DÖNGÜ İSTATİSTİK MAX `> 100ms ? warn`. Ancak backend `config/default.json::engine.cycle_interval = 10` saniye — döngü bütçesi **10000ms**. Canlı ortamda DataPipeline ~2596ms, toplam döngü ~2739ms ölçülüyor; bu değerler bütçenin %26-%27'sidir ama UI literal'leri karşısında %2596 / %913 oranları çıkıyor, `Math.min(pct, 100)` ile clamp'lendiği için **tüm çubuklar daima dolu** ve StatCard sürekli kırmızı gösteriyordu. Sonuç: performans paneli hiçbir zaman "normal" durumu yansıtmıyordu — kullanıcı "döngü sürekli aşıyor" algısı alıyor, gerçekte backend `overrun_count = 0` idi (çünkü backend doğru 10sn bütçesini kontrol ediyor). Frontend/backend arasında **bütçe senkronsuzluğu**. **Atomik değişiklik (1 dosya + 1 test):** (1) `Monitor.jsx` tepesine `CYCLE_INTERVAL_MS = 10000` sabiti eklendi (POLL_MS yanına, backend `engine.cycle_interval` değeri ile senkron; Flow 4h testi sabit değerini 10000'de kilitler). (2) `ResponseBar` bileşeni genişletildi: yeni `barColor = pct > 70 ? '#e74c3c' : pct > 30 ? '#f39c12' : color` mantığı — yük yüzdesi %70'i aştığında kırmızı, %30'u aştığında turuncu, altında temel renk. Etiket rengi de aynı mantığa bağlandı. (3) 6 `ResponseBar` max prop'u (BABA DÖNGÜ, OĞUL SİNYAL, ÜSTAT BEYİN, H-ENGINE, VERİ GÜNCELLEME, TOPLAM DÖNGÜ) hardcoded 50/100/300 yerine `CYCLE_INTERVAL_MS` ile değiştirildi — her bar artık "10sn bütçesinin payı" olarak okunur. (4) DÖNGÜ SÜRESİ StatCard color: `cycleAvg > CYCLE_INTERVAL_MS * 0.9 ? red : > * 0.5 ? orange : teal`; sub etiketi "ortalama döngü" → "bütçe 10000ms" olarak backend değerini gösterecek şekilde güncellendi. (5) DÖNGÜ İSTATİSTİK MAX renk eşiği: `(cycle?.max_ms ?? 0) > CYCLE_INTERVAL_MS * 0.9 ? red : > * 0.5 ? orange : '#8ab0d0'` — üç aşamalı. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_monitor_response_bars_use_cycle_interval_budget` — 6 aşamalı kontrol: (a) `CYCLE_INTERVAL_MS = 10000` sabit tanımı var; (b) eski literal max prop'lar (`max={50}`, `max={100}`, `max={300}`) YASAK — geri eklenirse commit bloklanır; (c) `max={CYCLE_INTERVAL_MS}` en az 6 kez kullanılır (6 ResponseBar); (d) `pct > 70` / `pct > 30` yük renk eşikleri mevcut; (e) eski `cycleAvg > 50 ?` StatCard hardcode YASAK, `cycleAvg > CYCLE_INTERVAL_MS` mantığı var; (f) eski `> 100 ? '#f39c12'` MAX hardcode YASAK, `cycle?.max_ms ?? 0) > CYCLE_INTERVAL_MS` mantığı var. **Dokunulmayanlar (bilinçli):** Backend `config/default.json::engine.cycle_interval` DEĞİŞMEDİ (Kırmızı Bölge #8 — risk/strateji parametresi değil ama yine de dokunulmadı, yalnız frontend senkronize edildi); `engine/main.py::_run_single_cycle` döngü zamanlaması dokunulmadı (Siyah Kapı #22); Dashboard'daki POLL_MS = 10000 ayrı bir sabittir (API poll interval, döngü bütçesi değil) — bilinçli ayrılık. **Etki analizi:** 1 dosya (desktop, Yeşil Bölge) + 1 test, ~20 satır net JSX (1 sabit + 3 satır renk mantığı + 6 max prop değişikliği + 2 eşik güncellemesi) + ~60 satır test; Siyah Kapı/Kırmızı Bölge/Sarı Bölge dokunusu YOK; çağıran zinciri yok (CYCLE_INTERVAL_MS yerel sabit), tüketici zinciri dahili (ResponseBar prop'u, StatCard color prop'u). **Doğrulama:** critical_flows **41/41 yeşil** (40 baseline + 1 yeni Flow 4h), 2.57s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.45s, 0 hata, dist/ üretildi: index.js 882.52 kB). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (Electron bundle yeniden yüklenir). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 11.4 + Bölüm 17 A9 (B10+H14). (desktop/src/components/Monitor.jsx, tests/critical_flows/test_static_contracts.py)
- #170 — Widget Denetimi Bulgu B25 fix: Monitor modül hata sayacı yapısal event.type classifier'ına dayandırıldı (C1 — Yeşil Bölge `desktop/src/components/Monitor.jsx`, tek helper + tek hesaplama bloğu). **Kök neden:** `Monitor.jsx::errorCounts` hesabı eski kodda `(ev.message || '').toLowerCase().includes('baba')` stilinde serbest metin substring kontrolü kullanıyordu. İki kritik kırılganlık: (1) Canlı üretimde DB'den gelen ERROR/CRITICAL event type'ları (`KILL_SWITCH` 25, `DRAWDOWN_LIMIT` 12, `TRADE_ERROR` 9, `SYSTEM_STOP`, `RISK_LIMIT`, `DAILY_LOSS_STOP`) mesaj metninde `baba`/`ogul`/`manuel`/`hengine`/`ustat` kelimelerini İÇERMİYOR. Örnek mesajlar: "Kill-switch L2 aktif: daily_loss", "Günlük kayıp limiti aşıldı", "Toplam drawdown: %30.43", "Emir başarısız: F_HALKB BUY 1.0 lot". Substring fallback **hiçbir üretim eventi ile eşleşmiyordu** — `errorCounts` daima {0,0,0,0,0} kalıyor, Monitor "HATA 0 bugün" gösterdiği halde DB'de 48 kritik event vardı. Tam ölü kod. (2) `String.prototype.toLowerCase()` Türkçe locale'e duyarsız; Unicode 'ğ'/'İ' karakterlerinde inconsistent eşleşir — 'Oğul'/'Üstat' içeren mesajlar yanlış modüle atanabilirdi. Ek olarak else-if zinciri ilk eşleşme kazandığı için baba en başta tanımlıydı → "baba hatası sırasında OĞUL sinyali reddedildi" tamamen baba'ya atanırdı. **Atomik değişiklik (1 dosya + 1 test):** (1) `Monitor.jsx` üstüne `classifyEventModule(ev)` helper fonksiyonu eklendi. İki aşamalı: (a) `MODULE_TYPE_PREFIX` tablosu — üretimde kullanılan event type prefix'leri yapısal olarak eşleşir (hengine: `HYBRID_`; manuel: `MANUAL_|MANUEL_`; baba: `KILL_SWITCH|DRAWDOWN_|RISK_|COOLDOWN_|REGIME_|BABA_|HARD_DRAWDOWN|DAILY_LOSS_STOP|MONTHLY_LOSS|WEEKLY_RESET|DAILY_RESET|LOT_HALVED|STOP_TRADING|SYSTEM_STOP|SYSTEM_HALT`; ogul: `TRADE_|ORDER_|SIGNAL_|OGUL_|VOLUME_SPIKE|PARTIAL_|FAKE_|SIGN_MISMATCH|EOD_|PAPER_TRADE`; ustat: `USTAT_|PATTERN_|STRATEGY_|TOP5_`). (b) Prefix eşleşmezse `MODULE_MSG_WORD` tablosu ile mesaj üzerinde `toLocaleLowerCase('tr-TR')` + word-boundary regex (`\b...\b` + `/u` flag) fallback çalışır: `\bh[-_ ]?engine\b|\bhibrit\b|\bhybrid\b`, `\bmanue?l\b`, `\bbaba\b`, `\bo[gğ]ul\b`, `\b[üu]stat\b`. (c) Sınıflandırılamayan event sayılmaz (false-positive yerine sessizlik; audit sonucunda yakalanamayan type'lar Kritik Akış testinde not edilir). (2) `errorCounts` hesap bloğu `(health?.recent_events || []).forEach(ev => { if (ev.severity === 'ERROR' || ev.severity === 'CRITICAL') { const mod = classifyEventModule(ev); if (mod) errorCounts[mod]++; } })` şeklinde sadeleştirildi — else-if zinciri kaldırıldı, öncelik sırası yapısal. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_monitor_error_counts_uses_structured_classifier` — Monitor.jsx'i text olarak okur ve 5 aşamalı kontrol yapar: (a) `function classifyEventModule` tanımı var mı; (b) eski `msg.includes('baba')` ve `toLowerCase().includes(` kalıpları YASAK (false positive, gelecek refactor'da geri eklenirse pre-commit hook'u bloklar); (c) `toLocaleLowerCase('tr-TR')` kullanımı var; (d) üretimde görülen 9 canonical type prefix (`KILL_SWITCH`, `DRAWDOWN_`, `TRADE_`, `SYSTEM_STOP`, `DAILY_LOSS_STOP`, `HYBRID_`, `MANUAL_`, `MANUEL_`, `USTAT_`) MODULE_TYPE_PREFIX haritasında mevcut; (e) word-boundary `\bbaba\b` regex fallback yerinde. İlk denemede test false-positive verdi (Monitor.jsx yorumu eski kalıbı literal olarak anmıştı), yorum yeniden yazıldı; ikinci denemede **40 passed**. **Dokunulmayanlar (bilinçli):** Backend `events` tablosuna `module` kolonu eklenmedi (`engine/database.py` Kırmızı Bölge #6, DB migration + tüm `insert_event` call-site'larının güncellenmesi savaş zamanı riski — frontend classifier yeterli koruma sağlıyor); `api/routes/health.py::_build_recent_events` şeması dokunulmadı (yeni alan opsiyonel değil); `error_tracker.py` ve `ErrorTracker.jsx` ayrı bir akış (gruplama algoritması farklı), audit kapsamı dışı; event type'larını üreten engine modülleri dokunulmadı — yalnız tüketici (Monitor.jsx) akıllandırıldı. **Etki analizi:** 1 dosya (desktop, Yeşil Bölge) + 1 test, ~55 satır JSX ekleme (helper + 2 tablo + fonksiyon) + 8 satır hesap bloğu refactor + ~70 satır test; Siyah Kapı / Kırmızı Bölge / Sarı Bölge dokunusu YOK; çağıran zinciri tek (`errorCounts` sözlüğünü modStatus, ModBox detail grid ve hata sayacı grid kullanır — hepsi aynı sözlüğü okur, yeni imza geriye dönük uyumlu); backend değişikliği GEREKSİZ. **Doğrulama:** critical_flows **40/40 yeşil** (39 baseline + 1 yeni Flow 4g), 2.94s; Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.60s, 0 hata, dist/ üretildi: index.js 882.41 kB). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (Electron bundle yeniden yüklenir). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 11.4.2-11.4.5 + Bölüm 16 B25. (desktop/src/components/Monitor.jsx, tests/critical_flows/test_static_contracts.py)
- #169 — Widget Denetimi B-finding fix: `engine/event_bus.py::emit` dup-key bug'i giderildi (C2 — Sarı Bölge değil, Yeşil Bölge `event_bus.py`, 1 fonksiyon, ~15 satır refactor). **Kök neden:** A3 fix sırasında tespit edilen bu bulgu event_bus'un `emit` fonksiyonunda dict literal + spread deseninden kaynaklanıyordu: outer `event` anahtarı önce set ediliyor, sonra iç `data` sözlüğü spread ile üzerine yazılıyordu. İç `data`'da `type` anahtarı varsa dict spread kuralları gereği inner değer outer'i eziyordu. Canlı etkisi: `engine/h_engine.py`'de 3 yerde (`_check_end_of_day` satır 701, `_check_direction_flip` satır 793, `_check_daily_reset` satır 2593) `_emit("notification", {"type": "hybrid_eod"|"hybrid_direction_flip"|"hybrid_daily_reset", ...})` çağrıları yapılıyordu; bu çağrılar sonrası `drain()` payload'i `{"type": "hybrid_eod", ...}` olarak çıkıyor, Dashboard'daki `if (msg.type === 'notification')` WS branch'i (satır 379) hiçbir zaman eşleşmiyor, bildirim drawer'a WS üzerinden hiç düşmüyordu — ÖLÜ KOD. Notifications sadece HTTP `getNotifications({limit:50})` polling path'inde geliyor, WS canlı gelmiyor; kullanıcı hibrit EOD uyarısını gecikmeli alıyor, direction flip ve daily reset ise drawer'a hiç yansımayabiliyordu (polling 20sn interval). **Neden A3'te çözülmedi?** A3 fix'i display-layer filter kararı aldı (`shouldShowNotification`) ve WS branch'i ölü kod olarak not etti; WS canlılığı için event_bus fix'i ayrı B-finding olarak işaretlendi. **Atomik değişiklik (1 dosya + 1 test):** `event_bus.py::emit` dict literal `{"type": event, **(data or {})}` yerine explicit merge ile değiştirildi: `payload: dict = dict(data or {})` → eğer `type` iç data'da varsa `payload.pop("type")` ile çıkarılıp `payload.setdefault("notif_type", inner_type)` ile `notif_type` altına taşındı → `payload["type"] = event` ile outer event type her zaman dış `type` olarak yazıldı. `setdefault` kullanımı: caller zaten `notif_type` set ettiyse override edilmez, explicit değer korunur. Dashboard'daki `shouldShowNotification` zaten `msg?.notif_type || msg?.type` okuyor — frontend değişikliği GEREKSİZ. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_event_bus_emit_preserves_outer_type` — 3 aşamalı: (a) **Runtime:** `emit("notification", {"type": "hybrid_eod", "title": ..., "message": ..., "severity": ...})` çağrısı sonrası `drain()` payload'inde `ev["type"] == "notification"` (outer korundu) + `ev["notif_type"] == "hybrid_eod"` (inner taşındı) + diğer alanlar (message) korundu; (b) **Override koruma:** caller zaten `notif_type` set ettiyse `setdefault` override etmez — `emit("notification", {"type": "X", "notif_type": "explicit_value"})` sonrası `notif_type == "explicit_value"` kalır, `X` sessizce düşer; (c) **Statik kaynak kontrolu:** `inspect.getsource(event_bus.emit)` içinde eski dup-key dict literal deseni YASAK + `notif_type` anahtarı mevcut olmalı. Test docstring'deki yanıltıcı pattern ilk denemede false positive verdi, `event_bus.py` docstring'inden dup-key pattern literal'i çıkarıldı, genel açıklamaya çevrildi. Gelecekte emit refactor'u dup-key'i geri eklerse pre-commit hook commit'i bloklar. **Dokunulmayanlar (bilinçli):** `engine/h_engine.py` emit call-site'ları (3 yer) dokunulmadı — fix event_bus merkezindedir, call-site'lar aynı `{"type": "hybrid_eod", ...}` formatını kullanmaya devam edebilir, event_bus artık bunu güvenle ele alıyor; Dashboard WS branch'i (satır 379-395) dokunulmadı — `msg.type === 'notification'` check'i artık gerçekten tetiklenecek, `shouldShowNotification` filtresi zaten yerinde (A3'ten); mevcut `getNotifications` HTTP path'i dokunulmadı — fallback olarak kalmaya devam ediyor. **Etki analizi:** 1 dosya (engine, Yeşil Bölge) + 1 test, ~15 satır net refactor, ~55 satır test; Siyah Kapı / Kırmızı Bölge / Sarı Bölge dokunusu YOK; tüketici zinciri — `api/routes/live.py::_event_drain_loop` → `broadcast(ev)` → WS → Dashboard (A3 filter) → notification drawer; geriye dönük uyumluluk — eski call-site'lar aynı davranır, yeni `notif_type` alanı Dashboard zaten okuyor, hiçbir tüketici kırılmadı. **Doğrulama:** critical_flows **39/39 yeşil** (38 baseline + 1 yeni Flow 4f), 2.56s; build gereksiz (backend-only Python değişikliği, React/JSX dokunulmadı). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için `restart_app` yeterli (API modül reload). Kaynak: A3 session raporundaki "Dokunulmayanlar" bölümünde tespit edilen B-finding. (engine/event_bus.py, tests/critical_flows/test_static_contracts.py)
- #168 — Widget Denetimi Bulgu B9 fix: Monitor motor durum rozetleri gerçek sinyallere bağlandı (C1 — Yeşil Bölge `desktop/src/components/Monitor.jsx`, tek fonksiyon, ~35 satır refactor). **Kök neden:** `Monitor.jsx::modStatus` hesabında `manuel: 'ok'` **sabit hardcode** duruyordu — manuel motorda hata olsa bile rozet asla sarı/kırmızı olmuyordu. Ayrıca BABA/OĞUL/H-Engine/ÜSTAT rozetleri `engine_running=false` olduğunda bile "ok" görünebiliyordu (killLevel defaults 0, errorCounts kullanılmıyordu); `errorCounts` sözlüğü her cycle'da hesaplanıp modStatus tarafından referans edilmiyor, yalnız detail açılır paneline yazılıyordu (ölü değişken); OĞUL/H-Engine rozetleri `killLevel >= 2` (L2+ = motorlar durur, Anayasa 4.5 #10) kontrolünü yapmıyordu, yalnız `daily_loss_stop` bayrağına bakıyordu. Canlı L2 aktifken bile Monitor'da "OĞUL AKTİF" yeşil görünebiliyordu — kullanıcı "trading duruyor mu?" sorusunu Monitor'a güvenip yanıtladığında **yanıltıcı gösterim** alıyordu. Audit raporu 11.4.2-11.4.5: "🐛 `modStatus.manuel = 'ok'` SABİT HARDCODE — manuel motor hatası hiç warn/err göstermez. 🧱" + "BABA/ÜSTAT rozetleri sadece `engine_running`'e bağlı değil, ayrıca errorCounts ve killLevel ile zenginleştirilmeli". **Atomik değişiklik (1 dosya + 1 test):** `Monitor.jsx::modStatus` obje literal'i **conditional expression** ile değiştirildi. Yeni semantik: (a) `!engineRunning` → tüm modüller `'err'` (motor ölü, hiçbir rozet yeşil gösterilemez). (b) BABA: `killLevel >= 3 → err`, `killLevel > 0 → warn`, `errorCounts.baba > 0 → warn`, aksi `ok`. (c) OĞUL: `killLevel >= 2 → err` (YENİ — L2+ motorlar durdu), `daily_loss_stop → err`, `errorCounts.ogul > 0 || reject_count > 0 || timeout_count > 0 → warn`, aksi `ok`. (d) H-Engine: `killLevel >= 2 → err` (YENİ), `errorCounts.hengine > 0 → warn`, `active_hybrid_count > 0 → warn`, aksi `ok`. (e) ÜSTAT: `cache_stale → warn`, `errorCounts.ustat > 0 → warn`, aksi `ok` (kill-switch'te durmaz, risk motoru değil). (f) MANUEL: `errorCounts.manuel > 0 → warn`, aksi `ok` — **hardcode 'ok' KALDIRILDI**. `errorCounts` sözlüğü artık ölü değişken değil, modStatus tarafından ana referans. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_monitor_modstatus_not_hardcoded` — Monitor.jsx'i text olarak okur, modStatus bloğunu regex ile izole eder ve (1) `manuel: 'ok'` / `manuel: "ok"` literal'lerinin KALDIRILDIĞINI, (2) `engineRunning` referansının modStatus blogunda bulunduğunu, (3) `errorCounts` referansının blokta olduğunu, (4) `killLevel >= 2` L2+ kapısının OĞUL/H-Engine için yazıldığını assert eder. Gelecekte refactor sırasında hardcode geri eklenirse pre-commit hook commit'i bloklar. **Dokunulmayanlar (bilinçli):** `errorCounts` substring parse kırılganlığı (audit 11.4.2 B-finding — `msg.includes('baba')` yanlış pozitif riski) ayrı bir maddeye bırakıldı; backend `/api/manuel-motor/health` endpoint'i henüz yok — manuel rozet şu an yalnızca event log parse ile "en azından hata görünmüyor" garantisini veriyor (eski tam hardcode'tan net iyileşme, tam çözüm değil); BABA chip `killLevel >= 2` için `warn` kaldı (audit 11.4.2 "L2=warn Doğru" kararı korundu — BABA risk motoru, L2'de haberdar ve çalışıyor; OĞUL/H-Engine durduğu için onlar `err`). **Etki analizi:** 1 dosya (desktop, Yeşil Bölge), 1 test, ~35 satır JSX + ~45 satır test; Siyah Kapı/Kırmızı Bölge/Sarı Bölge dokunusu YOK; çağıran zinciri tek (`ModBox status` prop'una geçen değer), tüketici zinciri tek (Badge renk eşleme `ok/warn/err`); backend değişikliği GEREKSİZ (mevcut `/api/health.recent_events` + `/api/status.engine_running` + `/api/risk.kill_switch_level` alanları zaten mevcut). **Doğrulama:** critical_flows **38/38 yeşil** (37 baseline + 1 yeni Flow 4e), Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.63s, 0 hata, dist/ üretildi). Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için build + Electron restart gerekli (frontend bundle yenilenir). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 11.4 + Bölüm 17 A4 (B9). (desktop/src/components/Monitor.jsx, tests/critical_flows/test_static_contracts.py)
- #167 — Widget Denetimi Bulgu S1 fix: Bildirim tercihleri gerçek davranışa bağlandı + backend kalıcılık (C3 — `config/default.json` Kırmızı Bölge sadece yeni `ui.notification_prefs` bölümü eklendi, risk/strateji/eşik sabiti DOKUNULMADI). **Kök neden:** Settings sayfasındaki 5 toggle (`soundEnabled`, `killSwitchAlert`, `tradeAlert`, `drawdownAlert`, `regimeAlert`) `localStorage['ustat_notification_prefs']` + `POST /api/settings/notification-prefs` yapıyordu, ancak backend `api/routes/settings.py::_notification_prefs` **process belleğinde dict** idi (restart → bellek sıfırlanır, yalnız `DEFAULT_PREFS` kalır); dahası projenin hiçbir yerinde bu dict'i okuyan kod yoktu (grep: 0 eşleşme). Frontend'de de hiçbir `new Audio`/`playSound`/`beep` altyapısı yoktu — audit "kullanıcı sessize aldım sanıyor ama sistem ses çıkarıyor" dedi, gerçek daha net: hiç ses zaten yok, 5 toggle tamamen UI tiyatrosu. **Atomik değişiklikler (4 dosya + 1 test, 0 Siyah Kapı dokunusu):** (1) `config/default.json` — yeni `"ui": { "notification_prefs": {...} }` bölümü eklendi (5 anahtar, default değerler). Kırmızı Bölge #8 dokunusu ama risk/strateji/eşik/baseline parametreleri **DEĞİŞMEDİ**, yalnız UI namespace altında yeni bir alan. (2) `api/routes/settings.py` — eski `_notification_prefs: dict` modül sabiti **KALDIRILDI**, yerine: `DEFAULT_NOTIFICATION_PREFS` fallback sabiti + `_read_notification_prefs_from_config()` helper (engine.config'den oku, eksik key'leri default ile merge et, tip kontrolü boolean) + `GET /settings/notification-prefs` helper'dan okur + `POST /settings/notification-prefs` `config.set("ui.notification_prefs", ...)` + `config.save()` zinciri ile disk'e yazar (`update_risk_baseline` ile aynı persistence deseni). Engine yoksa warning log + `success=False`. (3) `desktop/src/components/Dashboard.jsx` — `notifPrefsRef` (`useRef`) + `readNotifPrefs()` helper + `shouldShowNotification(msg)` filtresi + `storage` event listener (Settings'te toggle değişince otomatik güncellenir) + HTTP fetch path'inde (`getNotifications({limit:50}).then(...)`) ve WS `msg.type === 'notification'` branch'inde filtre uygulanır. Filtre şu an yalnız `hybrid_*` tiplerini `tradeAlert` bayrağına bağlıyor; diğer tipler her zaman gösterilir (backend'de emit edilmiyor zaten). (4) `desktop/src/components/Settings.jsx` — toggle bölümünün üstüne dürüst kapsam bilgi kutusu (`"Kapsam: Şu an yalnızca İşlem & Hibrit Uyarıları toggle'ı gerçek davranışa bağlıdır..."`) + `tradeAlert` label'ı "İşlem bildirimi" → "İşlem & Hibrit Uyarıları" olarak netleştirildi + diğer 4 toggle açıklamasına "(henüz aktif değil)" notu eklendi. Kullanıcı artık hangi toggle'ın gerçekten davranış ürettiğini biliyor. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_notification_prefs_persists_via_config` — (a) eski `_notification_prefs: dict` tanımının KALDIRILDIĞINI, (b) `DEFAULT_NOTIFICATION_PREFS` sabitinin 5 anahtarla var olduğunu, (c) `_read_notification_prefs_from_config` helper'ının `ui.notification_prefs` key'ini kullandığını, (d) `update_notification_prefs` POST'unun `config.set("ui.notification_prefs", ...)` + `config.save()` çağırdığını `inspect.getsource` ile statik olarak doğrular. Gelecekte bellek tabanlı dict geri eklenirse pre-commit hook commit'i bloklar. **Dokunulmayanlar (bilinçli):** `engine/h_engine.py` notification emit noktaları (Sarı Bölge ama gereksiz risk), `engine/event_bus.py` (ayrı bir bulgu: `emit("notification", {"type": "hybrid_eod", ...})` payload'ında dup-key nedeniyle dış `type` iç `type`'a ezilir → Dashboard'daki `msg.type === 'notification'` WS branch'i şu anda ölü kod — ancak HTTP fetch path canlı, filtre orada aktif), yeni notification türleri (kill_switch/drawdown/regime emit) — ayrı A-maddesi. **Etki analizi:** 4 dosya + 1 test, ~100 satır net ekleme; Siyah Kapı fonksiyonu dokunulmadı; çağıran zinciri: Settings → Dashboard (localStorage+storage event), Dashboard → notifications drawer (filtre sonrası); tüketici zinciri: yeni field/key'leri kimse eski yolla okumuyor, geriye dönük uyumluluk korundu. **Doğrulama:** critical_flows **37/37 yeşil** (36 baseline + 1 yeni), py_compile temiz (`api/routes/settings.py`, `api/schemas.py`, `api/routes/trades.py`), Windows-side `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, 2.67s, 0 hata, dist/ üretildi) — Vite JSX compile Dashboard.jsx + Settings.jsx syntax temiz. Piyasa kapalı (Cumartesi, Barış Zamanı). Deploy için API restart gerekli (backend modül reload). Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 14.5 + Bölüm 17 A3 (S1). (config/default.json, api/routes/settings.py, desktop/src/components/Dashboard.jsx, desktop/src/components/Settings.jsx, tests/critical_flows/test_static_contracts.py)
- #166 — Widget Denetimi Bulgu B2 fix: Trade veri güvenilirliği — `best_trade`/`worst_trade` seçiminde MT5 netting anomalileri hariç tutuluyor (C1 Yeşil Bölge — `api/routes/trades.py`, `api/schemas.py`). **Kök neden:** Canlı `/api/trades/stats` çıktısında `best_trade = F_TOASO BUY 8 lot entry=259.87 exit=257.80 pnl=+2705` tespit edildi — BUY için exit<entry zarar olmalıyken pnl +2705. DB sorgusunda benzer örüntü: F_TKFEN SELL 104.26→104.40 pnl=+485, F_KONTR SELL 8.90→8.91 pnl=-7925 (magnitude 53x). Örüntü `engine/mt5_bridge.py::get_history_for_sync` (satır 2030-2135) kaynaklı: MT5 netting mod'unda aynı `position_id` altında scale-in/out veya ters dönüşler tek pozisyon olarak birleştirilir; fonksiyon `entry_price` ve `exit_price` için weighted average hesaplar, `pnl` için MT5 raw profit toplamını alır. Parçalı pozisyonlarda `(exit-entry)×lot×multiplier ≠ MT5 pnl` — matematik çelişir. `best_trade = max(items, key=lambda t: t.pnl)` ile seçildiği için anomali kayıt Performans sayfasında "En Kârlı İşlem" olarak kullanıcıya gösteriliyordu → veri güvenilirliği algısı bozuluyordu. **Atomik değişiklik (C1, 3 dosya, 0 Kırmızı Bölge dokunusu):** (1) `api/schemas.py::TradeItem` — `data_warning: str | None = None` alanı eklendi (`"SIGN_MISMATCH"` = parçalı/ters netting pozisyon şüphesi). (2) `api/schemas.py::TradeStatsResponse` — `anomaly_count: int = 0` alanı eklendi (UI "N işlem anomali nedeniyle hariç" banner'ı için). (3) `api/routes/trades.py` — yardımcı fonksiyon `_check_trade_consistency(direction, entry, exit, pnl)` eklendi: pnl ve `direction × (exit-entry)` işaretleri çelişirse `"SIGN_MISMATCH"`, aksi halde `None`. (4) `_to_trade_item` bu kontrolü her trade için çağırıp `data_warning` alanını dolduruyor. (5) `get_trade_stats` `clean_items = [t for t in items if t.data_warning is None]` filtresi ile `best_trade` ve `worst_trade`'i temiz kayıtlardan seçiyor; `anomaly_count` hariç tutulan trade sayısını UI'a iletiyor. **Kritik koruma:** `total_pnl`, `win_rate`, `avg_pnl`, `winning_trades`, `losing_trades` hesapları filtreden ETKİLENMEZ — pnl değerleri MT5 ground truth olarak kalır; yalnız best/worst seçimi temizlenir. **Mimari karar:** `engine/mt5_bridge.py::get_history_for_sync` DOKUNULMADI (Kırmızı Bölge #3, sync mantığını değiştirmek DB migration + re-sync + Savaş Zamanı riski demektir). Bunun yerine anomaliler tespit edilip filtreleniyor; ham veri DB'de korunuyor, UI'a doğru özet sunuluyor. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_trade_stats_excludes_sign_mismatch_from_best_worst` — helper fonksiyonun varlığını, `TradeItem.data_warning` + `TradeStatsResponse.anomaly_count` field'larını, `get_trade_stats` içinde `data_warning` filtresini ve `_check_trade_consistency`'nin 5 canonical durumda doğru sonuç ürettiğini (BUY/SELL × tutarlı/tutarsız × pnl=None) `inspect` + `Pydantic model_fields` ile doğrular. **Etki analizi:** 3 dosya, ~60 satır net ekleme; Siyah Kapı/Kırmızı Bölge dokunusu yok; frontend değişikliği GEREKSİZ (yeni field'lar opsiyonel, `best_trade` otomatik olarak temiz gelmeye başlar). **Doğrulama:** critical_flows **36/36 yeşil** (35 baseline + 1 yeni), py_compile temiz. Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 16.2 B2 + Bölüm 17 A2. (api/schemas.py, api/routes/trades.py, tests/critical_flows/test_static_contracts.py)

### Security
- #165 — Widget Denetimi Bulgu B1 fix: Anayasa Kural #6 L2→L3 eskalasyon (C4 Siyah Kapı #1 `check_risk_limits` — ADDITIVE bug fix, davranış genişlemesi): Widget denetimi sırasında canlı `/api/risk` çıktısında `total_drawdown_pct=0.304293` (%30.43) ama `kill_switch_level=2` tespit edildi; limit `hard_drawdown=0.15`. Anayasa Kural #6 "hard drawdown ≥%15 → L3 otomatik" ölü görünüyordu. **Kök neden:** `engine/baba.py::check_risk_limits` (Siyah Kapı #1) L2 aktifken 2. blokta koşulsuz `return verdict` yapıyordu (satır 1509-1525) → `_check_hard_drawdown` (Siyah Kapı #6) çağrısı yalnızca L0/L1 seviyesinde çalışıyordu → L2 önceden tetiklendikten sonra drawdown büyümeye devam etse bile otomatik L3 eskalasyonu yapılmıyordu. Canlı olay sırası: saat 16:10:49 `daily_loss` ile L2 tetiklendi; sonrasında floating zarar %15 eşiğini aşıp %30'a ulaştı; her 10 sn'de `check_risk_limits` çağrıldı ama `_check_hard_drawdown` tek seferlik de çalışmadı (grep: `_check_hard_drawdown` tüm kod tabanında 1 çağrı noktası). **Mantık çelişkisi:** Kural #3 monotonluk SADECE düşürmeyi yasaklar (L2→L1 YASAK); yukarı yönde eskalasyon (L2→L3) monotonluğa UYGUN ve zorunludur. **Atomik değişiklik:** `engine/baba.py` L2 return bloğunun başına hard drawdown kontrolü eklendi — L2 aktifken `_check_hard_drawdown(risk_params, snap=snap)` çağrılır, `"hard"` dönerse `_activate_kill_switch(L3, "hard_drawdown", ...)` ile L3'e yükseltilir ve verdict L3 bilgileriyle döner; `"hard"` değilse orijinal L2 davranışı (can_trade=False, risk_multiplier ks_reason'a göre) aynen çalışır. L0/L1 → L3 yolu (satır 1554) değişmedi, ikinci tetik noktası olarak korundu. **Etki analizi:** 1 dosya, 1 fonksiyon, ~20 satır net ekleme; `_check_hard_drawdown` imzası değişmedi; tüketici zinciri (`ogul.py::_execute_signal` BABA `can_trade` kapısı) değişmedi; `_activate_kill_switch` → `_close_all_positions` zinciri zaten L3'te çağrılıyordu — davranış değişimi SIFIR yeni state, mevcut state'e erişim yolu açıldı. **Yeni statik sözleşme testi:** `tests/critical_flows/test_static_contracts.py::test_check_risk_limits_escalates_l2_to_l3_on_hard_drawdown` — L2 blok içinde `_check_hard_drawdown` çağrısı, `KILL_SWITCH_L3` eskalasyon yolu ve `hard_drawdown` reason varlığını inspect ile doğrular. Gelecekte refactor sırasında eskalasyon yolu silinirse pre-commit hook commit'i bloklar. **Doğrulama:** critical_flows 35/35 yeşil (34 baseline + 1 yeni). Piyasa açık ama deploy kullanıcı kontrolünde — fix commit'lendi, `restart_app` YAPILMADI; kullanıcı deploy zamanlamasına karar verecek. Deploy sonrası sistem canlı L2'den L3'e yükselecek ve 1 açık pozisyonu kapatacak (amaç bu). Anayasa Kural #3 uyum: monotonluk yukarı yönde geçerli. Kaynak: `docs/2026-04-11_widget_denetimi.md` Bölüm 16.2 B1. (engine/baba.py, tests/critical_flows/test_static_contracts.py)

### Changed
- #164 — v6.0.0 versiyon bump + "ÜSTAT Plus V6.0" ürün adı yeniden markalama (build: semver+rebrand, davranış değişmez): v5.9.0 → v6.0.0 majör bump ve uygulama adının "ÜSTAT" → "ÜSTAT Plus V6.0" olarak yeniden markalanması. **Bump gerekçesi (CLAUDE.md Bölüm 7 Adım 3):** Son versiyon commit'i `e57b008` (v5.9 ilk, 2026-03-28) ile HEAD arası `git diff --shortstat`: 202 dosya, +89737 / -5096 satır = 94833 toplam. Toplam izlenen kod satırı 168064 → **ham oran %56.4**. Docs hariç üretken kod (engine+api+desktop+tests) oranı **%33.7** — her iki metrikte de %10 eşiğinin çok üzerinde, versiyon artışı zorunlu. Ek olarak `baba.py` modül docstring'lerinde 13 kez `"v5.9.3 — BULGU"` etiketi (informal internal convention) mevcut, versiyon dosyaları 5.9.0'da kalmış — hizalanma gerekliydi. CLAUDE.md kuralı "minor 9'dan sonra major artar" → 5.9 → **6.0**. **22 atomik patch (tek commit):** Fonksiyonel sabitler (9): `engine/__init__.py` VERSION, `config/default.json` version, `api/server.py` docstring+API_VERSION+title+root_name, `api/schemas.py` version field, `desktop/package.json` version+description; UI/Electron (13): `desktop/main.js` docstring+APP_TITLE+splash HTML, `desktop/preload.js` docstring, `desktop/mt5Manager.js` docstring (v5.7→V6.0 — geçmiş bump'ta atlanmıştı, fırsatla düzeltildi), `desktop/src/components/TopBar.jsx` docstring+layout comment+h1 logo, `desktop/src/components/LockScreen.jsx` docstring+h1 logo, `desktop/src/components/Settings.jsx` docstring+VERSION const+"Uygulama" FieldRow; Kısayol script'leri (5): `create_shortcut.ps1` header+filename+Description, `update_shortcut.ps1` $newName+Description. **Ürün adı yapısı:** "ÜSTAT" → "ÜSTAT Plus" (UI başlıkları), sürüm etiketi `v5.9` → `V6.0` (büyük V). Splash screen, TopBar logo, LockScreen logo, Electron APP_TITLE, Settings Uygulama satırı hepsi yeni isim. Kısayol dosya adı `USTAT v5.9.lnk` → `USTAT Plus V6.0.lnk` (kullanıcı `create_shortcut.ps1` script'ini çalıştırarak güncelleyecek). **Koruma kapsamı:** Tarihsel/arkeolojik yorumlardaki `v5.9.2`, `v5.9.3 — BULGU`, `// v5.9: ...` stili referanslara DOKUNULMADI — bunlar "bu değişiklik ne zaman yapıldı" etiketi, ürün adı değil. Test altyapısı (`tests/critical_flows/*`) değişmedi. Davranış değişimi sıfır (yalnız string/sabit değerleri). **Doğrulama:** py_compile `engine/__init__.py` + `api/server.py` + `api/schemas.py` temiz, critical_flows **34/34 yeşil**, production `npm run build` başarılı (`ustat-desktop@6.0.0`, 728 modül transformed, dist/ üretildi: index.html 1.06 kB, index.css 90.52 kB, index.js 880.33 kB). Etki analizi: 13 dosya, 22 patch, 0 davranış değişimi, 0 çağıran zinciri kırığı. Piyasa kapalı doğrulandı (Cumartesi, Barış Zamanı) (engine/__init__.py, config/default.json, api/server.py, api/schemas.py, desktop/package.json, desktop/main.js, desktop/preload.js, desktop/mt5Manager.js, desktop/src/components/TopBar.jsx, desktop/src/components/LockScreen.jsx, desktop/src/components/Settings.jsx, create_shortcut.ps1, update_shortcut.ps1)

---

## [5.9.0] — 2026-03-28 / 2026-04-10

### Security
- #163 — BULGU #12 dead module constant