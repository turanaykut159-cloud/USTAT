# v5.8/CEO-FAZ2: Test suite aktivasyonu
# Testler: USTAT DEPO/tests_aktif/
# Çalıştırma: pytest (proje kökünden)
[pytest]
testpaths = USTAT DEPO/tests_aktif
pythonpath = .
markers =
    slow: yavaş testler (UI smoke testleri)
    integration: entegrasyon testleri
    unit: birim testleri
asyncio_mode = auto
